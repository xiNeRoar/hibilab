# HIBI LAB Store — WordPress / WooCommerce 交接文件

> 給接手嘅 WP dev。呢份係「點樣將 HTML 設計交付物 轉成 Scent.M bespoke theme 入面嘅 WooCommerce 店中店」嘅完整跟進指引。設計稿全部喺 repo 根目錄嘅 `HIBI LAB *.html`。**本文件＝現行單一真相源；§§21–25 必須一併遵守：§21＝商業／UX 決策、§22＝實作規範、§23＝視覺／狀態合約、§24＝安全／效能／SEO 加固、§25＝營運／供應商／交付契約。任何直接衝突都係文件缺陷，必須提出並修正，唔准自行估優先次序。**

---

## 0. 定位（好緊要，先睇）
- **店中店（shop-in-shop）**：HIBI LAB 店係**套落現有 Scent.M bespoke theme 入面**，唔係另起新站、唔係將 Scent.M 改成純網店。
- **WooCommerce data-driven**：客淨係喺 wp-admin 入結構化 product data，前端 category / filter / 價自動出，唔使逐件寫 code。
- **入口（共 4 個，flip 時全數駁通）**：① Scent.M 全螢幕選單「Explore Hibi Lab」② footer「Hibi Lab →」③ 首頁「Explore Store」CTA（theme 現時三個都係 inert `data-coming-soon` stub）④ **top-nav utility cluster 嘅「HIBI LAB」文字 link（`.hibi-entry`，見 `scentm-home.html`；手機 nav 唯一店入口 — theme 未有，flip 時加入 `header.php`，flip 前唔好加（避免多一個死 link））**。
- **Scent.M 整合時序**：Scent.M 站現時只喺 staging、未 production —— **先做好 HIBI LAB，之後先整合入 Scent.M**。三個 coming-soon stub 嘅「即將開幕」標示／隱藏處理、第 4 入口、首頁描述句等入口層決定全部推遲到整合階段（見 §22 flip checklist）。
- **共通 chrome（nav / footer / 全螢幕選單 / WhatsApp / GSAP+Lenis）= 同 Scent.M 100% 一致**。設計稿已經 embed 咗真 chrome；轉 theme 時共通部分直接用返 theme 現有 partial，唔好另整。
- **同一個 WordPress 後台（硬性契約）**：HIBI LAB 係 Scent.M 子網店，並非另一套管理產品。必須沿用現有 `scentm_manager`、左側選單分組、權限收窄方式、港式書面語、WordPress／WooCommerce 原生表格／tabs／notice／按鈕及操作節奏；唔准另起獨立 Dashboard、另一套 admin design system 或另一個管理角色。完整規格見 §21.A.6。

---

## 1. 技術設定
- `add_theme_support('woocommerce')` + `woocommerce/` template override，維持 bespoke 設計全控制（唔使 Blocksy 等）。
- 沿用現有 **workshops CPT / template + ACF pattern**（archive ↔ single、Load-More `/page/N/`、image fallback、bilingual label）。
- Workshops 現有 Stripe 直駁線照舊；Products 用 **WooCommerce Stripe gateway**（on-site checkout、multi-item cart）。兩條 commerce 線並存，互不干擾。
- **Design tokens**：色 `#FDFBF9`(light) / `#F3EAE1`(beige) / `#A64B2A`(terracotta) / `#3B261D`(brown) / `#3B4A3D`(moss，寵物) / `#e8b896`(gold accent)。字 Baskervville + Cactus Classical Serif（serif）、Montserrat + Noto Sans TC（sans）。
- **狀態／輔助 tokens**：成功 `#1f7a4d` · 錯誤 `#b4452a`（付款錯誤 banner 另用 `#fbeae4`/`#e3a58c`/`#8a3618` 三色組）· hairline 分隔 `#ece0d5` · input 邊框 `#d8c8ba` · 售罄灰掣 `#cbb8a6`（§20.8 售罄 disabled 掣色；_p15 售罄樣本已 render）· 圖片 fallback 底 `#efe6dd`。
- **Breakpoint**：store 頁用 **771px**（刻意 store-scoped）— 同 Scent.M theme 嘅 768px 並存，**唔好「修正」統一**（落地方式見 §22）。

### 1.1 唯一 component ownership（後文全部用呢張表解讀）

| Component | 唯一 owner | 可以做 | 絕對唔可以做 |
|---|---|---|---|
| Scent.M bespoke Theme | 前台 render／HTML／CSS、store chrome、已批准 client interaction；只呼叫下列 server contract | Woo template skin、分類／搜尋／cart UI、載入更多、顯示 server projection | 保存或裁決價錢、VIP、付款、退款、庫存、出貨；實作 privileged AJAX／REST mutation；另開第二套狀態機 |
| `hibilab-commerce` | HIBI policy/application layer及所有 HIBI server controllers | VIP／best-wins／免運規則、filter/search/cart server endpoint、server validation、付款／退款／庫存／出貨 read projection、durable jobs／incidents | 取代 Woo order/cart/stock ledger；直接建立／退款 Stripe object；複製官方 gateway webhook |
| WooCommerce core＋官方 Woo Stripe Gateway | Cart、coupon、Checkout、order/item、stock、付款／退款 provider lifecycle及原生 endpoint | 交易 mutation及canonical commerce truth | 由 Theme 或 HIBI 私有 meta 反向改寫 provider truth |
| ShipAny／Scent.M Mail Center／Subscriber service | 各自 provider integration／寄送／訂閱真相 | 只經本文批准 adapter contract同HIBI交換最小 projection／receipt | 將自己嘅 dashboard/status 當第二套 Woo payment、order或VIP truth |

後文「custom in-theme」只指**畫面及 browser interaction**；同一句涉及 query、price、cart mutation、authorization、REST／AJAX callback或持久資料時，server owner一律係`hibilab-commerce`並再次呼叫Woo公開API。後文「Theme只skin／唔改邏輯」指唔取代Woo交易生命週期，唔代表`hibilab-commerce`可以省略本文要求嘅server gate、projection及公開hooks。呢張表優先於任何章節嘅縮寫句，Dev不得因檔案方便將server business logic放回Theme。

---

## 2. 頁面清單（HTML 稿 → 對應 WC template · 完整 inventory）
| 設計稿 | 對應 template | 類型 |
|---|---|---|
| `HIBI LAB Landing.html` | 自訂 page template | 品牌 landing |
| `HIBI LAB Shop.html` | `archive-product.php` + `content-product.php` 卡 + filter | PLP（★ filter 重點） |
| `HIBI LAB Product.html` | `single-product.php` | PDP |
| `HIBI LAB Cart.html` | `cart/cart.php` override | 購物車 |
| `HIBI LAB Checkout.html` | `checkout/form-checkout.php` override | 結帳（詳見 §13/§17） |
| `HIBI LAB Order Received.html` | `checkout/thankyou.php` override | 訂單完成（跟 Scent.M `page-thanks.php` 版式） |
| `HIBI LAB Account.html` | `myaccount/*` override（logged-in） | 會員中心（詳見 §9/§18） |
| `HIBI LAB Account Login.html` | `myaccount/form-login.php` + `form-lost-password.php` + `form-reset-password.php`（logged-out） | 登入／註冊／忘記／重設（詳見 §18） |
| `HIBI LAB Shipping.html` | 靜態 page | 送貨 & 退換政策 |
| `HIBI LAB 404.html` | `404.php`（按 URL 分流：`REQUEST_URI` 以 `/hibi-lab/`（或 WC journey slug）開頭 → render HIBI 版；否則 render 現有 Scent.M 版。兩個設計原封不動，§21） | 404 |
| `HIBI LAB Tier Cards.html` | 會員等級卡設計（融入 `myaccount` dashboard，動態 render，§5/§21） | 等級卡 live 樣板（seed：一般／銀級／金級＋進度條） |
| `HIBI LAB Email.html` | 後台可編輯 email 範本（option 儲存，§21/§22）。**六款齊**（⑥補購提醒樣本喺 _p24） | 交易 email 設計稿 |
| `index.html` | —（= `HIBI LAB Landing.html` 嘅 byte-identical alias，方便 repo 直開） | alias |
| `scentm-home.html` | —（Scent.M 首頁 chrome 整合示範：Explore Hibi Lab 連結點樣駁去店；非 HIBI 頁面） | 整合參考 |
| `_p01-states-book.html`＋`states-img/` | —（**全狀態型錄＝review helper**：50 格狀態說明／互動預覽；其中 49 格有 review 截圖，新增付款待處理 helper 暫無截圖；**本文件同 linked HTML 先係現行規範，截圖只係非規範 review snapshot**；詳見 §23.1） | 型錄 helper |
| `_p13-shop-emptycat.html` | —（PLP 空分類狀態獨立示範，對應 §22 空狀態變體 (a)） | 狀態示範 helper |
| `_p14`–`_p30` 樣本檔（包括 `_p29-orderreceived-payment-states.html` 付款待處理 helper） | —（**狀態樣本檔，唔上線**：每檔＝一個預設 demo 見唔到嘅狀態嘅視覺樣本；清單＋行為合約補充見 **§23**） | 狀態示範 helper |

---

## 3. 產品 data model（客喺 wp-admin 入，唔使逐件寫 code）
- **分類** = `product_cat`（護膚品 / 寵物護理 / 助眠系列 / 家居香氛），hierarchical，客自己加減。
- **分類入口動態化**：Landing 4 張入口卡、Shop 分類 pills、PLP 橫幅 map **全部由 WP loop top-level `product_cat` terms 動態 render**（唔 hardcode 4 個 slug）；每個分類固定用 committed ACF term fields：`hibilab_category_eyebrow`（text、純文字、最多 60 grapheme）、`hibilab_category_title`（text、必填、最多 100 grapheme）、`hibilab_category_intro`（textarea、純文字、最多 300 grapheme）、`hibilab_category_banner`（image、attachment ID return）。現有 4 個分類嘅稿內文案＋`cat-*.png` 做 seed 預設值。客加第 5 個分類 = 入口卡／pill／橫幅／archive 自動齊；缺 banner 用現有無圖 fallback，缺 title 就用 term display name，唔用另一份 option。Demo 稿嘅 4 卡／4 pill 係呢個 loop 嘅視覺樣板。
- **產品唯一分類context（唔加「主分類」UI）**：需要單一分類做related products／客面系列label時，從該產品已指派而且可見嘅`product_cat`中，固定揀hierarchy depth最深者；同深度先按canonical term slug做bytewise ascending，再按term ID升序，結果跨request可重現而唔依賴`get_the_terms()`回傳順序或不存在嘅category `menu_order`欄。多個同深度候選唔會令客面跳來跳去，但會喺產品編輯頁及System Status顯示資料警告，提示Admin收斂分類；零分類就唔顯示系列label／related section。Related products先用該canonical leaf，無結果先用其直接parent；禁止由陣列第一個、顯示名稱或query順序猜。
- **Filter 軸 = global product attributes**（一次過設定 term，之後每件剔；後台顯示港式中文 label、程式固定用 ASCII machine slug）：`pa_benefit`（功效）/ `pa_audience`（適用對象：人/貓/狗/居家）/ `pa_scent`（香味：單一香味成分 term + swatch，**只做 filter，唔用作 variation**）/ `pa_size`（容量）/ `pa_form`（形態：噴霧/按摩油/洗髮水/消炎水/精華/潔面/擴香）。→ filter facet + count **自動由 attribute 生成**，加新 term 客即場加。呢組 machine slug 一經 production 建立不可改名；新增 term 使用穩定 ASCII term slug，中文只係顯示名稱。`hibilab-commerce`用versioned、idempotent migration建立／核對以下固定physical contract；Files-only環境不可依賴Local DB預先存在attribute taxonomy：

  | taxonomy | Woo attribute type | public archive | sort order | 用途／額外欄位 |
  |---|---|---|---|---|
  | `pa_benefit` | `select` | off | `menu_order` | PLP filter；term由Admin排序 |
  | `pa_audience` | `select` | off | `menu_order` | PLP filter＋卡面「適用」單一來源 |
  | `pa_scent` | `select` | off | `menu_order` | PLP原子香味filter；term ACF `hibilab_scent_swatch`只准空值或`#RRGGBB`，空值以文字label降級，唔另存圖片／CSS class |
  | `pa_size` | `select` | off | `menu_order` | variation＋容量filter |
  | `pa_form` | `select` | off | `menu_order` | PLP filter |
  | `pa_scent_formula` | `select` | off | `menu_order` | variation專用，永不加入PLP filter |

  所有term content仍由Admin管理；migration只建立／核對taxonomy contract，唔覆寫已存在term名稱、slug或排序。任何attribute type／archive／machine slug漂移由System Status指出並令相關產品不可購買／filter不可發布，唔靜默開第二套taxonomy。
- **容量 = `pa_size` variation attribute** → variable product → PLP 顯示「HK$X.XX 起」、PDP size 揀掣、容量 filter。
- **Variation 模型（明確版）**：demo PDP 係**雙軸 variable product** — `pa_size`（容量、帶價）×`pa_scent_formula`（香味配方、完整配方名）；每個容量 × 配方組合係一個可購買 variation，使用 WC 原生 SKU／價錢／庫存／圖片／variation description。每個 variation 嘅供應狀態獨立按 WooCommerce 庫存與 backorder 設定判斷：有貨＝正常可購買；缺貨但接受 backorder＝顯示「預訂」並保持可選；缺貨且不接受 backorder＝disabled（見 §20.8）。例：「薰衣草・依蘭依蘭」係一個配方選項，而唔係兩個可購買選項。 `pa_scent_formula`必須用schema-as-code註冊成全站global product attribute（只供variation選項，唔加入PLP filter）；禁止用每件產品local attribute冒充，避免term ID／variation mapping跨環境漂移。
- **配方與 Filter 必須分開**：同一產品另行剔選佢包含嘅原子 `pa_scent` terms；父產品嘅 filter terms ＝所有已發佈配方 variations 所含香味嘅聯集。例：產品有「薰衣草・依蘭依蘭」及「洋甘菊・甜橙」兩款配方，父產品要標上「薰衣草／依蘭依蘭／洋甘菊／甜橙」四個 filter terms，客揀任何一個都會搵到該產品；暫時缺貨／預訂配方仍屬產品選項，產品卡及 PDP 再如實顯示供應狀態。永久下架配方先從 variation 及父產品 filter assignment 一併移除。`pa_scent_formula` 同 `pa_scent` **不可靠拆字或顯示名稱互相推算**，兩組資料由 admin 明確維護。
- **配方內容真相**：父產品介紹只放所有配方共用嘅內容；配方專屬文案用 WC variation description。若完整成分會隨配方改變，另加結構化 variation meta（唔准焊死喺 theme），選配方時連 SKU、圖片及成分區一併由同一份 variation payload 更新。Demo 目前只提供「薰衣草・依蘭依蘭」嘅精確成分；未有商戶資料前唔可以自行編造「洋甘菊・甜橙」成分。Variation專屬成分固定用schema-as-code meta key `_hibilab_variation_ingredients`，typed repeater rows=`{ingredient:string required max 120, benefit:string optional max 240}`；選中variation時同一個server payload一併更新SKU、圖片及成分。meta空時只可fallback去父產品`pdp_blocks`內**唯一一個**`spec_list`、`spec_kind=ingredients`而且`applies_to_all_formulas=true`嘅block；零個就唔顯示成分，超過一個或標記矛盾就產品save-time拒絕兼System Status告警，絕不猜。SKU、price、stock、weight、dimensions及shipping class一律只讀Woo product／variation原生欄位；`spec_list`只作客面展示，ShipAny preflight重讀Woo物流必需欄，唔由文字欄推算。
- **固定 ACF 商品欄位（逐件產品，跟 workshops ACF pattern）**：只放操作性資料—**建議補購間隔（`hibilab_repurchase_interval`，integer週、1–52；空＝此產品唔納入補購提醒 opt-in，§21.12）**、**預訂到貨期（`hibilab_preorder_leadtime`，純文字；空＝用全局預設 option `hibilab_preorder_leadtime_default`，出廠值「約 1 週到貨」，§19）**。兩個到貨期欄共用同一normalizer：strip全部markup、Unicode NFC、連續空白收成一格、trim後1–120 grapheme；超長／空白-only拒絕儲存，唔截斷，避免付款同意snapshot同客面文字不一致。產品介紹、功效、成分、用法、適用對象及規格全部只由§15 `pdp_blocks` Flexible Content擁有；現稿內容做seed blocks，唔可以同時再建一套top-level ACF editorial fields，避免兩份內容真相。
- ⚠️ **紀律**：data 入落**結構化欄位**（剔 attribute term），唔好淨係打落產品名／描述 → filter 讀 attribute，唔 parse 個名。

---

## 4. Filter 引擎 = custom storefront UI＋`hibilab-commerce` server controller（§21）
- **實作**：Theme沿用現有 journal AJAX partial + load-more + `scentmRevealScan` browser pattern；`hibilab-commerce`經公開WP/Woo API擁有同一個server query/controller，使用`tax_query`、授權／nonce、輸入schema及canonical response。Facet live counts 用「先攞符合現有 filter 嘅 product ID set → `wp_get_object_terms` 計數」（boutique 目錄規模零壓力）；價格 slider min/max 由 `wc_product_meta_lookup` 攞；URL 參數使用 machine slug（例：`?pa_scent=lavender`，SEO-friendly、可分享），客面仍顯示中文 term 名。Theme唔保存、重算或另外擁有product set。
- **多選、URL同瀏覽器歷史係同一份狀態**：同一 facet 內多個值用 OR、不同 facet 之間用 AND。Canonical query 每個 attribute 用已排序、去重嘅 ASCII term slug，以逗號分隔（例：`?pa_scent=lavender,ylang-ylang&pa_size=100ml`）；query args 固定按 allowlist 次序輸出，並保留有效 `s`、`min_price`、`max_price`、`orderby` 及 page。每次客人完成篩選／搜尋／排序／翻頁用 `history.pushState()`；只喺首次載入將等價但非 canonical URL 用 `replaceState()`；`popstate` 必須由 URL 還原所有 controls 及同一結果，唔另加 history entry。Filter／search input 固定 debounce 250ms，較舊 request response 不可覆蓋較新結果；server query係結果、count、分頁及「載入更多」唯一真相，前端唔自行拼湊第二份 product set。
- **無 plugin、無年費**（如日後 SKU 過千先再評估 FacetWP 一類）。
- `HIBI LAB Shop.html` 示範咗目標 UI：checkbox facet + count + active chips + price 雙 handle slider + sort + 手機 filter drawer + 載入更多。

---

## 5. 會員帳戶 ＋ VIP 等級（簡化版・已對抗式 review：logic＋money-safety＋copy）
> 完全後台可設定、等級數任意（demo 一般／銀／金＝seed）。詳細裁決見 §21；build guard＋客面文案見 §22。核心：會員價 = 一個**自動 price filter**（非 coupon、never-store、落單一刻 server 用當刻等級重算）。

**A. 後台（`hibilab-commerce` 擁有並驗證設定 schema；committed ACF field groups只喺§21.6固定Woo submenu提供原生 Admin 編輯介面）**
- 自由**新增／刪除**等級；**唔提供拖拉排序**，每次儲存後按升級門檻由低至高自動排序，避免顯示次序同資格次序分裂。每級：中/英名 · 升級門檻（近 12 個月**商品淨消費** HKD，明確不計運費／稅／費用）· **一個全單會員折 %** · **主色一隻**（系統自動搭「深底→主色」漸變卡、符合品牌）· **生日月折扣 %**（只 %、非 $）· **無限「其他會籍禮遇」文字**（描述類 perk、人手兌現）· 啟用／停用。兩個折扣欄只接受 `0.00–100.00`（含端點）、最多兩位小數；空值、非數字、負數、超過100或更多小數一律拒絕儲存並保留原值，唔自動 clamp／四捨五入錯誤設定。
- **基本級（HK$0.00 / 0%）= 明確第一行、不可刪不可停用。** 全域只設 **總開關**（OFF = 全站無會員價、所有人原價）同 **手動 override**（2026-07-23 客戶裁決 B＋清除方式 A；2026-07-23 後台位置裁決 B）：WooCommerce 分隔線以下新增一個細型原生 submenu「VIP 會員」，供 `scentm_manager` 以姓名／電郵搜尋單一會員後操作；唔使用 WordPress Users／編輯會員頁，亦唔授予 `edit_users`／`list_users`。頁面只顯示會員身份摘要、系統計算等級及一個等級下拉選單；第一項固定為「使用系統計算等級」，其後先列出 enabled 等級。揀第一項＝清除手動指定兼停用／清空到期日欄；揀指定等級後可選填日期欄「有效至（包括當日）」，留空＝一直有效直至 Admin 改回第一項，填日期＝香港時間該日完結後自動恢復系統計算等級。唔新增 top-level menu、獨立 Dashboard、額外清除按鈕或到期 email。計算窗口固定為「計算當刻向前 12 個曆月」，唔另設可改窗口，避免後台設定同客面規則漂移。
- 每級有隱藏 stable id；驗證：一個 HK$0.00 base、門檻嚴格遞增、儲存後按門檻自動排序。demo 三級（HK$0.00／HK$2,000.00／HK$5,000.00、Silver 10%／Gold 15%）＝seed 可改值。

**B. 會員價（自動、非 coupon）**
- 登入會員按等級**自動**享全單 % 折後價（price filter）。全店貨幣固定 HKD；計價全程用 WooCommerce decimal／minor-unit 規則，同一金額只喺 canonical money function round 一次，客面固定顯示兩位小數（`HK$X,XXX.XX`），唔做整數蚊 rounding。
- 會員價、有效 sale、coupon 二選一、minor-unit rounding、平手規則及免運 eligible subtotal **只由 §6 定義**；本節及其他實作章節只引用，禁止複寫另一條公式。**★WC「特價」(sale price)欄＝唔用（2026-07-11客戶裁決）**：減價＝直接改regular price、限時promo＝coupon；admin欄旁加「請勿使用特價欄位」＋health check警告。§6 嘅 sale 分支只係萬一有人誤填時仍不收錯價嘅安全網，前台唔新增任何特價視覺態。
- **生日月自動加成**：生日月會員價選擇及計算完全依§6唯一公式，**無 code、無 wallet**；1 號寄祝賀 email（**只寄俾已 opt-in「接收優惠及新品消息」嘅會員 — 2026-07-11 客戶裁決，PDPO Part 6A；email 內附退訂連結**）；會員喺1號後先設定生日或先取得有效opt-in，會喺當月下一次dispatcher補寄一次；同一香港曆年以semantic key保證最多一封，唔補過去月份。一年一次。生日與否喺**落單一刻**用香港時區定死、寫入 order meta，之後唔再判。

**C. 等級升降（永不儲級、每次即場用商品淨消費算）**
- **唯一計算公式（2026-07-23 客戶裁決 A）**：每次以香港時區「計算當刻向前12個曆月」做rolling window，窗口精確為**`[now − 12 calendar months, now]`兩端包含**（剛好cutoff計，早一瞬間唔計）。Calendar subtraction 必須用 `Asia/Hong_Kong`、保留本地時分秒並採 **no-overflow／month-end clamp**：例如2月29日向前12個曆月＝上一年2月最後一日同一時間；計好local cutoff先轉UTC查詢，唔准用365日、固定秒數或UTC直接減。只納入以該會員身分、喺帳戶註冊後新建，而且`PaymentEvidenceService`已確認並回傳嘅`first_confirmed_at`落喺窗口內嘅成功訂單；Guest身分建立嘅舊單之後即使綁定帳戶或先註冊再付款亦不可補計。每張單貢獻＝`max(0, 折扣後商品行實付總額 − canonical商品資金流出額)`；資金流出只由同一charge嘅唯一、provider-verified成功退款及最終`lost/funds_reversed` dispute／chargeback組成，按provider object identity去重後由單一累計projector分配，唔准將refund同chargeback兩份aggregate各自相減造成雙扣。商品行實付額已包括會員價／coupon結果；**運費、稅項及任何fee一律不計**，明確記錄喺shipping／tax／fee line嘅退款亦不影響VIP。成功退款依Woo line分類；未歸類amount-only refund及無line分類嘅final reversal預設先扣尚餘商品貢獻，逐單cap至原商品實付額及provider可證明嘅charge-level淨流出，餘額先屬非商品。Provider outcome未final、identity／net amount不明或同退款重疊無法唯一對數時暫不改VIP並開`needs_attention`，唔估。UAT固定覆蓋2月29日、月尾、cutoff同一秒、早一瞬間，以及部分退款後再chargeback／反向次序而商品貢獻只扣一次。
- **付款真相只經一個 read-only `PaymentEvidenceService`**：此 service 只正規化 Woo order＋所 pin 官方 gateway 已保存嘅受支援證據，**唔另建付款 ledger、唔直接 call Stripe、唔因讀取而寫金錢狀態**；固定輸出 `confirmed_paid|confirmed_zero|pending|unpaid|unknown`、首次確認時間及可稽核 reference。Compatibility manifest 必須逐個 pin 版本寫明官方 gateway 嘅公開成功 hook／order datum，並以 contract test 證實；目前所 pin Woo Stripe Gateway 嘅成功 lifecycle 會呼叫 Woo `payment_complete()`，其持久化結果 `date_paid`＋非空 transaction ID只係**候選成功證據**，唔可以喺付款身份及訂單快照未安全凍結前直接向外回傳confirmed。Order status、Woo paid-status／needs-payment helper、browser return、order note、未列入manifest嘅private meta或Admin手動改 status任何一項單獨都唔係付款證據。客面確認、email、VIP、預訂 demand、ready-to-ship、ShipAny及Account counts全部呼叫同一 service，唔准各自再寫第二套判斷。
- **付款狀態／客面 action matrix（同一 service projection，禁止由 template 自判）**：

  | `PaymentEvidenceService` | 必要 reason／前置 | 客面狀態及 action | 系統副作用 |
  |---|---|---|---|
  | `confirmed_paid` | 完整正銀碼 confirmation projection | 顯示已付款／成功頁；無 Pay CTA | 只此狀態可寄付款確認信、計VIP、建立預訂 demand及進入履約 |
  | `confirmed_zero` | 完整 zero-total confirmation projection | 顯示免費訂單已確認；無 Pay CTA | 同上，但不造 Stripe reference，VIP貢獻固定HK$0.00 |
  | `pending` | provider 已有可驗證 in-flight／async payment（`provider_in_flight`） | 使用 `_p29` 已批准 pending 文案：eyebrow「**付款處理中 · Payment processing**」、標題「**付款正在處理**」、主句「**付款仍在處理中，請勿重新付款。訂單狀態會在付款結果確認後自動更新。**」；無 Pay CTA | 不寄確認信、不計VIP、不履約；由官方gateway結果／reconciler收斂 |
  | `unknown` | 回傳中斷、付款證據暫時矛盾或無法可靠判定（包括 `confirmation_ambiguous`） | 精確顯示「**付款狀態確認中，請勿重新付款。我哋確認後會更新訂單狀態。**」；無 Pay CTA | 不寄確認信、不計VIP、不履約／ShipAny；開reason-scoped incident並只做read-only reconcile，絕不重收款 |
  | `unpaid` | canonical evidence證實冇成功亦冇in-flight付款 | 顯示「**此訂單尚未付款。**」；只有Woo訂單仍payable、hold／claim仍有效及全部server gates通過先顯示原生Woo Pay CTA | Pay CTA只resume同一張order；已過期／不可付款就無Pay CTA，沿用既有重新結帳／聯絡支援路徑 |

  Browser return、thank-you URL、order status或`needs_payment()`只可選擇頁面容器，**永遠唔可越過上表改付款狀態或顯示Pay action**。任何狀態缺reason、reason與證據不吻合或由第三方改寫，都降級到上表 ambiguity 行而唔估。Confirmation email、VIP、Account counts、預訂 demand、ready-to-ship及ShipAny全部只接受兩個`confirmed_*`狀態；confirmed async payment仍須先完成confirmation projection，唔因gateway回傳「processing」而提早履約。
- **單一確認投影器及crash recovery**：`PaymentConfirmationProjector`係唯一確認writer。Checkout喺call gateway前先以Woo CRUD封存versioned `_hibilab_checkout_seal_v1`，只包括order／item identity、quantity、HKD totals、pricing／preorder／consent snapshot及canonical hash；save＋read-back成功先可交gateway。確認快照只保存喺`{$wpdb->prefix}hibilab_payment_confirmation`：每張Woo order固定一行、`order_id BIGINT UNSIGNED PRIMARY KEY`，另有`schema_version`、`kind=confirmed_paid|confirmed_zero`、`first_confirmed_at_utc`、`currency`、`order_total_minor`、nullable Woo gateway／Stripe account／livemode／PaymentIntent／Charge IDs、`checkout_seal_hash`、`confirmation_hash`及`created_at_utc`；paid row必須有完整provider tuple，zero row必須total=0兼provider欄全NULL。呢張表係insert-once不可變derived projection，冇pending/refund balance、唔係第二payment ledger，正常流程永不update/delete，亦完全唔另存canonical Woo meta mirror。安全流程固定：①order guard以deterministic `payment_confirmation:{order_id}:v1` active identity＋generation claim後commit；②所有DB lock外經Woo CRUD／data store同所pin gateway公開證據建立normalized candidate，零provider mutation；③重取同一guard，active identity／generation仍吻合而又未有final row先喺短transaction INSERT，physical PK衝突時exact hash相同＝idempotent no-op、任何差異＝`unknown`＋exact incident兼永不覆寫；④commit後清該active claim。Crash喺INSERT前由同一identity續行，INSERT後重試只exact-compare現有row。`PaymentEvidenceService`以此row作唯一immutable confirmation projection，仍逐項核對checkout seal、hash及受支援證據；有候選成功但未有row只回`pending`＋`confirmation_projection_due`，任何不一致回`unknown`。HK$0.00沿用同一schema及projector，kind=`confirmed_zero`、保存標準zero-total completion source而不造provider ID；`zero_payment_confirmed:{order_id}`仍係semantic key。Projector永遠唔發起金錢mutation。
- **歷史付款身份凍結**：正銀碼訂單首次成為 `confirmed_paid` 時，必須已由上述confirmation projection一次過只讀保存 Stripe `account_id`、`livemode`、Woo gateway ID、`HKD` currency、首次確認時間及原始 transaction chain（PaymentIntent／Charge reference；按該 gateway 公開模型）；之後即使後台換 Stripe 帳戶亦唔可改寫。每筆退款、同步及外部復原都逐項對返該張訂單嘅歷史 tuple，唔准用「目前設定緊邊個帳戶」代替歷史交易身份；缺一項、跨模式、跨帳戶或 chain 不吻合一律 `unknown`／`needs_attention`，停止退款投影及出貨，絕不猜配。
- **商品實付基數只讀 Woo 訂單快照**：以 `PaymentEvidenceService` 首次確認成功時已固定嘅 Woo order item `line_total`（會員價／勝出 coupon 後、tax excluded、HKD minor units）加總；成功付款後永不重讀目前產品價、live cart 或 Admin 後改資料。正銀碼訂單用 service 回傳嘅首次確認時間／reference；HK$0.00訂單貢獻固定0且不要求不存在嘅transaction reference。應有嘅關鍵資料缺失／不一致就標`needs_attention`並暫不計VIP，唔准估算或另造付款快照。
- 只認 provider／Woo 已確認 `succeeded` 嘅退款；pending／failed／canceled／unknown 暫不改商品淨消費。退款發生後重算原訂單貢獻；原訂單付款時間已跌出 12 個月窗口就本來已貢獻 HK$0.00，該筆退款不可再造成負數。HK$0.00 訂單貢獻 HK$0.00；取消／付款失敗訂單唔計。
- 成功付款令商品淨消費達門檻就**即時升級**；成功退款或舊訂單跌出 rolling window 令商品淨消費跌穿門檻就**即時降級**。每日用 Action Scheduler 再全量核對一次，修正跨過 12 個月邊界或事件遺漏造成嘅 projection drift；**冇會員週年結算、冇 cycle floor、冇保級期**。
- Guest：**唔累積**；就算之後同 email 開帳戶，舊 guest 單**唔計**入等級（只計註冊後嘅單；歷史可顯示但唔計級）。

**D. 會員搶先（限會員先買嘅專屬商品）**
- 閘喺**產品**（勾「限會員」+ 揀最低等級）；夠級會員正常買、未夠級／訪客**買唔到**，**server 端攔**（加入購物車＋結帳都攔）。
- 顯示：夠級 → 會員價＋tag＋正常加購；**未夠級會員 → 鎖住提示「需 {級} 會員」＋升級指示（見正常原價、無會員價 tag、無劃線，只係加購位變鎖）**；訪客 → 提示登入（同樣見正常原價）。
- 引用嘅最低等級被刪／停用 → **停售（fail closed，絕不 fail-open）**；gate 用門檻值比較、非 row 次序。同「預訂」分清（搶先＝有貨限會員；預訂＝缺貨任何人，§19）。

**E. 前台顯示**
- Account 概覽頂：**一張自己等級卡**（單張全闊漸變橫額＝名＋近 12 月商品淨消費＋進度／已達最高，`HIBI LAB Tier Cards.html` 樣板、色按等級自動；**唔做全部等級 ladder**）。
- Shop 卡／PDP：**會員價＋劃線原價＋「{級}會員價」細 tag**；未登入 PDP 只出一行產品專屬提示：「**登入享會員價，此產品低至 HK$X**」（§23.2#14：PLP／Cart／Checkout 一律唔出登入推銷；HK$X 由該產品最低可購 variation 會員價 derive）。
- Cart／Checkout：**一行「會員優惠」**（label 淨四字 — 無級名、無「自動」章，2026-07-10 客戶裁定 §22.5），**不可移除**（非 ✕）。

**保留（WC 原生）：** 註冊／登入／忘記密碼（§8；帳戶設定只喺生日尚未設定時收**生日 月+日**，成功儲存一次後唯讀）· 訂單／地址／設定（§9）· guest checkout（§17）· 優惠碼（§6）。

**唔做：** 積分 · **優惠券錢包**（生日改自動加成，冇券要儲）· generic 分類／產品／滿額折扣引擎（呢類 promo 用原生 coupon，§6）· 軟著陸。

---

## 6. 優惠券 Coupon（native WC · 一次一張 · vs 會員價二選一取最抵）
- 全部 **WC native**：admin 喺 WooCommerce 原生後台建碼（%／$、限產品／分類／滿額、用量限制、到期）。**無自訂引擎、無 auto-apply**；客人 Cart／Checkout 自己輸入。
- **一次一張**：所有碼 `individual_use`。**冇免運碼、冇並存例外**（免運＝全店門檻 `hibilab_free_ship_threshold`，唔係coupon類型、亦唔接受另一個免運碼；實際資格只由§7 canonical threshold predicate決定，§7）。**刪除 demo 遺留嘅 `FREESHIP`／`ship` 類型同「免運碼可並存」分支**（`HIBI LAB Cart.html` 嘅 `#coupon-input`／`DEMO_CODES`；`HIBI LAB Checkout.html` 嘅 `#co-coupon-input`／`DEMO_CODES`）。
- **vs會員價＝二選一取最抵（唯一 normative 公式；全單口徑）**：canonical quote service由同一cart建立兩條完整、互斥嘅payable merchandise quote。有效會員／生日折扣率 `d = max(tier_discount, birthday_discount)`，而且已經通過§5 `0.00–100.00`驗證；member path逐行用 `min(Woo當刻有效sale/current catalog price, quantize(regular_price × (1 − d / 100)))`。coupon path用Woo current catalog price（含有效sale）再經Woo官方discount APIs套唯一coupon。兩邊以相同eligible商品、qty及同一canonical minor-unit rounding計最後**商品應付subtotal**；**數值較低者勝**，其中coupon path只有嚴格低過member path先勝出，平手回member path。每次cart recalc及Checkout submit都server-side重跑，生日只讀已pin snapshot；唔用「regular subtotal減折扣」兩個不同基準比較，避免sale存在時揀錯。100.00%只令eligible商品行變HK$0.00；運費／fee仍按各自規則存在。Woo最終order total精確HK$0.00就行原生zero-total completion並由§5 confirmation projector產生`confirmed_zero`，唔建立Stripe object；如運費或其他合法金額令總計大過零，官方gateway只收餘額。
- **客面文案（禁「保留／暫停／留」；全套 §22）**：碼唔夠會員價抵 → 唔套用,出「**會員價更抵，此優惠碼無需使用。**」（§22.3 短版，2026-07-10 裁定）；碼贏 → 折扣行 label「**優惠碼 {code}**」＋訊息「**已套用優惠碼。**」（§22.4 短版，2026-07-10 裁定）
- **管理 affordance**：Cart 客人自套碼嗰行「移除 ✕」；**會員自動價嗰行唔俾移除**（§22.5：label 淨「會員優惠」，無「自動」章）。Checkout 收埋式欄有碼時「已套用 {CODE} · 更改」。套第二張碼 → WC 原生拒絕「一次只可使用一張優惠碼,請先移除現有優惠碼」（經 §22 notices skin）。無效碼**唔影響**已套用嘅碼。
- **免運門檻口徑（2026-07-27 客戶裁決 A）**：canonical setting 只得 `hibilab_free_ship_threshold`，WC／ShipAny 內建門檻保持停用。Eligible subtotal 固定讀 §6 **最終勝出 quote path** 入面可配送商品嘅 pre-coupon item prices：member path 勝出就用有效會員／生日價（並保留較低有效 sale 安全網）；coupon path 勝出就用 Woo catalog／有效 sale 價、但喺扣 coupon amount **之前**計。兩條 path 都只計勝出 path 自己，絕不混價或先用 coupon 後 subtotal；一經達標，之後套用 coupon 只減應付金額，唔會取消已取得嘅免運。計算只用 server-side minor units，排除 shipping、tax、fee 及非商品金額。Cart、Checkout、側滑車、Shipping 政策及 structured data 等**當前購物面**讀同一 canonical setting／predicate；Order Received、Account 訂單詳情及 email 等**歷史訂單面**只讀該 Woo order 已保存嘅 shipping line，並以 immutable order meta 保存落單時winner path、門檻、eligible subtotal及達標結果作稽核，絕不可因 Admin 日後改門檻而改寫舊單。

---

## 7. 送貨 & 訂單狀態（ShipAny）
- **送貨方式 = 送上門 + 自取點**（智能櫃／便利店／順豐站 — ShipAny 網絡）。Checkout「送貨方式」UI：radio 卡「送上門」／「自取點」（= WC 原生 shipping method 列表嘅品牌 skin）；揀自取點 → 出選點器。**production 選點 UI = ShipAny plugin 自家「pick-up dialog」彈窗**（有地區 filter＋Change Address 掣，手機版另有優化）— demo 嘅 inline mock 下拉+已選點卡係視覺 placeholder，實作以 plugin 彈窗為準、theme 只 skin 外圍。自取點清單「寄生」喺 WC 原生 Local pickup method 上（要喺 Shipping zones 加 Local pickup 先開到 Locker/Store List）。
- **運費 = 商家預設 per-method 費率（唔係 checkout 即時報價）**：官方文檔冇任何「checkout 按地址 live quote」；checkout 費率由商家喺 WC shipping zones／ShipAny Portal「Shipping Profile」（plugin v1.1.70+「Show Shipping Options at Checkout」）預先設定，金額對齊 ShipAny 價目（例：SF E-comm Box F1–F5 = HKD 28–98）。「即時報價比較」係商家出單嗰刻用嘅功能。**Demo 稿內運費銀碼（HK$45.00／HK$32.00 等）都係示範值。** Shipping Profile 精確行為官方冇公開文檔 — staging 實測定案。
- **免運門檻 = 一個後台輸入、一個 business truth（2026-07-23 客戶裁決）**：`hibilab-commerce` 註冊、驗證及讀寫 canonical setting `hibilab_free_ship_threshold`；§21.6固定嘅 **WooCommerce → HIBI LAB 設定**只係 Admin 唯一輸入介面（demo HK$500.00 只係示範），Theme 只讀 plugin 提供嘅 projection。設空／0＝停用，當前購物面免運文案+nudge 自動隱藏；達門檻 → 所有當前可用送貨方式免運（差額商家吸收）。`hibilab-commerce` 必須用 WooCommerce 受支援嘅 shipping-rate extension point，按 §6/§20.1 同一 canonical eligible subtotal 對現有送上門／自取點 rate 套用免運；Cart／Checkout／側滑車／Shipping 政策及 structured data 讀同一 setting，歷史 Order Received／Account／email 則讀 order shipping line＋落單 snapshot。WC 原生 free-shipping 門檻及 ShipAny「Locker/Store List Minimum Checkout Amount for Free Shipping」不可再成為獨立輸入來源；整合時關閉／留空，health check 驗證冇第二個門檻生效。唔准寫 ShipAny private option、靠三處人手同步或只用 warning 接受 drift；如 plugin 版本冇受支援方式保持呢個 contract，staging fail closed，先修 adapter 再上線。Eligible subtotal嚴格跟§6唯一winner path同§7可配送商品scope，只由可配送商品 line amount 組成、全程 minor units；達標後 coupon 不撤銷免運。shipping、tax、fee 及非商品金額永遠排除，禁止用 display string 或整張 order total 判門檻。
- **4 區地址欄**（香港島／九龍／新界／離島，`woocommerce_states`）；客人揀完自取點 plugin 會 write-back 地址欄（block checkout 曾有 write-back bug、v1.1.101 先修 — 我哋用 classic shortcode checkout，一向係穩陣嗰邊）。
- **訂單狀態／時間線 = 4 步（2026-07-27 客戶裁決 B）**：已確認 → 處理中 → **已寄出** → 已完成。「已寄出」步顯示**追蹤號＋追蹤 link**。`PaymentEvidenceService=confirmed_paid|confirmed_zero`只進「已確認」；全單所有有效實物數量已分配並 ready 先進「處理中」；真正交予物流並有真實 handoff 記錄先進「已寄出」及寄信；只有 `ShipmentEvidenceService` 按以下 invariant 驗證完成，先進「已完成」。Woo `processing`只代表已付款等候履約，唔直接等同客面「處理中」。若 staging 無法證實可靠 final-delivered signal，訂單保持「已寄出」並將整合列為 launch blocker，唔用 timer、估算、手砌 fallback或私有API補位。
- **送達覆蓋 invariant（Phase 1：一單一件 current shipment）**：每張有餘下實物履約責任嘅 HIBI order，Woo order meta 只保存一份 versioned、可重建嘅 `_hibilab_shipment_projection`，至少有 generation、唯一 current provider shipment ID、全單餘下實物 obligation hash（或逐 order-item quantity map＋hash）、state、provider updated/version/hash，以及 handoff／delivered evidence reference。呢份 projection 只係本地 cache，**completion 前必須用所 pin 整合嘅受支援 public API／public plugin data 按 Woo order reference 列出 provider 端全部 current shipments**，再證明 **exactly one current shipment**、佢就係 projection 記錄嗰件、完整覆蓋全單所有未取消實物 quantity，而且有 allowlist 最終送達證據；零件、超過一件、只覆蓋部分、quantity/hash 不符、void 後未有 replacement 或任何資料矛盾，一律未完成兼 reason-scoped `needs_attention`。Provider 已驗證 void 只可經受控 replacement action 推進新 generation；新 shipment 成為唯一 current shipment，並重新覆蓋全單餘下 obligation，舊 generation 只保留 audit reference。Phase 1 **唔支援 split／multi-shipment**；native／bulk／Portal入口必須停用，或由上述 enumeration 證實仍守 exactly-one。若 pin 整合無法可靠按 order 枚舉全部 current shipments，2B final completion 屬 launch blocker；唔靠單一 local ID 假設冇第二件。等 staging 證實受支援 item mapping 並經客戶另行批准先可擴充，唔預先自建 replacement graph 或 shipment platform。呢個 predicate 明確避免 `all(empty)=true`。
- **ShipAny observation inbox／核對**：`hibilab_shipment_event`係 webhook 同 public API poll 共用嘅最小化 observation inbox。只有 **HIBI 自己擁有並驗證嘅 endpoint**先可承諾驗簽→durable insert→2xx；如 endpoint 由 ShipAny 官方 plugin 擁有，HIBI只可消費其文件化 public hook，唔聲稱控制對方 ACK，hook observation best-effort 落盤後仍由受支援 public API reconciler 收斂。Webhook／hook 事件早過 shipment↔order relation時保持 unmatched 並重試配對；poll observation以`shipment_id + provider_updated_at/version`（無version先用normalized payload hash）去重。Projection mutation要喺order-scoped lock下做 compare-and-set：較舊provider version／updated-at只記audit並no-op；同一version但hash不同即`needs_attention`；新version先可推進state。Provider無可靠monotonic version時，以public API當刻完整truth重新建立projection，唔由事件先後猜。低頻reconciler掃 current、已寄出但未 final、unmatched／retry-due observation及provider-side enumeration；未知、exception、RTS、無法解析或持續矛盾只可開可見`needs_attention`，之後有一致證據先自動關閉同一reason，絕不猜完成。如 pin 整合既冇可靠 public hook、亦冇可核對／枚舉 public API，2B「已完成」保持 launch blocker。完成後若再收到相反 terminal evidence，唔自動倒退客人時間線或撤回已寄 email，只開高優先`needs_attention`俾營運核實。
- **完成 transition同電郵只有一個owner**：只有 `ShipmentEvidenceService`由false→true嘅semantic transition先可經Woo CRUD一次轉 core order做`completed`，同時驅動客面第4步及以`order_completed:{order_id}`為UNIQUE semantic key嘅唯一品牌完成email。HIBI order嘅原生Woo completed email固定由allowlist replacement關閉；Admin／REST／第三方單純手動改status做`completed`唔會令客面完成、唔會寄完成信。受支援入口先server-side拒絕呢種非法transition；若外部路徑仍造成drift，客面 projection／完成 email 仍只認 shipment evidence，並即時開可見`needs_attention`，停止新的出貨／客戶通知等 outbound mutation，**但保留 read-only reconciliation、provider核對及告警**。Woo order內只顯示一個原生notice＋「修正訂單狀態」action，沿用既有`hibilab_manage_shipments`窄capability、nonce及order-scoped lock；冇任意dismiss，亦唔畀Admin自由揀目標status。每次先重跑read-only reconciliation：若已驗證final-delivered，就只關閉同一drift reason，再由尚未發生嘅false→true completion transition推進core status及以`order_completed:{order_id}`寄一次完成信；若未final但付款＋履約證據一致，只預覽並恢復由同一projection推導出嚟嘅唯一正確core status；證據仍矛盾就拒絕操作、保留alert。Audit固定保存actor、from／to、reason、evidence reference及時間；其他reason-scoped blocker唔會被一併清走。系統唔會因timer或alert本身自動倒改 Woo core status，呢個 recovery亦唔改錢、庫存或已驗證shipment真相。
- **追蹤資料**：ShipAny 實際建立運單後，以所裝版本公開提供嘅 order meta／API 讀出 shipment UID、courier、courier tracking number 及 tracking URL，經一個 `hibilab-commerce` adapter 正規化成 `{tracking_no}`／`{tracking_url}`／`{carrier}`。Meta key、shortcode 或內部 class **唔寫死入業務層**；implementation PR 必須以 staging 所裝版本記錄 adapter contract test。追蹤資料未齊只可顯示「處理中」兼通知營運，唔拼接估計 URL。
- **Email**：客人寄出通知只由 HIBI 品牌範本第 ② 款擁有。ShipAny／Woo 如會因 customer note 或內建 tracking notification 另寄一封，必須喺 staging 以**供應商正式設定／公開 hook**關閉嗰個 customer-facing sender；未證實可關閉就屬 launch blocker，唔用未記載 option 名或字串攔截。Shipping 政策頁「出貨後將以電郵提供追蹤資訊」只喺實際「已交物流」後寄出並有真實 tracking data 時成立。
- **WP 實作**：官方 plugin「**ShipAny WooCommerce: Ship, Label, Tracking**」（wordpress.org slug `shipany`）；compatibility baseline 以 implementation manifest 鎖定並完成 staging UAT 嘅版本為準（本文件 2026-07-26 參考 WordPress.org stable 為 **1.1.105**；唔把今日版本號當永久契約）。設定流程：安裝 → WooCommerce Settings → Shipping 貼 ShipAny Portal Access Token → 首次出貨前 portal 增值。**Processing 自動出單關閉**。正常出單只經已受保護嘅 Woo order metabox／order-list action；Portal 只供查閱及經 audit 嘅異常復原。ShipAny 未提供一個已證實覆蓋所有 single／bulk／AJAX／Portal入口嘅公開 pre-create gate，所以 production integration 要先做 staging compatibility spike：若無受支援 interception point，就唔宣稱已封鎖所有原生入口，亦唔靠隱藏按鈕、private method 或 monkey patch；由客戶同供應商確認支援方案前保持 launch blocker。收費／add-on條款會變，簽約前以 ShipAny Portal／合約為準，唔把示範價錢寫成程式規則。
- **已知風險／Phase 1貨品邊界**：① 自取點寄生喺 Local pickup — 想「工作室自取」＋「智能櫃」並存曾有全部 method 被改名做「ShipAny SF Locker」嘅衝突紀錄（v1.0.55 有 fix）— **staging 必測**；② **Phase 1明確不售、不接受結帳及不交ShipAny處理任何香水、酒精、易燃、受限制液體或其他危險品**；產品發布checklist以明確商品分類／運輸資料阻止呢類貨上架，唔保留「先賣再靠提示」或未定hazmat branch。如日後要售，必須另行取得承運商書面contract、法規核對、包裝／標籤流程、可支援shipping method及客戶新批准後先擴scope；③ plugin 用戶基數細（200+ installs）但官方活躍維護 — 更新前 staging 先行。
- **Launch gate**（§22）：ShipAny 帳戶開通 + API Token 入好 + portal 增值 + Courier Service Point Listing 開通 + Processing 自動出單已關 + staging 測試「未 ready 拒絕出單→ready 出單→印單→追蹤 write-back→label_created 不寄信→實際交件先 shipped＋寄信→選點」全鏈一次；另實證 provider shipment void/cancel 同 timeout 後查詢能力，未證實前不可宣稱取消／安全重試已完成。

---

## 8. 登入 / 註冊 / 忘記密碼（原生 — 只 skin）
- 登入 / 註冊 / **忘記密碼（lost-password → email → reset）** / 重設密碼 / 登出 = **WooCommerce + WP 原生**（endpoint + email 全包）。**唔使寫 auth 邏輯。**
- 唯一工作 = **template override + CSS 套返品牌樣式**：`myaccount/form-login.php`、`form-lost-password.php`、`form-reset-password.php`。
- 四個 view — 登入／註冊／忘記密碼／重設密碼 — 已全數喺 `HIBI LAB Account Login.html`；demo 切換流程見 §18。

---

## 9. 會員中心面板（`HIBI LAB Account.html`，對應 my-account endpoints）
- **概覽**：歡迎句 + 統計（累積訂單／累積消費）+ **自己等級卡**（概覽頂一張，loop VIP repeater 出當前級＋近 12 月消費＋進度條／已達最高；主色讀該級設定；`HIBI LAB Tier Cards.html` 樣板）+ 最近訂單（cap 最近 3 張）。**唔做全部等級 ladder、唔做獨立「會員等級」nav、唔做優惠券錢包**（生日改自動加成、冇券要儲；demo Account nav ＝ 會員概覽／訂單記錄／補購提醒／地址／帳戶設定／登出（6 項））。
- **補購提醒**：獨立面板（新增 nav 項、列表第 3 位／共 6 項，2026-07-10 鎖定）— 列表（產品／週期／下次提醒日）＋改排程（模式＋數值：每隔 N 日 1–90／N 週 1–12／N 月 1–6／每月 N 號 1–31，短月自動用月尾）＋**暫停／恢復**（「已暫停」態：設定保留，恢復＝由恢復日重新開錶）＋取消提醒；**循環直至客暫停／取消（無自動熄火）**；**只限登入會員**（guest 唔見 opt-in）；逐件獨立 schedule；**per-product 唯一（再 opt-in＝upsert 更新現有提醒，唔開第二個）**；**購買即重錨（下次＝最近購買日＋排程間隔；每月固定日子模式唔重錨）**；列表分頁只讀§25.2 `HibilabOperationalPolicy`（Phase 1=`20/頁`），超過policy page size先顯示Woo原生分頁，排序固定`next_due_at_utc,id`，翻頁後保持同一Account endpoint；空狀態句「你未有補購提醒。喺產品頁勾選「到期提醒我補購」即可設定。」＋前往商店 link；email ⑥「管理提醒」連結指嚟呢度。樣本：Account demo 面板＋PDP 購買區 panel（Product demo＋_p27）。**Data model：一張 canonical logical table `hibilab_reminder` table 保存 schedule truth，實體表名由 `$wpdb->prefix` 組合；DB `UNIQUE(user_id, product_id)`；Action Scheduler只排帶 reminder ID＋generation嘅 one-off wake-up，唔做第二份真相，亦唔鏡射 user meta。**產品建議間隔＝ACF `hibilab_repurchase_interval`（§3）做預帶；完整排程／重送契約見 §25。
- **訂單記錄**：可展開詳情（**4 步狀態時間線** 已確認→處理中→已寄出→已完成，「已寄出」步顯示追蹤號+追蹤 link + 商品明細 + 小計/運費/總計 + 送貨地址 + 再次購買 + 「查詢此訂單」細 link）。**>10 張＝WC 原生分頁（`/orders/page/N/`）。**
  - **非快樂狀態視覺**：只有**全額退款成功而且已無任何餘下履約責任**、已取消，或 `PaymentEvidenceService=unpaid` 並有canonical failure reason先唔出 4 步時間線，改單行狀態 label（已退款／已取消 = muted brown、付款失敗 = `#b4452a`）；raw Woo status本身不可揀呢個畫面。全額退款另顯示「已退款 −HK$X」。部分退款仍按未取消商品嘅正常履約狀態顯示時間線，另列「已退款 −HK$X」累計成功退款額；每筆 Woo refund ID 仍可獨立audit。Provider pending／unknown 絕不可顯示為已退款。Label token 對照見 §22。
  - **再次購買 = WC 原生 order-again endpoint**：全部可購 line 以**現價**重新入車，唔可購（售罄/下架）項出 per-item notice；成張單冇 line 可購 → 藏個掣。demo 個單一 PDP link 只係 placeholder。
- **地址**：帳單 + 送貨兩組地址卡 + inline 編輯表單（對應 WC billing/shipping；**無「新增地址」UI** — §20.6 兩組固定）。
- **帳戶設定**：個人資料（名／電郵／電話／**生日**）＋變更密碼（對應 WC account details）。生日月日只可由會員喺尚未設定時成功寫入一次；一經設定，Account 顯示唯讀。其後只可由 `scentm_manager` 喺 WooCommerce → VIP 會員以專用窄 capability 更正，唔授予 Users 管理權；每次更正必須記錄 actor、target member、舊值、新值、原因及時間。所有入口（Account、REST、直接 POST）共用同一 server-side invariant 及合法月日驗證，並以 compare-and-set 阻止並發第二次寫入。
- ⚠️ 稿內數字全部係 placeholder → 由 WC / user data 取代。

---

## 10. 全站設計鐵則（轉 theme 時要守）
- **HIBI LAB 內容區文字最小 16px**（label / eyebrow / 註腳 / 按鈕 一律 ≥16px）。**例外**：共通 Scent.M chrome（nav/footer/menu）維持原樣；icon glyph 不受管；及 `HIBI LAB Landing.html` 現有 sub-16px 元素獲豁免 — 維持原樣，其餘 9 頁照守。
- 同排 card **等高**（flex-col + 圖固定 4:5 aspect + 價/CTA `mt-auto` 釘底）。
- **產品圖 borderless**：`object-cover` 填滿 4:5 框、統一暖色 fallback `#efe6dd`（因商家自上載相、背景唔一致）。**唔好逐張 sample 相片色做 card bg**。
- 無假數據（唔放假評價數 / 假頭像）。
- 全站一個 `max-w` 容器、`px` 同層對齊。
- 動畫：scroll-reveal 為主；hover 用 image-zoom（`group-hover:scale-105` / `duration-500`）；禁 cheap lift/shadow。

---

## 11. 商家上載相片規範
- 白底 / 透明背景最佳；但 borderless 卡設計對任何背景都 graceful。

---

## 12. 共用店面組件 `store-chrome.js`（搜尋 + 側滑購物車 — 單一真相源）
- **係咩**：全站共用嘅 search overlay（command-palette 式）+ slide cart（側滑抽屜）。**一份 source**，每個店頁 `<script src="store-chrome.js"></script>` 載入，自動注入 markup + wire 返 nav 嘅搜尋掣同購物車 icon。**唔好逐頁 copy**。
- **觸發**：nav `#nav-search-btn` → 搜尋；`a[aria-label="購物車"]` → 側滑車（intercept click，`href` 作 fallback）。
- **Scroll-lock**：抽屜 / overlay 開啟時鎖背景 —— 依賴每頁 chrome 將 Lenis 實例 expose 做 `window.hbLenis`（`const lenis = window.hbLenis = new Lenis(...)`）；`store-chrome.js` 用 `hbLenis.stop()/start()`（`overflow:hidden` 實際由 Lenis 嘅 `.lenis-stopped` CSS 提供）。**新頁必須照樣 expose，否則背景會 scroll-through。WP 側：theme 嘅 `assets/js/theme.js` 已經 expose 咗 `window.hbLenis`（theme.js:25；header.php no-js guard 亦已依賴佢）— 唔使再加。**
- **搜尋資料**：`store-chrome.js` 內含 `PRODUCTS_JSON`（demo static；Shop 有 `#plp-data` 就用返，其餘頁自動注入）。**Production：兩份都由 WooCommerce 產品資料取代 — overlay 行 §22 拍板嘅自訂 REST endpoint、直入 `?s=` 301 去 PLP（單一來源，見 §20.5/§22 搜尋契約）；static dataset 只係 demo。**
- **WP 對應**：`store-chrome.js` → theme JS（`wp_enqueue_script`）；抽屜/overlay markup → 一個 `get_template_part('template-parts/store-chrome')`，喺 footer include 一次。購物車內容接 WooCommerce cart fragments（`woocommerce_add_to_cart_fragments`）即時更新；**drawer 嘅 qty +/− 同 移除 = 自訂 wc-ajax endpoints（§22 — fragments 只係 re-render 機制，唔包 mutation）**；drawer 空狀態見 §22；結帳掣 → `wc_get_checkout_url()`。
- **一致性守則**：nav utility cluster（搜尋 + 會員 + 購物車 icon，全部 ≥ 手機可見）+ `store-chrome.js` include **每個店頁都要有**，順序一致。

---

## 13. 新增頁面 + 購買旅程接線（skin，交接用）
- **新頁（全部 = WooCommerce 原生流程嘅品牌 skin；邏輯用原生，呢啲係樣式參考）：**
  - `HIBI LAB Checkout.html` → `woocommerce/checkout/form-checkout.php` override（聯絡 / 送貨 / **送貨方式（ShipAny 送上門+自取點，§7）** / Stripe 付款 / 訂單摘要 + 優惠碼收埋式欄 + 預訂同意 checkbox（如車內有預訂項，§19/§20.1））。
  - `HIBI LAB Order Received.html` → `woocommerce/checkout/thankyou.php`（訂單完成頁）。**沿用 Scent.M `page-thanks.php` 編輯式視覺版式**（serif-italic eyebrow + fluid-display 標題 + 白底 `<dl>` 詳情卡 + underline 連結）；只reuse presentation pattern，身份／資料存取必須用Woo thank-you／view-order原生session、order key及authorization，**唔複製Workshop `?booking=ID&token=`流程或另鑄HIBI token**。
    - **商品／總計展示合約（已批准）**：成功付款頁所有商品行必須讀 Woo order item＋付款時保存嘅 HIBI order-item meta；付款失敗／重試頁只讀同一張未付款 Woo order，並按 Woo 真實狀態顯示，絕不將 pending／on-hold 當失敗。每行依次顯示商品名、0..N 個 public variation meta、獨立數量、由 HIBI base-line minor-unit snapshot 取得嘅折扣前行金額，以及由 Woo order item total 取得嘅實收行金額；唔可以假設會員價 mutation 後嘅 Woo `line_subtotal` 仍等於 regular-price subtotal。唔可以重新讀取現時產品名、售價、庫存或重新計折扣；產品日後改名／刪除亦要保留歷史訂單真相。優惠行、所有商品實收行及總計必須逐分對數返 Woo order total；總計區由同一張 Woo order 動態輸出實際存在嘅小計、唯一勝出優惠、運費、fee／tax／退款及總計，冇值嘅行唔 render。長商品名／meta 自然換行，金額 `flex-none`＋`white-space: nowrap`；任何商品數量都完整列出，頁面自然延長，**禁止截斷、收合、分頁及商品清單內層 scroll**。
  - `HIBI LAB Account Login.html` → `myaccount/form-login.php` + `form-lost-password.php`（登入 / 註冊 / 忘記密碼，tab 切換 = demo；實際 3 個原生 template）。
- **旅程接線（前端）：** `HIBI LAB Landing.html` 4 類別門 → `HIBI LAB Shop.html?cat=<slug>`；Shop 讀 `?cat=` 自動套 dept filter（沿用現有 dept pill；demo slug = skincare/pet/sleep/home，**production slug 跟 top-level `product_cat` terms 動態出，唔 hardcode — §3**）；搜尋結果喺 overlay 顯示首 50 件，超出時清楚提示並連去 PLP 瀏覽完整結果（唔另開第二套搜尋頁）；PDP「加入購物車」→ 彈 slide cart 確認；Cart / slide cart「去結帳」→ Checkout → 確認及付款 → Order Received。
- **WP 對應：** `?cat=` → `product_cat` archive 或預套 filter；結帳 / 訂單完成 / 登入全部係原生 endpoint，只做 template override + CSS skin，唔改邏輯 / payment。

---

## 14. Demo 硬資料 → WP 綁定對照（交接檢查表）
> 所有靜態頁上嘅數字 / 文字 / 相片都係 **demo placeholder**（靜態設計交付物本質），全部應由後端資料取代：
- 產品(名/價/相/容量/成分/功效) + 搜尋資料(`store-chrome.js` `PRODUCTS_JSON`) → **WooCommerce products / 產品查詢**。
- 購物車行項目 + nav 購物車數字「2」+ 小計/折扣/總計 → **WC session cart / cart totals**。
- 會員(Amanda、電郵、訂單 #HL-…、地址、**等級／近 12 月消費**) → **登入用戶 / WC 訂單 / VIP 等級計算（§5）**。
- **Demo 每頁嘅 `<head>`（title/description/canonical/JSON-LD）全部係 throwaway** — production由theme嘅`inc/seo.php`統一生成meta／canonical／OG、非Woo頁schema及journey頁noindex；**Woo core structured-data pipeline係Product／Offer／Breadcrumb schema唯一owner**，theme只可經Woo官方structured-data filters補充，`inc/seo.php`唔另出第二份Product JSON-LD，唔好照搬demo。
- 免運門檻、VIP 門檻／折扣 → **`hibilab-commerce` 擁有並驗證設定 schema，ACF options／repeater 只提供 Admin 編輯介面**；Checkout 摘要、Order Received 訂單編號／日期／金額、運費 → **WC order data + WC shipping zones 商家預設 per-method 費率（§7）**。Theme 只讀 application service 提供嘅 projection，唔直接將 ACF row 當交易真相。
- Menu(About/ODM/Workshops/Journal/Testimonials/Contact)、Privacy/Terms 連結 → **現有 Scent.M 站頁面**（production 解析，非 HIBI 範圍）。
- Scent.M nav logo → `home_url('/')`（靜態暫指 `index.html`）。
- **WP-readiness**：共用 chrome 全站一致、`store-chrome.js`=partial、`?cat=` 要接；轉 theme 時 HTML→PHP template + Tailwind 由 `build-css.js` 編譯（唔用 CDN）。

---

## 15. PDP 內容區塊 = ACF Flexible Content（merchant 自由 create + 拖拉排序，同 Journal/Workshop 一致）
PDP 下半部**一定只由一個 ACF Flexible Content field `pdp_blocks` 提供**：商家喺 wp-admin **自由新增／刪除／拖拉排序** block，同 `scentm-wp` Journal／Workshop flexible-content用同一個後台pattern；`single-product.php`按row順序render，現靜態稿每個section係1:1視覺樣板。每個layout共用可選`heading`（純文字，最多100 grapheme）；空row唔render。Field group key、field key、layout name及child key全部按§21.6 committed，production後不可重生。

| layout | 固定欄位schema及驗證 | 客面行為 |
|---|---|---|
| `rich_text` | `body` WYSIWYG required；保存／輸出限`wp_kses_post` | 標題＋正文 |
| `spec_list` | `spec_kind` required select=`ingredients|specifications|shelf_life|other`；`rows` min1，每row `label` required max80、`value` required textarea max500；`applies_to_all_formulas` boolean只可喺`ingredients`出現 | 標籤／值對照；variation成分fallback只按§3唯一標記 |
| `steps` | `rows` min1；每row `title` optional max80、`body` required textarea max500 | 依row順序自動編號 |
| `callout` | `body` WYSIWYG required，限`wp_kses_post` | 左terracotta邊提示框 |
| `stat_row` | `rows` min1；每row `value` required max40、`label` required max100 | 數字統計 |
| `faq` | `rows` min1；每row `question` required max160、`answer` required WYSIWYG並限`wp_kses_post` | FAQ accordion；schema按§21.18同一內容 |
| `accordion` | `rows` min1；每row `title` required max120、`body` required WYSIWYG並限`wp_kses_post` | 預設全部closed；唔設第二個default-open欄 |
| `image` | `image_id` required attachment ID、`caption` optional max200；Media Library alt required先可發布 | 單圖；輸出attachment srcset／sizes |
| `gallery` | `image_ids` attachment ID list min1、`caption` optional max200；每張Media Library alt required先可發布 | 多圖，次序跟欄位 |
| `video` | `youtube_url` required，唯一URL欄；驗證／consent gate跟§20.7 | 使用示範；無片長欄 |

每個layout樣式跟現稿token（serif標題／≥16px正文／`#ece0d5`分隔／terracotta accent）。**每件產品嘅`pdp_blocks`係唯一內容來源**：無任何theme option、全局default block、自動注入、merge或override分支；全線通用內容由Admin喺需要嘅產品明確複製一個block，避免「產品空白但客面突然出預設內容」同兩份真相。安全須知、純露科普及保存期限內容由客戶上架時提供；空就唔顯示，launch checklist照驗。上半部（相片庫／標題／價／香味配方variation／加入購物車＋**補購提醒opt-in panel（§21.12：會員＋適用產品＋可購買先render）**）係WooCommerce固定summary，唔入Flexible Content。

---

## 16. WhatsApp 浮動掣 ＋ 支援渠道
- **WhatsApp 浮動掣（context-aware · 兩個號碼，WP 後台可入）**：Scent.M 頁 = B2B「Bespoke Inquiry」；HIBI LAB 店頁 = B2C「Customer Care」（客服 label 用英文，同 Scent.M chrome 一致；預填訊息維持廣東話）。**兩個 WhatsApp 號碼由客喺 wp-admin 自己入**：Scent.M沿用既有`whatsapp_number`＋`whatsapp_text` owner；HIBI只喺§21.6 **WooCommerce → HIBI LAB 設定 → 一般**保存平行`hibilab_whatsapp_number`＋`hibilab_whatsapp_text`，**只喺有號碼先render、唔fallback去錯號碼**（同`footer.php` `if($wa)`一致）。前端按頁面context出對應號碼＋文案；綠色WA本體不變，只換label／預填／號碼。HIBI LAB demo沿用inert `href="#"`＋`data-wa-pending`（唔render假號碼）；上線由客喺wp-admin填客服號碼先出現（同Scent.M hide-if-empty行為一致）。
- **支援 email**：平行 option `hibilab_support_email` — Shipping 政策頁「退換流程／有疑問」以 mailto render，hide-if-empty；「查詢此訂單」link 嘅 fallback 渠道。**兩個支援渠道（WA 號碼＋support email）都係 §22 launch gate 硬項** — hide-if-empty 唔可以變成「店開咗但零支援渠道」。

---

## 17. 登入狀態 → Cart / Checkout 顯示
- **Guest checkout 允許**，並啟用 Woo 原生 optional account creation；WooCommerce → Settings → Accounts & Privacy 固定驗證：允許 guest checkout＝ON、checkout 可登入現有帳戶＝ON、checkout 可建立帳戶＝ON、向新帳戶寄 password setup link／用 email 作 login＝ON。設計稿另有 member 同 guest checkout，只有 guest 顯示預設不剔「建立帳戶」checkbox；帳戶建立、setup token 同電郵生命週期全交 Woo 原生流程。Woo會喺checkout submit建立所選帳戶並可即時寄setup email，**唔會等付款成功、付款失敗亦唔回滾已建帳戶**，所以客面文案唔可承諾「付款後先開戶」。剔選後先由Woo建立帳戶、再建立嘅order凍結`created_as_guest=false`，屬member-origin，但VIP／counts仍要等`PaymentEvidenceService`確認成功先計；無剔就凍結`created_as_guest=true`，官方 order-key URL 繼續提供 Guest 查看訂單，日後以同一 email 註冊亦**唔會附加、認領或補計**舊 Guest order。
- **Guest vs Member 顯示唔同**：小計 + 運費（送貨方式於結帳計算，§7；免運門檻 admin option）+ 總計；coupon 欄可用（§6 規則）。**登入會員**額外出 **會員優惠行（自動會員價，不可移除，§5）**＋（若同時輸入優惠碼）**「二選一取最抵」結果（完全依§6唯一公式／predicate）**；guest 冇會員行、可用優惠碼。
- **Checkout「已有帳戶？登入」= WC 原生 inline returning-customer 展開式表單**（`woocommerce_checkout_login_form`，用 `.co-field` tokens skin，喺 checkout 頁內展開，**唔係**跳去 Account Login 頁 — demo 個跳頁 link 只係 placeholder）。登入成功留喺 checkout，購物車不變。
- **其他入口嘅登入後返回路徑**：任何由店頁去 login 嘅 link 帶 `?redirect_to={當前 URL}`；theme hook `woocommerce_login_redirect` + registration redirect 尊重佢（fallback = my-account）。 `redirect_to`只接受同站HTTPS URL，server必須用`wp_validate_redirect()`配合明確fallback並拒絕外站／protocol-relative值，防止open redirect。
- **Cart summary = 可編輯估算**（coupon + qty）；**Checkout summary = 唯讀 review**（line items 縮圖 + 最終數 + **收埋式優惠碼欄**，無 qty 編輯，改數返 Cart）。cart / checkout 總計必須一致（共用 `WC()->cart`）。

---

## 18. 帳戶 auth = 單一 `/my-account/` route（session 切換 · WooCommerce 標準）
- 設計稿有兩個 state 檔：`HIBI LAB Account Login.html`（logged-out：登入 / 註冊 / 忘記密碼 / 重設連結已寄 / 重設密碼）+ `HIBI LAB Account.html`（logged-in dashboard）。
- **Production 唔係兩條獨立 public route** —— WooCommerce = 一個 `/my-account/` core page 按 session 自動切換：未登入 → login/register；已登入 → dashboard；`/my-account/lost-password/`；email 重設連結 → reset form。
- Theme override：`woocommerce/myaccount/form-login.php` / `form-lost-password.php` / `form-reset-password.php` / `dashboard.php`，**唔好將 static HTML 原封搬入 production**。
- **Login 頁 eyebrow「HIBI LAB」= link 去 `/hibi-lab/`**（Login 係唯一冇 breadcrumb 嘅店頁，eyebrow 做返回路徑；demo 已改 `<a>`，form-login.php override 要跟）。
- Demo flow loop（已通，非 orphan）：會員 icon → Account（假設已登入）→ 登出 → Account Login → 登入 submit → Account。兩份稿 = 同一 route 兩個 state；auth 邏輯 + email 全 WooCommerce 原生，theme 只 on-brand skin。

---

## 19. 庫存狀態 product state（有貨 / 預訂 / 售罄）
- **資料來源：** WooCommerce 原生庫存 ＋ backorder 設定。**三態**：有貨（正常）／**預訂**（缺貨但開放 backorder，客即刻付款、到貨期＝per-product 欄＋全局預設「約 1 週到貨」§3）／售罄（缺貨且不開放）。
- **售罄：** 產品卡 + PDP 圖上 `售罄` badge（深啡底淺字）· 圖 `opacity:.6` · 加入購物車掣 disabled 灰態「已售罄」。（**唔做「補貨通知」訂閱** — 售罄只顯示灰態掣。缺貨想收訂單就設「預訂」，唔設就係售罄。）
- **預訂：** 產品卡 + PDP 圖上 `預訂` badge ＋ 卡/PDP 出到貨期行（**per-product 欄位＋全局預設，出廠「約 1 週到貨」§3** — 2026-07-10 鎖定）；PDP「加入購物車」→「預訂」掣（**仍可撳、即刻全額付款**）；結帳必填同意 checkbox（§20.1 新句）；**混合車允許＝等齊貨一併寄**（同意句尾明示）；**無接單上限**（商家熄 backorder 手動停）。badge class **`.stk-pre`＝外框 terracotta**（白底 terracotta 框字，別於搶先實色／售罄深啡；引擎 branch 已入 Shop×3＋store-chrome）。樣本：_p27（PDP 全頁）＋_p28（卡／搜尋行／checkbox／email 標示）。
- **會員搶先（同「預訂」分清）：** 只限qualifying tier會員買嘅產品→PDP出「會員搶先」badge，非會員／未達級先行鎖定加入購物車，避免洩漏可繞過嘅購買action。**授權同庫存係兩個依次判斷嘅gate**：未夠資格一律只見搶先鎖；夠資格會員通過後，再按同一variation真正庫存／backorder顯示有貨、預訂或售罄。即使開backorder，未夠資格仍不可預訂；引用級失效照G11 fail closed。呢個唔係將「會員搶先」同「缺貨預訂」混成一個狀態（§5）。
- badge class `.stk-out`（深啡）/ `.stk-pre`（預訂），`position:absolute;top:12px;left:12px;font-size:16px;padding:5px 13px;border-radius:30px`（demo `.stk` 實值）。WP 由庫存條件 render，設計稿提供視覺 pattern。
- **搜尋 overlay 結果行同樣要出 售罄／預訂 tag**（重用 `.stk` token；§22 搜尋 endpoint payload 嘅 `stock` 欄為 `in|preorder|out`，overlay 消費佢 — 免得撳個結果入到 PDP 先見到狀態）。

### 19.1 預訂收貨、FIFO 分配與出貨閘門（Woo 原生 lifecycle＋最小支援表）

> **唯一日常流程：Admin 按實際管理庫存嘅產品／variation 執行「收貨」，輸入今批實收正數增量 +N；系統只更新一次 Woo 庫存，再按付款時間 FIFO 自動分配預訂需求。Admin 唔逐張訂單標記貨齊。**

#### 架構邊界

- **全域lock order＝三階段、永不巢狀**：① order lock只claim operation／保存obligation generation＋CAS後即釋放；② 如要改stock／demand，按numeric stock-owner ID排序逐一取得owner locks，完成Woo結果核對＋row CAS後全部釋放；③ 再取得order lock，重讀operation、generation、owner projections及provider truth，先更新readiness／shipment／email並釋放。任何階段generation已變就退出重讀，唔跨階段保留舊決定；任何外部provider call都喺lock／DB transaction之外，但durable active claim一直保留。所有退款、zero adjustment、external recovery、receipt／correction、delivery及label create共用呢個次序，禁止order→owner或owner→order nested lock。
- **唯一order serialization primitive（唔留畀Dev自選）**：`{$wpdb->prefix}hibilab_order_guard`係窄InnoDB guard table；schema固定為`order_id BIGINT UNSIGNED PRIMARY KEY`、`version BIGINT UNSIGNED NOT NULL`、nullable `active_operation_kind VARCHAR(48)`、nullable `active_operation_uuid CHAR(36)`、nullable `active_generation BIGINT UNSIGNED`、`updated_at_utc DATETIME(6) NOT NULL`，只保存協調身份，唔保存order狀態、金額、地址或provider payload。Schema/upgrade provisioner idempotent建立row；writer只可喺短DB transaction內`SELECT … FOR UPDATE`該row，空閒先以expected version claim exact `{kind,uuid,generation}`，同一operation可resume，另一operation退出；commit後即釋放。Woo CRUD會fire第三方hook，所以Woo CRUD／data-store read-write及provider/network call全部喺row-lock transaction之外；外部步驟前已由guard active identity＋各domain唯一durable operation row保存exact intent/checkpoint，完成後重取guard，逐項核對active identity、generation、domain version及外部truth先投影或清claim。PHP/request crash由InnoDB釋放短row lock，但active identity＋domain checkpoint保留畀同一operation續行；取lock、row provision、CAS或read-back任何一步失敗即fail closed並開exact incident，禁止fallback去MySQL advisory lock、object-cache/transient、timer、IP或browser identity。PaymentConfirmation、refund、FulfilmentAddress、shipment、order-status，以及建立order-bound email semantic outbox row嘅writer共用呢個primitive；outbox row建立後嘅sender/delivery只由該row CAS/claim序列化。生日、補購、auth及subscriber等冇order_id嘅email只用自己canonical user/reminder/job row＋generation／semantic key，唔造假order_id或借用order guard；各模組亦唔准另造order lock。
- **唯一site-wide serialization primitive（同樣唔留畀Dev自選）**：`{$wpdb->prefix}hibilab_site_guard`係bootstrap可用嘅窄InnoDB singleton table，固定只得`guard_id=1`；schema＝`guard_id TINYINT UNSIGNED PRIMARY KEY`、`version BIGINT UNSIGNED NOT NULL`、nullable `active_kind VARCHAR(32)`、nullable `active_operation_uuid CHAR(36)`、nullable `active_generation BIGINT UNSIGNED`、`updated_at_utc DATETIME(6) NOT NULL`，`active_kind`只准`stripe_connection|upgrade|role_reconcile`，schema/backfill歸`upgrade`同一owner。Bootstrap只可用固定idempotent `CREATE TABLE IF NOT EXISTS`＋singleton `INSERT IGNORE`建立並read-back；engine/schema/singleton/read-back任何不符即令HIBI mutation fail closed。Stripe connection、UpgradeCoordinator（包括schema/backfill）及role reconciler全部共用呢一行：短transaction內`SELECT … FOR UPDATE`，空閒先以expected version claim exact active identity，同一operation可resume、另一operation只退出並顯示現有active operation；Woo CRUD、Options API、provider/network call及長backfill全部喺transaction外，進度只寫各domain唯一canonical checkpoint。每步後重取singleton，核對active identity＋guard/domain version＋postcondition先推進；只喺canonical terminal read-back成功後清active fields兼`version+1`。Crash只釋放DB row lock，durable active identity/checkpoint仍由同一operation續行；冇lease、timer、force-clear、option/advisory/transient第二把鎖或自動當完成，永久不明就開incident並按該domain recovery/rollback收斂。

- **WooCommerce 係唯一 commerce owner**：Woo order、order item、商品庫存、付款後stock reduction／restore、order status、退款及官方 Stripe Gateway 仍然係交易真相。hibilab-commerce 唔重寫 Checkout、PaymentIntent、Stripe refund、Order Pay 或 webhook lifecycle，亦唔自訂第二套 payment／refund／inventory ledger。Woo官方`ReserveStock`對`backorders_allowed()`商品會跳過整行reservation，無法提供已批准嘅「同一行X件現貨＋Y件預訂」並發保證；HIBI只為呢個明確缺口保存下述**未付款正現貨claim**，唔複製或改寫Woo私有reserved-stock SQL。
- **HIBI 自訂範圍只得政策同投影**：VIP／best-wins、付款時預訂快照、未寄出預訂 demand／allocation、ready-to-ship projection、ShipAny readiness adapter、補購排程及品牌電郵。所有外部付款 mutation由官方 Woo Stripe Gateway發起；HIBI只消費Woo已驗證嘅order/payment/refund結果。
- **只用公開 extension points**：唔停用或複製 Woo reserved-stock私有SQL，唔用DB transaction包住會fire第三方hooks嘅Woo CRUD，唔override gateway private callback，唔另外call Stripe建立第二個object。指定版本冇公開hook做到嘅要求，必須先做compatibility spike或向供應商確認；未證實就fail closed，唔用monkey patch。
- **資料最少化及表名規則**：預訂／庫存支援只用四張窄表：`hibilab_stock_owner_state`（每個stock owner一個lock/version anchor，**唔保存庫存量**）、`hibilab_checkout_stock_claim`（只保存未付款訂單暫時claim到嘅正現貨數量）、`hibilab_preorder_demand`（已確認付款但未履行嘅預訂需求）及`hibilab_stock_receipt`（收貨／更正operation audit）；全HIBI writer另共用上段一張`hibilab_order_guard`作純serialization anchor，佢唔屬inventory／payment ledger。另用一張常設、最小化 observation inbox `hibilab_shipment_event`接 webhook／公開 hook／public API poll 嘅去重與配對。每筆正規化 observation 計算 `dedupe_key UNIQUE`；最少欄位＝source、provider event／shipment／order reference、normalized state/evidence、provider version／updated-at／hash、nullable matched Woo order ID、processing state（`pending_unmatched|retry_due|matched|processed`）、attempt count、next attempt、last error同timestamps，並建 `(state,next_attempt_at,id)` index；無法收斂就保留最後真實processing state並由`OperationalIncidentRegistry`持有原因／gate，唔另加`needs_attention` state。Webhook／hook／poll只insert observation；單一 projector writer先可更新 `_hibilab_shipment_projection`。目前 current shipment truth 只係 Woo order meta 一份 versioned projection，唔另開shipment ledger；implementation 必須證實 HPOS-safe、order-scoped CAS，證實唔到先退到一張**每order只一row**、`UNIQUE(order_id)`＋version嘅projection table，仍唔係shipment ledger。只保存正規化後reference/state，**唔保存raw payload、唔複製完整shipment資料或PII**。所有文件內`hibilab_*`都係logical name，實體名稱一律用`$wpdb->prefix . 'hibilab_…'`建立／查詢，禁止硬編`wp_`。Claim唔係inventory：可售現貨仍只讀Woo stock，claim只喺未付款窗口內扣減「可再承諾嘅正現貨」；付款、取消或失效即收斂。Action Scheduler做durable job queue，Woo order notes／status保存客戶及營運結果。唔另起WMS、payment identity、refund engine、warehouse或generic command platform。
- **付款確認投影最少化**：`hibilab_payment_confirmation`只係上文每order一行、physical PK保證唯一嘅immutable confirmation projection；冇pending state、refund balance、provider mutation或通用event log，亦冇Woo meta canonical副本。`order_id`只係logical Woo reference，唔對posts或HPOS physical table加FK；所有Woo order讀取仍經CRUD/data store。Retention、privacy export/erase、backup/restore及physical-contract tests必須明列呢張表；歷史付款責任存在期間唔可刪除。

#### 預訂快照與 Woo 庫存

- **FulfilmentAddressService封地址／出單race**：收件地址、聯絡資料及自取點write-back雖不屬付款hash，但只可經同一controlled Woo CRUD service。未有label時先喺order phase claim＋version CAS，核對冇`creating|unknown` shipment，再寫地址、audit、bump shipping-obligation hash／generation並重算readiness；label建立方必須重讀同一generation。已有label時，先要求受支援provider void並以public truth證明冇current shipment，先可更改及重新出單；carrier handoff／delivered後拒絕改出貨地址並開營運incident，唔靜默改一邊。Admin、REST、Account address copy、自取點plugin write-back及第三方同步全部走同一service；既有表單UI不變。

- **庫存身份**用 Woo Product嘅 stock-managed owner ID；variation自己管庫存就用variation，跟parent就用parent。SKU只作顯示，唔作關聯鍵。
- **唯一owner lock**：所有checkout claim、付款後claim消耗、claim釋放、Woo stock reduction核對、receipt／correction、退款restock及demand allocation都先按numeric owner ID排序，喺短InnoDB transaction內`SELECT … FOR UPDATE`對應`hibilab_stock_owner_state` row；row以`stock_owner_id PRIMARY KEY`由schema/provisioner或首次使用時idempotent建立，只有version／updated timestamp，絕不緩存stock。呢個係全系統唯一owner serialization primitive；取唔到lock或read-back失敗即fail closed，唔fallback去timer、object-cache lock、IP/browser identity或先讀後寫。
- **Checkout server-authoritative**：每次order review／place order都由Woo重新驗商品、variation、數量、backorder資格、會員資格、coupon、運費及final total。前端posted price、stock、VIP tier或split一律唔可信。
- **一般貨沿用Woo hold；backorder行用最小claim**：不接受backorder嘅一般有貨商品完整沿用Woo原生reserved-stock及不足即拒絕。接受backorder嘅stock owner由於Woo原生不保留任何數量，HIBI喺order item已建立、交gateway前建立一筆未付款claim：owner lock內先確認該owner冇`quantity_delta<0`而state仍`pending|applying|applied|projecting`嘅未完成stock correction，亦冇該owner任何open incident（有就fail closed）；再重讀Woo public stock `S`、以Woo公開`wc_get_held_stock_quantity()`取得該stock owner其他order仍有效嘅native held qty `H`（exclude current order），以及所有`state=active` HIBI claims嘅`claimed_in_stock_qty`總和`C`。Active claim就算另有incident仍保持active並計入C；incident唔改claim state、亦唔釋放承諾。計`claim_in_stock=min(order_qty,max(0,S-H-C))`、`preorder_qty=order_qty-claim_in_stock`，並將S/H/C輸入及hash同claim一併保存／read-back，再以`order_item_id＋claim_generation`唯一落盤。咁即使Admin喺既有native hold未完時由「不接受預訂」切去接受backorder，亦唔會將同一正現貨承諾兩次；已claim後先落盤嘅負數correction亦受同一owner lock序列化。Claim只保留正現貨承諾，唔改Woo stock；付款成功仍由Woo／官方gateway標準stock reduction將全數order qty扣入唯一Woo stock（可變負），HIBI絕不預扣或扣第二次。
- **可用現貨不變量**：任何仍可能扣正現貨嘅未付款HIBI order，在Woo stock `S`真正減少變得可見之前，必須一直由native hold `H`（不接受backorder行）或`state=active` claim `C`（接受backorder行，包括另有open incident嗰啲）代表；native hold只可喺同一次Woo成功stock reduction之後先release，HIBI claim亦只可喺相同public reduction evidence read-back後consume。因此並發讀取`max(0,S-H-C)`最多暫時低估，結構上不可高估或雙重承諾。所pin Woo／gateway版本必須用hook-priority contract test證明「reduce先於native release」；升級後次序不符即fail closed。
- **付款前封旁路；付款後唔攔Woo扣貨**：所有HIBI Woo產品由schema-as-code保存隱藏、不可由一般表單改寫嘅`_hibilab_catalog_schema=hibilab-v1`（variation繼承parent；Admin新增／CSV import經同一product service自動建立並read-back）。Checkout、Order Pay、Admin、REST及manifest列明嘅第三方order/status入口，喺**建立／重用PaymentIntent或令order進入任何可觸發stock reduction狀態之前**，同一`OrderLifecycleGuard`必須驗`_hibilab_order_schema`、完整checkout seal，並對每個backorder line見到同generation active claim；缺任何一項就拒絕進入付款，唔畀未經HIBI checkout嘅手建order變成可付款HIBI order。呢個係付款前gate，唔接管Woo stock writer。付款一旦已由官方gateway開始／成功，`woocommerce_payment_complete_reduce_order_stock`、`woocommerce_can_reduce_order_stock`及native hold release對HIBI都保持Woo原生pass-through，確保Woo一定按自己次序扣貨、設stock-reduced flag再釋放hold；HIBI只喺之後read-back核對並consume claim。候選付款已成功但claim／seal其後損壞時仍保留付款及Woo扣貨真相、開incident兼關ShipAny，絕不因投影故障阻止扣貨、放大可售量或造第二次stock mutation。非HIBI產品／order完整pass-through。
- **Order-item immutable snapshot**：Woo order item 本身係產品名、數量、公開 variation meta、SKU 顯示、subtotal／total 嘅唯一歷史真相，HIBI 唔複製一套同名欄位。HIBI 只另存 Woo 本身冇嘅 `stock_owner_id`、客人確認嘅 provisional 現貨／預訂 split、最終履約 split、到貨期、同意版本，以及 §22 best-wins 顯示所需嘅 base/effective unit price、winner type/code、line/member savings minor-unit snapshot。付款成功頁、Account及交易email由同一 reconciliation model讀 Woo order item＋呢份最少化meta；產品日後改名／改價／收貨都唔倒改歷史承諾，所有顯示行相加必須等於 Woo order total。 Woo order建立時另寫immutable local scope marker `_hibilab_order_schema=hibilab-v1`（只由HIBI cart／checkout service產生）；所有HIBI email、報表、readiness、ShipAny、refund projection及capability resource check只認呢個local marker，Stripe metadata可完全缺席。預訂同意用committed immutable consent registry按version保存exact normalized sentence／hash；每個order item凍結consent version＋sentence/hash＋當時ETA及split snapshots，日後改文案仍可重現舊單證據。由checkout seal完成起，Admin、REST、CSV/import、bulk及第三方同步任何入口都必須server-side拒絕改寫受保護嘅product／variation／order-item identity、quantity、currency、line subtotal／total、order total、pricing winner、stock owner、split／ETA及consent evidence；只准經有typed operation、窄capability、鎖及audit嘅指定writer作退款／履約adjustment，原始行仍不改寫。收件地址、聯絡資料及內部note唔屬呢個hash，可按既有受控流程更正。
- **Split 兩階段、付款前封口**：Order Review只顯示provisional split。Woo建立order item後、交gateway前，HIBI按上文owner lock一次過建立／重用同generation claim並取得final split。若final split同客人剛才確認嘅畫面不同，保留同一active claim及同一pending Woo order，經所pin版本受支援嘅order-resume extension point返回**同一個既有Checkout畫面**，更新商品行／到貨期；如新split包含預訂量，既有預訂同意checkbox必須重新由未剔狀態確認。第二次提交只可resume同一order／claim generation，逐項核對cart identity、數量、價錢、地址、consent及claim未過期；全部一致先用Woo CRUD凍結final split／ETA／consent及checkout seal，save＋read-back＋hash一致先call gateway。唔建立第二張order、唔釋放後再搶同一批stock、亦唔新增任何UI元件；若pin版本無公開resume boundary可保證同一order/item/meta，backorder checkout保持launch blocker。Item meta／seal write明確失敗且尚未call gateway先安全釋放claim並取消attempt；結果不明就claim保持`active`並開exact incident，唔估已釋放。Process crash留下嘅active claim由下述truth reconciler按Woo付款／可支付狀態收斂，唔靠browser回程。
- **Claim lifecycle同Order Pay**：Claim state只准`active|consumed|released`。只有`PaymentEvidenceService=confirmed_paid|confirmed_zero`且Woo公開`_reduced_stock`／order stock-reduced evidence核對全數正確後，先喺owner lock將claim `active→consumed`；付款證據已確認但stock reduction證據缺失／矛盾時claim保持`active`、另開精確incident兼關ShipAny，唔自行再扣一次。`PaymentEvidenceService=unpaid`只代表可再驗§5 Pay gates，唔代表可以先放位；`pending|unknown`或raw Woo `on-hold|failed|cancelled`本身亦不可因時間到就猜失敗或釋放。Order Pay只可resume同一張order及原有仍有效嘅reservation（非backorder行＝Woo native hold；backorder行＝HIBI active claim）；提交前，HIBI claim喺owner lock用CAS原位遞增同一claim嘅generation／attempt fence，quantity及佔位全程不變，舊generation完成後只可no-op，唔准先release再重搶；只有native hold嘅行則完全沿用Woo原生order-pay／hold lifecycle，HIBI唔造第二份claim。若hold／claim已失效、final split同原承諾不同或order已不可支付，§5規定無Pay CTA，取消／expire舊單並要求重新checkout，唔改寫舊單承諾。Claim／Woo pending order嘅到期核對時間**只讀§21 Stripe plugin鎖定嘅Woo Inventory「Hold stock」canonical setting**（Phase 1=`60分鐘`），HIBI唔另存第二個expiry或自訂timer；reconciler到期後仍要重讀付款真相同order可支付狀態先release。Payment success、failed／declined、cancelled、hold到期、3DS超時／晚到、double-submit、order-pay重試及claim commit後crash全部屬pin-version compatibility gate；任何一路無法唯一收斂就唔開backorder付款，絕不靜默洩漏或過早釋放現貨。

#### Demand、收貨與 FIFO

- **受保護owner永不absolute set庫存**：產品頁／REST／CSV即使提交absolute target，`StockAdjustmentService`都先喺durable claim內以當刻public stock truth換算signed delta，再只用`wc_update_product_stock(...,abs(delta),'increase'|'decrease')`嘅相對、一query Woo操作；禁止對有confirmed未履行HIBI責任嘅owner使用`set`覆寫，避免同時checkout decrement被抹走。Mutation後observed若唔等於保存嘅before＋delta，先按Woo order stock-reduction evidence reconcile；仍不可證明就`needs_attention`，絕不repeat或改用set。Admin／REST／CSV absolute edit對撞checkout、refund及receipt都要有barrier UAT。
- **Owner truth異常先fail closed**：若Woo stock deficit `max(0,-S)`大過該owner全部open preorder demand `R`，代表仲有普通現貨、遺漏、第三方或未解釋責任，conservation projector不可用`min()`將差額藏起。建立`stock_deficit_outside_preorder:{owner_id}` incident，停止allocation mutation並對使用該owner嘅所有未履行HIBI訂單關ShipAny，直至reconciler以可驗證order／stock truth解釋及收斂；ready-to-ship永遠為false。
- **Final-delivery亦係同一owner mutation**：`ShipmentEvidenceService`要將demand由`open→fulfilled`前，先按全域三階段protocol喺order phase凍結obligation generation，釋放後再按owner ID排序取得stock-owner locks，重讀immutable obligation hash、最新成功quantity cancellations及current shipment truth並以row version CAS更新；釋放全部owner locks後先喺新order phase重算readiness。Delivery、refund、void或stock correction並發只可一個generation／version成功，失敗方重讀真相，唔以事件先後猜結果。
- **退款、出單、負數更正共用durable claim barrier**：建立ShipAny label前先喺order phase以CAS將shipment projection設為`creating`，保存operation UUID＋obligation hash，並逐個重讀該單所有owner都冇active negative-stock operation；帶quantity退款先claim active refund operation並拒絕`creating|unknown|label_created|shipped`；signed負數correction先為stock operation durable claim全部可能受影響owner，再喺每個owner lock內拒絕任何`state=active` checkout stock claim（不論有冇open incident），並逐張重讀冇active label/refund claim，模擬conservation結果亦不可撤走current shipment責任。即係claim先到就暫停負數更正直至付款consume或安全release；負數operation先到就令新claim fail closed，兩個方向都唔可抽走已承諾正現貨。所有external call都喺claim成功後先發出；correction喺call Woo前最後一次於owner phase核對同一generation。三者任何一方先claim就令其餘兩方fail closed；timeout保持`unknown`並只做public-truth reconciliation，唔靠兩次「再檢查時間」賭race。若外部庫存已漂移，只開incident及關outbound，唔改寫已承諾shipment obligation；正數receipt不受影響。
- **共用stock adjustment audit row**：`hibilab_stock_receipt`係最小stock mutation audit table，唔係第二份inventory；新增`operation_kind=receipt|correction`及signed `quantity_delta`。日常「收貨」只准`receipt`＋正整數`+N`；既有Woo產品庫存編輯／CSV／REST adapter只可建立`correction`並保存來源、actor、before／expected／observed checkpoint。兩者共用`operation_uuid UNIQUE`、同一owner lock、Woo公開stock API、read-back及同一conservation projector；任何不明結果保留最後checkpoint state並開incident，唔再加另一張表或另一套allocation writer。
- **單一stock-owner conservation projector**：所有confirmed demand建立／補建、receipt、成功quantity cancellation／restock及受控庫存更正，全部喺同一個sorted stock-owner lock protocol內完成或隨即呼叫同一projector；任何writer都唔可自行加減allocation。對每個owner，以Woo公開stock truth `S`、全部`state=open`需求總和`R=Σrequired_qty`計`waiting_total=min(R,max(0,-S))`、`covered_total=R-waiting_total`；再按`priority_at_utc,order_id,order_item_id`嚴格FIFO逐行投影 `allocated_i=clamp(covered_total-prefix_required_before_i,0,required_i)`。因此遲到嘅較舊demand會自然取得應有優先權，較新行先失去allocation；部分取消、部分退款、補貨盈餘或負數更正全部用同一守恆公式收斂，唔靠sticky例外、逐個edge-case補數或二次Woo stock mutation。每次寫後read-back驗`0≤allocated≤required`、`Σallocated=covered_total`及FIFO前綴；Woo stock owner無法唯一解析、數量非整數或public truth不可證明就零mutation並開`needs_attention`。鎖內只做HIBI row短transaction；Woo API call先以checkpointed operation落盤，離開DB transaction執行，重讀結果後再入owner lock投影，避免第三方hook造成跨系統假原子。

- **Claim／owner schema**：`hibilab_stock_owner_state`只得`stock_owner_id PRIMARY KEY`、version、created_at_utc、updated_at_utc。`hibilab_checkout_stock_claim`至少有`claim_uuid UNIQUE`、`UNIQUE(order_item_id,claim_generation)`、nullable `active_order_item_id UNIQUE`、order_id、order_item_id、stock_owner_id、order_qty、claimed_in_stock_qty、preorder_qty、state、expires_at_utc、version、last_error及timestamps；守所有qty為非負整數、`claimed_in_stock_qty + preorder_qty = order_qty`。只有`active`時`active_order_item_id=order_item_id`，incident唔改呢個state；安全收斂成`released|consumed`先原子設NULL，因此同一order item同時間結構上只得一個未收斂generation，唔靠先查後寫。Released／consumed row保留做audit但唔再扣availability。主要index固定為`(stock_owner_id,state,expires_at_utc,order_id,order_item_id)`及`(order_id,state,order_item_id)`。
- **一行一筆 demand＋一套stock operation schema**：`hibilab_preorder_demand`至少有`order_item_id UNIQUE`、order_id、stock_owner_id、required_qty、allocated_qty、priority_at_utc、state、version、created_at_utc、updated_at_utc；守`0 ≤ allocated_qty ≤ required_qty`。`state`只准`open|fulfilled|cancelled`：已全數allocated但未經`ShipmentEvidenceService`確認final-delivered仍係`open`；只有該item全部餘下quantity已驗證送達先係`fulfilled`；只有成功、帶精確quantity嘅退款／取消先可按同一projector成為`cancelled`。`needs_attention`係獨立incident／gate，唔係demand state。Conservation projector必須喺owner lock內查**全部**`state=open` rows並按穩定FIFO重算；只有waiting列表／報表先可另篩`allocated_qty<required_qty`，waiting_qty即時計`required_qty − allocated_qty`。`hibilab_stock_receipt`只得一個canonical row shape：`operation_uuid UNIQUE`、`operation_kind=receipt|correction`、stock_owner_id、signed `quantity_delta`、state（`pending|applying|applied|projecting|completed`）、version、stock_before、expected_after、observed_after、source、attempt_count、last_error、actor_user_id及timestamps；不明／失敗時保留最後真實checkpoint state並由incident registry持有blocker，唔創造錯誤state。日常收貨UI只係建立`kind=receipt,quantity_delta=+N`，唔另有`receipt_uuid`／`received_qty`第二套欄。唔保存成本、倉位或lot。主要query index固定為demand `(stock_owner_id,state,priority_at_utc,order_id,order_item_id)`及stock operation `(stock_owner_id,state,updated_at_utc)`；schema migration要用接近production行數跑`EXPLAIN`／smoke test，避免claim到期掃描、FIFO及監察變成full scan。
- **建立時機**：只喺`PaymentEvidenceService`首次由非確認狀態轉成`confirmed_paid|confirmed_zero`後，按order-item快照為 preorder_qty > 0嘅行idempotent upsert demand；`priority_at_utc`固定用service回傳嘅首次付款／zero-total確認時間，再以order_id、order_item_id作穩定tie-break。UNIQUE(order_item_id)令webhook、return及reconciliation重送只建立一次；Admin手動改status、browser return或order note永遠唔會建立demand。即時 transition writer只係低延遲路；checkpointed、可分頁嘅 truth reconciler會以同一PaymentEvidence＋immutable order-item snapshot＋成功qty退款／取消＋履約真相重算應有demand，修復「付款證據已落盤、demand upsert前crash」而唔重做Woo stock mutation。
- **未確認付款唔入FIFO**：只按§5 service判斷；`PaymentEvidenceService`未到`confirmed_paid|confirmed_zero`一律唔建立open demand，raw Woo `pending|failed|cancelled`本身既不可放行、亦不可推翻已確認付款。Order Pay經同一service首次確認後先按真正首次確認時間排入；Guest日後開戶或修改帳戶唔改priority。
- **收貨 +N（crash-safe，但唔作假「原子」承諾）**：專用Admin確認面板先驗nonce、窄capability、stock owner及正整數，並由server預先簽發、綁定operator＋stock owner兼保存嘅operation UUID；同一個表單double-click／network retry必須沿用同一UUID，新一批收貨先取得新UUID。`operation_uuid UNIQUE`先落盤，同一UUID只讀同一stock operation。Worker取得stock-owner互斥鎖後讀`stock_before`、計`expected_after`、用optimistic version將row轉`applying`，再只經Woo公開stock API執行一次+N並即時重讀`observed_after`。確認觀察值符合本次mutation先轉`applied`並進FIFO allocation；projector失敗只可重跑projection，絕不再次加庫存。若process喺Woo mutation與operation checkpoint之間crash、row停喺`applying`，reconciler不可盲目再+N：能由保存值及Woo可驗證audit證明已套用先續行，否則row保留`applying`並開incident，由Admin用受控「確認已套用／確認未套用並執行」復原action核對一次。呢個界面承認Woo API同自訂row無跨系統原子交易，目的係令任何不確定狀態可見且永不自動重複入庫。
- **分配規則及並發封口**：同一owner永遠由最早付款order開始；一張單欠4、今批得3，就分3/4並保持not ready，唔跳過佢討好後面細單。N大過全部waiting時，餘數自然留喺Woo可售stock。其他商品未齊唔會改變呢個owner嘅可證明供應量；但allocation本身係上述守恆projection，唔係不可更正嘅sticky承諾——補建較舊demand、成功quantity cancellation或受控負數更正時必須按同一FIFO公式重算，並永遠由最新／較後行先減allocation。stock-owner lock由operation結果核對至本次projection完成一直持有；projector只對HIBI自訂operation／demand rows開短InnoDB transaction，以穩定FIFO次序`SELECT … FOR UPDATE`，commit前驗全部守恆式，deadlock按同一operation UUID安全重試。Woo API或會fire第三方hook嘅call永遠唔放入呢個DB transaction。
- **產品生命週期守衛**：只要任何已付款 HIBI 訂單仍有實物履約責任（包括普通現貨未寄、已分配預訂、open demand）、active退貨／換貨／爭議／legal hold，或最後一件相關商品`delivered_at`後尚未過已批准嘅香港曆日7日瑕疵退換期，就阻止永久刪除相關product／variation及任何會改變stock-owner identity嘅設定（包括variation自管轉parent-managed或反向）。第7日仍阻止；第8日亦只喺全部責任、case及hold都已結束先可永久刪除。可以下架、停backorder或移入垃圾桶以停止新訂單，但immutable order item、`stock_owner_id`、receipt及履約projection仍保留。**Active owner唔設重寫／搬移捷徑**：要由variation自管轉parent-managed或反向，必須等全部相關責任、case、hold及7日窗口完結先可改；否則server-side拒絕，避免改寫已付款snapshot或引入owner alias／generation系統。Woo原生產品頁、CSV import、REST或第三方同步如會改**任何confirmed、未完成實物責任所引用**嘅stock owner（包括冇preorder demand嘅普通現貨未寄單），全部經同一checkpointed `StockAdjustmentService`：先保存typed `receipt|correction` operation UUID、signed delta、source／actor及before／expected值，離開DB transaction只call一次Woo公開stock API，再重讀observed值並交同一conservation projector；既有產品頁／import UI不變。證據不明就`needs_attention`兼關ShipAny，絕不盲目重call、靜默改allocation或提供generic「清警告」捷徑。
- **可重建**：每日reconciliation由`PaymentEvidenceService`＋paid order-item快照＋成功退款／取消qty＋receipt/allocation／shipment audit重算並比較projection；只修projection，不重做stock mutation。證據完整而只係缺row時，以`order_item_id UNIQUE`同一writer idempotent補建或補成正確terminal projection，`priority_at_utc`仍用首次確認時間；hook同reconciler並發亦只得一row。已取消／已履行舊單唔重開obligation。只有重複row、snapshot矛盾、負數或總分配超出可證明收貨量等無法唯一判定嘅情況，先關ShipAny gate並喺System Status／相關order顯示reason-scoped `needs_attention`，唔猜數。Reconciler按穩定cursor分頁，任何舊錯單都唔可餓死後面新單。

#### 退款、取消與出貨數量

- **Woo refund hook choreography（pin版本契約）**：`woocommerce_create_refund`係Woo refund第一次save前嘅唯一claim點；呢度只可喺order lock內建立／核對唯一active operation，將operation UUID寫入**in-memory** refund object meta並read-back operation，唔假設refund ID已存在。其後`wc_stripe_refund_request`先可由active order slot精確定位同一operation（禁止「最新時間一筆」），綁定實際Woo refund ID、用Woo CRUD重載並核對amount／quantity hash／UUID meta，再將operation CAS到`processing`且read-back成功；最後以associative merge方式加入provider metadata，保留gateway既有request keys。任何一步失敗都喺provider call前fail closed。呢個hook次序、callback priority、非HIBI passthrough及gateway升級差異必須寫入compatibility contract test。
- **Dashboard外部退款係明示compatibility gate**：所pin gateway現行import可能喺文件化post-hook之前先呼叫`wc_create_refund(refund_payment=false)`，pre-save未必有可信provider provenance；staging spike必須證實受支援trusted pre-save context，否則該positive provisional row只能quarantine／阻止HIBI副作用，等文件化post-hook＋bounded provider read truth精確建立sanctioned projection。`wc_stripe_webhook_received`只當低延遲提示，consumer exception被gateway catch／ACK後仍要靠bounded reconciliation收斂。多次部分退款嘅canonical provider truth必須分頁列舉原PaymentIntent／Charge全部refunds，再以`(account_id,livemode,refund_id) UNIQUE`逐筆配對；禁止依賴parent refund meta、`charge.refunds.data[0]`、金額或時間猜第一筆。若所pin版本冇公開、可驗證嘅enumeration／matching contract，Dashboard自助復原保持launch blocker，正常Woo Admin退款仍可按已通過契約運作。
- **一張canonical refund operation table**：`hibilab_refund_operation`涵蓋`provider_refund|zero_fulfilment_adjustment|external_fulfilment_adjustment`三類；正金額、HK$0.00同外部復原全部先有`operation_uuid UNIQUE`、kind、order、amount minor units（zero可為0）、immutable item quantity map/hash、active slot及state。External recovery batch只係把多個provider refund identity綁去同一leader operation，唔另成quantity writer。Restock checkpoint除咗每個owner嘅before／expected／observed，亦要保存每個受影響order item預期`_reduced_stock`／`_restock_refunded_items`等所pin Woo公開helper會更新嘅bookkeeping hash；逐步read-back owner stock＋全部item meta一致先可標completed。Stock正確但item bookkeeping不明同樣`needs_attention`，唔盲目再call helper。
- **單一FulfilmentQuantityGuard**：正常Woo Admin／AJAX／REST退款、HK$0.00本地調整及Stripe Dashboard外部退款復原，凡帶item quantity都先讀同一`ShipmentEvidenceService`。未有label／shipment先可取消quantity；已有active／current label或shipment就拒絕，直至public truth證明void完成；carrier handoff或delivered後仍可退款銀碼，但一律按amount-only fulfilment semantics處理，唔減demand、唔restock、唔倒退fulfilled狀態。實物真正退回驗收後只經既有「收貨 +N」入庫。三個入口共用同一guard、同一reason code同一contract test，唔各自複製判斷。
- **單一RestockProjector及quantity算術**：所有成功normal refund、HK$0.00 adjustment及external recovery嘅quantity／restock副作用只由同一projector執行。每個order item喺immutable quantity map下計`k_pre=min(cancel_qty,required_qty)`、`waiting=max(0,required_qty-allocated_qty)`、`k_waiting=min(k_pre,waiting)`、`k_allocated=k_pre-k_waiting`，然後`required'=required-k_pre`；超出`k_pre`嘅取消量只減非demand physical obligation。`required'>0`保持`open`；送達前只有`required'=0`先成`cancelled`，已final-delivered保持`fulfilled`。Projector喺provider success／zero-local truth已確定後，先將每個stock owner聚合delta、before／expected／observed及step state durable寫入同一operation row，再按owner ID排序取得共用owner locks，於request-local、exception-safe typed context內暫時容許Woo公開`wc_restock_refunded_items()`，逐owner mutation後即read-back checkpoint；完成後交conservation projector重算FIFO。任何中途crash只續未完成step，observed不明就`needs_attention`，永不盲目再restock；所有owner lock釋放後先更新order／readiness／email projection，避免鎖序反轉。
- **RefundMutationGuard**：HIBI refund row由operation truth保護。`processing|unknown|succeeded`、provider external import、zero/external fulfilment adjustment一律immutable；只有同一typed recovery context先可hard-delete已精確證實provider terminal `failed|cancelled`嘅provisional Woo row。Implementation必須server-side接管所pin Woo版本嘅Admin AJAX delete-refund及REST DELETE boundary，非HIBI完整pass-through，HIBI未授權刪除拒絕；HPOS on/off均測。第三方direct CRUD如仍繞過，post-delete observer立即開incident並由immutable operation／external registry精確重建projection；新退款嘅remaining amount／qty及provider max-refund guard永遠讀immutable succeeded operation truth，唔單靠可刪嘅Woo `get_total_refunded()`，所以缺row亦不可造成第二次Stripe退款。

- **正常退款入口只有Woo order Admin**：金額退款由官方Woo Stripe Gateway處理；HIBI唔直接call Stripe。部分退款可有商品qty、運費、fee及純金額調整，Woo refund lines係本地分類真相。
- **禁止正金額本地繞過**：任何正金額 HIBI Stripe 訂單都唔准以 Woo「手動退款」／`refund_payment=false`、直接REST、AJAX、bulk action或第三方入口製造本地money refund去扮provider退款。Server-side validation必須覆蓋Admin、AJAX、REST及bulk mutation boundary，只容許四類：① 官方gateway真正退款；② 已按下文逐項精確配對嘅Stripe Dashboard外部退款；③ 金額精確HK$0.00、帶immutable kind＋operation UUID嘅本地履約數量調整；④ 初次gateway timeout／WP_Error令Woo row遺失，但官方read truth其後已精確證實provider succeeded嘅同一退款操作投影。第④類必須逐項吻合operation UUID、provider refund ID、原始immutable amount／quantity hash、歷史account／livemode／currency／PaymentIntent／Charge chain，並以operation/provider refund UNIQUE保證只建一筆，`refund_payment=false`只係避免再次call provider，唔係另一次退款。UI隱藏唔算安全閘，任何其他繞過嘗試都要拒絕兼記可稽核原因。
- **退款操作先落盤，再交官方gateway**：正金額HIBI退款仍由Woo Admin＋官方Woo Stripe Gateway唯一發起，但唔可假設`wc_create_refund()`建立嘅row已代表provider成功。Canonical `hibilab_refund_operation`保存`operation_uuid UNIQUE`、kind、order ID、nullable Woo refund ID UNIQUE、Stripe account＋livemode＋nullable provider refund ID（有值時三者UNIQUE）、amount minor units、immutable line quantity map/hash、restock checkpoints、兩個正交狀態、attempt／error及timestamps。`provider_state`只准`not_applicable|not_sent|processing|unknown|succeeded|failed|cancelled`（HK$0.00本地adjustment=`not_applicable`）；`projection_state`只准`pending|applying|completed`。Provider結果同本地投影永不塞入一個workflow label：provider已`succeeded`但projector crash仍永久保留money-success truth，projection保留最後checkpoint並由獨立incident gate阻止後續；VIP、remaining refundable amount及客面退款銀碼只讀provider truth，stock／demand／email只按projection checkpoint續行。Nullable `active_order_id UNIQUE`由row建立起封同一order第二筆quantity／money mutation，只喺`succeeded|not_applicable`且projection completed，或`failed|cancelled`且failure tombstone、未成功Woo row清理及remaining amount／qty恢復完成後先清；`unknown`或該operation任何open incident一直佔住。Hook實作只跟上文「Woo refund hook choreography」：pre-save只claim＋寫in-memory meta，唔做不存在嘅refund-ID／meta read-back；`wc_stripe_refund_request`階段先綁ID、CRUD reload核對、將provider state轉processing並合併provider metadata。兩個公開contract及provider outcome可見性必須喺compatibility spike證實，缺一就阻止正金額自動退款，絕不靠金額／時間／order note猜配或另寫Stripe client。
- **Woo原生副作用一律等成功**：對正金額HIBI退款，公開`woocommerce_can_restock_refunded_items`預設返回false，忽略Admin／REST傳入嘅restock；只有上述已驗證成功嘅`RestockProjector`以request-local、exception-safe typed context呼叫所pin Woo公開restock helper期間先返回true，`finally`必須清除context，非HIBI始終完整pass-through。公開`woocommerce_order_fully_refunded_status`預設返回false，未經成功projector唔將parent提早設`refunded`；對已有confirmed payment嘅HIBI order，公開`woocommerce_can_restore_order_stock`亦拒絕由直接status drift補貨。`woocommerce_refund_created`／`woocommerce_order_refunded`只係觀察輸入，唔可直接驅動VIP、demand、客面退款、電郵或ShipAny。只有可驗證provider `succeeded`先由單一RefundProjector喺order／operation lock下，以`UNIQUE(operation_uuid,transition_kind)`驅動一次RestockProjector、qty/demand更新、VIP／客面／email projection；全數成功退款且已無金錢或履約責任時先設定相應Woo final status。pending／request accepted／timeout／unknown期間保留operation active、關ShipAny及禁止第二筆退款；客面同交易email只讀normalized succeeded totals，唔將暫存Woo refund row講成已退款。
- **單一OrderLifecycleGuard封status旁路**：所有HIBI Admin、AJAX、REST、bulk及manifest列明整合嘅status write，save前先由同一guard核對`PaymentEvidenceService`、RefundProjector及ShipmentEvidenceService，只准相應sanctioned operation轉移；直接將confirmed order改`pending|failed|cancelled|refunded`一律拒絕，唔靠事後notice。Woo core會喺`woocommerce_order_status_refunded`自動呼叫`wc_order_fully_refunded()`建立一筆冇provider refund嘅正金額row，所以implementation須喺所pin版本用公開action registry將呢個core callback包成HIBI-scoped wrapper：非HIBI完全照原生；HIBI只有verified RefundProjector context先可呼叫，其他狀態誤改絕不建立refund。取消／pending嘅stock restore仍由`woocommerce_can_restore_order_stock`按同一context fail closed；failed／cancelled電郵guard亦讀同一service。若未知第三方仍造成status drift，order-lock reconciler即恢復evidence-derived status，經Woo公開`wc_update_total_sales_counts()`／`wc_update_coupon_usage_counts()`及stock-reduced flag核對修復已觸發嘅sales／coupon／stock projection，保留reason-scoped `needs_attention`供營運見到；唔先放貨、寄信或創造refund row。Wrapper、callback priority及非HIBI passthrough全部寫入compatibility manifest同contract test，Woo升級唔吻合就fail closed。
- **失敗／timeout可安全再做**：Woo core可能喺gateway回WP_Error時刪除refund row，亦可能喺provider仍processing時保留一筆已被Woo totals計入嘅row；兩種情況都由pre-call operation UUID保住身份。只讀reconciler只可經所pin官方gateway公開、可驗證truth核對同UUID嘅provider結果，永遠唔自動重call refund mutation。證明succeeded但Woo row遺失時，經同一operation以Woo CRUD建立一筆帶provider-succeeded marker嘅精確money-refund projection（`refund_payment=false`，不再call Stripe）；證明terminal failed／cancelled時，先durable寫failure tombstone，再喺order lock內用Woo CRUD hard-delete仍存在嗰筆**未成功**refund row、清除Woo order/refund caches並由真實evidence恢復parent status／remaining amount／remaining qty，最後先釋放active slot畀Admin建立一筆新operation。仍不可判就`needs_attention`兼保持鎖住，唔因時間到而當失敗。HK$0.00、`refund_payment=false`且有immutable kind marker嘅本地履約數量調整唔入provider normalizer、money totals／VIP delta或退款email，亦絕不call Stripe；佢只可影響指定quantity、一次restock及fulfilment projection。
- **Dispute／Radar履約閘門（唔自建第二套dispute engine）**：只有所pin官方Woo Stripe Gateway經公開、可驗證結果提供account＋livemode＋原Charge／PaymentIntent chain、唯一dispute／review identity及provider state，HIBI先可投影。任何已驗證open dispute、open Radar review或early fraud warning只建立exact reason-scoped incident並關閉該單fulfilment gate：禁止`ready_to_ship`、建立／handoff ShipAny、轉`shipped|completed`及寄出／完成信；**唔撤銷已確認付款、唔自動退款、唔自動取消quantity或改庫存**。Provider已驗證`won|closed_no_loss|review_approved`時只resolve同一incident，再經完整payment／refund／stock／shipment reconciliation先恢復履約。只有最終`lost|funds_reversed`且可驗證final reversed minor units同成功退款後嘅唯一淨流出，先由§5.C同一canonical money-outflow projector重算VIP；未寄出訂單繼續停止履約，任何取消／restock仍須經既有受控退款／FulfilmentQuantityGuard／RestockProjector；已寄出訂單絕不自動restock、倒退shipment或再退款，只開營運incident處理。`UNIQUE(account_id,livemode,dispute_id,final_outcome_generation)`令重送／亂序no-op，charge-level cumulative outflow cap原capture，product-level outflow cap原商品實付，退款＋chargeback永遠唔雙扣。版本冇受支援open／resolved／final-outcome identity及net-outflow signal時，**自動履約保持launch blocker**；絕不可用早期event文字、order status、order note、private hook或timer猜結果。呢個compatibility contract同退款final-outcome一樣納入§25 spike及UAT。
- **商品qty取消**：只有上述RefundProjector確認provider succeeded後，未寄出item refund qty先減waiting，再減allocated，並由同一durable operation做一次Woo stock restore；釋放嘅已分配數量按同一FIFO分畀下一張waiting order。純amount-only goodwill refund唔改required／allocated／stock，訂單仍照原數量履約。 已付款單不可只靠手動改order status去`cancelled`／`refunded`當作金錢或數量真相；有金額訂單必須經Woo refund流程及成功provider outcome，並指定item qty。已寄出唔自動restock。任何直接status change同退款／qty證據不符都要阻止或轉`needs_attention`兼關ShipAny，唔可以寄退款信或釋放履約。
- **已寄出退款**：唔自動加庫存；實物真正退回、驗收可再售後，Admin先用正常「收貨 +N」入庫。
- **HK$0.00訂單**：零Stripe object，但仍用標準 Woo order lifecycle；單一 zero-total completion writer 只喺 HIBI scope marker存在、order total精確為HK$0.00、order items／totals已凍結及 Woo標準zero-total checkout成功路徑完成後，先用 Woo CRUD add-only 寫入§5所定嘅時間／source／schema marker。`zero_payment_confirmed:{order_id}`去重，double-submit、重送或crash recovery只可保留首次值；`PaymentEvidenceService`核對完整 marker 後先回`confirmed_zero`。`processing`／`completed` status、`needs_payment()===false`或Admin手動改status單獨都唔足夠，亦不可製造 marker。只有呢個semantic transition先建立demand及寄確認信。取消未寄商品仍建立標準 Woo refund／cancellation record，指定item quantities並用`refund_payment=false`；通過同一FulfilmentQuantityGuard後，只由上述RestockProjector做一次Woo stock restore、demand算術及projection，唔另造本地取消engine或假金錢refund。
- **HK$0.00履約調整唯一writer**：上述零額取消及外部退款分類所需嘅本地adjustment全部經同一service；首次建立 `WC_Order_Refund` 時即用Woo CRUD保存 `_hibilab_refund_kind=zero_fulfilment_adjustment|external_fulfilment_adjustment`、不可覆寫operation UUID、精確item quantity map＋hash、expected stock-owner map及schema generation。Order-scoped lock＋semantic key（order／kind／operation UUID／quantity generation）保證重送只讀同一refund；reconciler由呢份immutable evidence判斷係續projection、補一次仍未做嘅restock，定係證據不明轉`needs_attention`，永不另建第二筆或盲目再restock。呢類row永遠唔入money totals、VIP delta、provider normalizer或客人退款email。
- **Stripe Dashboard外部退款復原（2026-07-27 客戶批准；正常入口仍只係Woo）**：直接喺Stripe Dashboard退款屬例外復原。所pin官方gateway偵測到provider已`Succeeded`嘅外部退款後，必須以公開hooks／order data將**精確Stripe refund ID**配對到官方gateway匯入嘅**精確Woo money-refund ID**，並逐項核對歷史Stripe `account_id`、`livemode`、Woo order、currency、amount，以及該refund所屬Charge／PaymentIntent確實係原訂單凍結嘅transaction chain；復原claim嘅唯一身份至少包含account＋livemode＋Stripe refund ID，唔准跨帳戶／模式撞ID，亦唔准按金額、時間、order note文字或相近資料猜。公開contract無法證實精確配對就保持reason-scoped `needs_attention`兼關ShipAny，列為staging launch blocker，唔開自助操作。
- **最小Admin分類面板／多次部分退款**：配對成功後，Woo order內只用原生notice顯示同一個最小面板，列出尚未分類嘅external refund；Admin可揀一筆或多筆一齊處理，仍只得兩個業務意圖：**「照常出貨（只退款金額）」**，將所選金額永久歸類amount-only並保留全部未履行quantity；或**「取消未寄商品」**，揀精確尚未寄出、亦未被任何Woo refund／adjustment取消嘅item quantity。取消時以所選、尚未分配嘅provider-succeeded refund balance作資金池，按provider created-at＋refund ID穩定次序分配；總額必須不少於所選quantity嘅canonical remaining paid merchandise value，否則拒絕，超出部分自動歸類amount-only。每條order item只讀付款時immutable net `line_total` minor units同original quantity `Q`；已成功、帶item quantity嘅Woo refund按created-at＋refund ID穩定次序累積已取消數量`c`，下一批取消`k`件嘅唯一所需值固定係`floor(line_total × (c+k) / Q) − floor(line_total × c / Q)`，最後一件自然承接所有毫子餘數，所有批次合計必須精確等於原line total。Coupon／會員價已反映喺line total；唔讀live price。既有item refund金額／qty同呢個allocation矛盾就轉`needs_attention`，唔估。正常Woo refund已帶item quantity就由Woo原生流程擁有該quantity，面板不可再次選取或restock。未分類退款可等下一筆外部退款到達後一併處理；已完成分類不可任意改寫或重分配。
- **履約調整／crash-safe復原**：`hibilab_refund_operation`係唯一workflow、`active_order_id`、quantity map、local adjustment ID、provider/projection states及restock checkpoint owner。`hibilab_external_refund_recovery`只係immutable provider identity／classification mapping：每個Stripe refund一row並`UNIQUE(stripe_account_id,livemode,stripe_refund_id)`，canonical semantic key固定為`external_refund_classification:{stripe_account_id}:{livemode}:{stripe_refund_id}`；另存`operation_uuid` foreign key、batch UUID、imported Woo money-refund ID UNIQUE、external／allocated amount minor units、classification kind、actor及timestamps；**冇第二個active slot、state、lease、adjustment ID或restock map**。短transaction先由canonical refund operation claim order slot，再凍結所選identity rows同payload；離開transaction用Woo CRUD建立一筆`refund_payment=false`、HK$0.00 local line-item adjustment，首次save同時帶immutable kind、operation UUID、batch UUID及精確external refund IDs。所pinWoo path無法原子保存markers或公開gate封quantity race，就保持自助復原launch blocker。Worker永遠由operation row續同一adjustment、provider/projection state及RestockProjector checkpoints；retry、double-click、webhook重送或crash唔建第二筆。Ambiguous時projection保留`pending|applying`最後checkpoint、open exact incident並繼續佔active slot；`scentm_manager`只見需網站管理員核對，真正網站管理員先可喺nonce、resource check及operation version CAS下用已批准兩個受控選項確認stock已／未套用，填原因後仍續同一operation，唔清其他incident。全部VIP、demand、stock及ShipAny projection完成先由operation釋放active slot；mapping rows只記最終classification reference，gateway匯入money-refund row保持不變。
- **已出面單／全額amount-only安全閘**：有任何active／current shipment或已建立label時，「取消未寄商品」保持disabled；Admin必須先經受支援provider void流程，並由public truth確認已無active shipment，先可取消quantity。已真正handoff／寄出嘅quantity只可等實物退回驗收後用正常收貨流程入庫。外部全額amount-only退款可能令Woo core order變`refunded`但仍有履約責任；staging必須證實所pinShipAny整合可喺guarded HIBI入口按fulfilment truth安全出單，而唔係只睇core status。證實唔到就保持`needs_attention`兼禁止「照常出貨」承諾，唔私改gateway status。

#### Readiness與ShipAny

- **Demand completeness先於vacuous truth**：readiness唔可以只問「現有open rows是否全allocated」。每個`confirmed_paid|confirmed_zero` HIBI order嘅immutable preorder item都必須left-join到**恰好一筆**同order item／stock owner吻合嘅demand，而且`required_qty + succeeded_cancelled_preorder_qty = snapshot_preorder_qty`；缺row、重複、owner／hash／數量不符全部令ready=false，先排同一writer reconcile，仍不明就開incident。付款確認已落盤但demand upsert前crash，任何即時ShipAny入口都必須因此被擋住，唔可因「零open row」錯判全齊。

- **ready_to_ship係可重建projection**：`PaymentEvidenceService=confirmed_paid|confirmed_zero`、remaining physical qty > 0、每筆open preorder demand allocated_qty = required_qty、送貨資料完整、冇退款／付款結果核對中、冇任何active `needs_attention` reason、冇active shipment，先為true。混合現貨＋預訂全單等齊一次寄；Phase 1唔拆單。
- **履約投影**業務狀態只准`awaiting_stock|ready_to_ship|label_created|shipped|fulfilment_cancelled`；另有不可客面化嘅active operation states `creating|unknown`，保存operation UUID／obligation generation並一律令readiness及第二次label／quantity mutation fail closed。任何異常保留最後可證明業務狀態並開獨立incident，唔把`needs_attention`塞入business enum。Provider結果確定後projector先轉狀態；timeout／crash只由public-truth reconcile同一operation，唔因時間猜成功失敗。Woo payment/order status同履約投影係兩軸，唔因金額退款自動假設取消出貨。
- **ShipAny adapter**每次建立運單前live query上述predicate；label_created只代表已有面單，唔等於實際寄出。真正交物流先可進shipped及寄通知。
- **官方接口限制要誠實**：現行ShipAny plugin未證實有一個覆蓋single／bulk／AJAX／Portal嘅公開pre-create hook，provider-by-order-reference搜尋及void語義亦要staging實證。未取得受支援hook／vendor contract前，唔可以宣稱所有入口已結構封死；相關操作保持launch blocker，唔以隱藏按鈕或private callback頂替。
- **Admin介面保持簡單**：Products／variation提供「收貨 +N」；Orders列表只加履約狀態/filter及受控「建立ShipAny運單」「已交物流」bulk action；錯誤用Woo/System Status及WP notice，唔另起warehouse dashboard。

#### Durable jobs與可觀察性

- **一個OperationalIncidentRegistry擁有所有reason-scoped警告**：logical table保存`incident_key UNIQUE`、scope type／ID、typed reason code、`open|resolved`、severity、PII-free evidence／correlation reference、first_seen、last_seen、resolved_at及version；同reason重報只upsert同一key。Payment／refund／stock owner／demand／shipment／email／migration所有gate只查呢個service嘅open incidents，domain business state永遠唔借`needs_attention`欄承載多個原因。每個reconciler只可用自己exact incident key＋version CAS resolve，絕不可「清晒警告」；owner incident可由registry projection fan-out到相關orders，而唔複製可獨立漂移bool。既有Woo order／System Status／WP notices只係同一registry嘅read projection，唔新增第二個後台。
- **`needs_attention`只係incident projection，唔係任何domain state**：本文其他段落凡寫「標／轉／保持`needs_attention`」，一律解作喺上述registry建立或維持一個exact reason-scoped open incident，並由該incident關閉相應side-effect gate；原本payment／refund／claim／demand／shipment／email／migration row必須保留最後可證明嘅typed state同checkpoint。Incident自動或人手解決亦只可關閉同一reason key，唔可順手改business state、釋放claim、重試provider mutation或清除其他警告。

- **Action Scheduler係唯一執行queue，唔係業務真相**：VIP日核對、補購提醒、projection reconciliation及receipt allocation用grouped actions；handler用stable semantic key／UNIQUE row或order meta做到idempotent。電郵另以§22細型HIBI outbox row作canonical delivery truth，Action Scheduler只喚醒sender；唔再起generic command queue。
- **失敗唔靜默**：每類job有§25.2 `HibilabOperationalPolicy`固定嘅bounded exponential backoff、最大attempt、last error同next attempt；用盡attempt時保留canonical row最後可證明state，並由`OperationalIncidentRegistry`開同一個reason-scoped incident。System Status顯示oldest due age／失敗數，persistent WP notice通知營運。外部結果unknown唔當失敗重造object。
- **營運資料保留**、私隱export／erase、cron heartbeat、plugin migration／deactivation及provider webhook契約見§25；任何job失敗唔rollback已由Woo確認嘅付款，但會阻止依賴佢嘅ShipAny／電郵projection並大聲提示。

---

## 20. 前端狀態、互動、data-source 補充（交接必讀）

### 20.1 結帳付款狀態（前端已呈現）
- **表單驗證** → 原生 `required` / `checkValidity` / `reportValidity`（卡號、到期、CVC、地址、條款 checkbox、**（如車內有預訂項）預訂同意 checkbox「我明白並接受預訂商品約 1 週到貨，訂單將於預訂商品到貨後一併寄出。」（「約 1 週」＝到貨期欄 derive；多件唔同期＝「訂單內預訂商品之到貨期（見商品標示）」變體）§19**）。
- **付款前最終重驗沿用 Woo lifecycle**：預訂判斷以server端、quantity-aware嘅最新Woo product／variation stock及backorder設定為準，唔信產品badge、client price或舊cart cache。新Checkout每次提交都先由Woo驗證cart items、可購資格、庫存、shipping、coupon及總額；有變就用Woo notices／Order Review更新畫面並要求客重新確認，未通過前唔交gateway。通過後由Woo core負責hold／reservation／stock reduction／`_reduced_stock` bookkeeping，官方Stripe Gateway負責PaymentIntent／3DS／return／webhook；HIBI只保存§19.1最少化snapshot，唔停用原生hold、唔自己寫`wc_reserved_stock`、唔自行扣第二次庫存、唔另建付款attempt。既有`order-pay`沿用已保存order items／totals；只驗order仍可支付及履行，失效就取消舊單並要求重新checkout，唔重價舊單。
- **處理中** → 全屏 `#pay-overlay`（role=dialog aria-live）+ spinner，防重複提交。
- **失敗 / 重試** → `#pay-error`（role=alert）紅色 banner；demo 用測試卡 `4000 0000 0000 0002` 觸發拒絕，客可改卡重試。
- **成功** → 跳 Order Received。
- **交俾 WP / Stripe（前端唔自造）：** 3DS/SCA 驗證 = Stripe hosted，唔喺本站設計。Order Received／order-pay只投影§5 `PaymentEvidenceService` action matrix；Woo／gateway raw `pending|on-hold|failed`只係輸入證據，**不得直接揀客面狀態或Pay action**。`PaymentEvidenceService=pending|unknown`一律用中性確認畫面、移除success timeline及Pay／Retry CTA；只有service明確回`unpaid`，再同時證實冇success／in-flight證據、同一張order仍payable、hold／claim仍有效及全部server gates通過，先可用unpaid畫面顯示原生Woo Pay CTA並resume同一張order。已有`confirmed_paid|confirmed_zero`就跟付款／履約projection，唔被raw status倒退。**唔好自己寫 payment math。**

### 20.2 送貨方式呈現（ShipAny，可揀列表係預設）
- Checkout「送貨方式」= **radio 卡列表**：送上門／自取點（§7），每卡顯示**商家預設 per-method 費率**（§7，對齊 ShipAny 價目 — 唔係即時報價；≥免運門檻 → 顯示 免費 + 劃線原價）；揀自取點 → 展開選點器（production = plugin 自家彈窗，§7）。
- WP theme loop `WC()->shipping` packages/rates render，唔寫死；方式/運費變動經 `update_order_review` 即時反映落 summary 運費行。
- 「單一方法 → 陳述行」規則只喺 ShipAny 停用嘅 fallback 情境先適用。

### 20.3 庫存狀態
- 見 §19。PLP demo 示範 售罄 badge；WP 由 WC 庫存條件 render 三態（有貨／預訂／售罄）＋會員搶先 badge（§5）。

### 20.4 現有語意屬性（非本次驗收範圍）
- Repo現有 `role`／`aria-*`／ESC等屬性照原樣保留，避免WP遷移無故倒退既有互動；本次不新增無障礙需求、不以WCAG或鍵盤掃描作release gate，完整範圍見§24.A。

### 20.5 產品資料單一來源（重要）
- Demo 有兩份 product JSON：PLP 內嵌 `#plp-data` + `store-chrome.js` 內 `PRODUCTS_JSON`（俾非 PLP 頁嘅搜尋用）。
- **Production：** 兩者都由 **WooCommerce 產品資料** 取代（PLP query + overlay 自訂 REST endpoint，§22 搜尋契約）→ 單一來源，唔會有兩份 JSON。

### 20.6 帳戶地址模型
- Account 地址 = **帳單 + 送貨兩組**（對應 WooCommerce core billing/shipping，已移除誤導性「新增地址」無限地址簿 UI）。若客要多地址簿 = 需額外 plugin / custom，現階段唔做。

### 20.7 「使用示範」YouTube block 行為
- PDP / ACF `使用示範` block = 16:9 自動封面 + 原生播放 `<button aria-label="播放使用示範影片">`；**唔設、唔推算、唔顯示片長**。桌面點擊開 **lightbox**（居中 modal + dim 背景 + ESC／點背景關，沿用 store-chrome dialog interaction pattern）；手機全寬 inline 展開播放。視覺沿用現稿，底部 caption 只寫「產品使用示範」。
- **Admin 來源（單一欄位）：** Flexible Content `video` layout 只提供一個 ACF URL／oEmbed 欄「YouTube 影片連結」。Admin 貼正常 `youtube.com`／`youtu.be` 連結即可；save-time 驗證 host 並正規化 video ID，空值或無效連結時整個 block 唔 render。唔提供 MP4/WebM 上載、Vimeo、poster 或片長欄，避免多套媒體狀態。儲存時由server按video ID下載官方thumbnail入Media Library並保存attachment ID；前台只載本站poster，點擊後先 lazy-load `youtube-nocookie.com` embed；瀏覽器喺點擊前不可請求YouTube／Google第三方資源。thumbnail下載失敗時block唔輸出並喺後台顯示可重試notice，唔新增poster欄；唔接受任意iframe HTML。

### 20.8 PDP 條件狀態（WP 按 WC 庫存 render）
- **變體供應狀態（邏輯已鎖定；沿用既有 option-box 視覺，唔宣稱已有獨立 full-page 樣本）：** variation 無庫存且**不接受 backorder** → `<input disabled>` + `.box{opacity:.45;line-through}` + 「· 缺貨」標；variation 無庫存但**接受 backorder** → 保持可選並顯示「預訂 · {到貨期}」，唔可以錯誤 disabled。父產品 filter terms 照 §3/§23.2#8，供應狀態按實際 `stock_owner_id`／variation 設定計。
- **整件售罄：** gallery 左上 `售罄` badge（深啡 `#3B261D` 底淺字，同 PLP `.stk-out`）+ 主圖 `opacity:.6`；加入購物車掣 → disabled 灰態 `background:#cbb8a6;cursor:not-allowed`，文案「已售罄」。WC `!is_in_stock()` 且不開放 backorder 觸發。（**唔加「補貨通知」訂閱 input**。）
- **整件／指定 variation 預訂：** 父產品共享庫存而整件缺貨＋開放 backorder → gallery `預訂` badge（`.stk-pre` 外框 terracotta）+ 到貨期行，全部有效 variation 照落單；variation-level stock 則只有該缺貨＋開放 backorder 組合係預訂，其他組合各自顯示有貨／售罄。加入購物車掣 → **「預訂」（仍 active、即刻全額付款）**；結帳必填同意 checkbox。到貨期＝per-product＋全局預設「約 1 週到貨」；樣本 _p27 係整件預訂視覺。
- **會員搶先鎖定：** tier-gated產品先做資格gate；非會員／未達級→出「會員搶先」badge、鎖定加入購物車並提示登入／升級。夠級會員通過資格後先按該variation庫存顯示有貨／預訂／售罄；唔可以因有搶先設定而遮住合資格會員嘅真正供應狀態。

### 20.9 圖片最佳化（deploy build-step）
- **13張build input有明確runtime owner：** committed`assets/image-build-manifest.json`逐張列明source、WebP output及`runtimeClass`，唔可以將全部硬編成theme URL。`cat-*`×4係product-category ACF term image嘅**可選seed attachment**；provisioning只可匯入Media Library再存attachment ID，runtime永遠讀Admin當刻揀選嘅term attachment並用`wp_get_attachment_image()`。`prod-*`×6係demo／初始商品嘅**可選Woo product／variation seed attachment**；如production唔匯入demo產品就完全唔seed，PLP／PDP／Cart／Checkout／search／order item永遠用Woo product／order-item image API，Admin日後換圖自然生效。只有`hero-product.webp`、`promise-candle.webp`、`by-scentm.webp`三張批准Landing品牌圖係真正static theme asset，隨theme release放`dist/images/`並經theme URI/helper引用。原始PNG只係authoring/build source，production template不得直接引用；manifest class同上述2+1 consumer contract不吻合即verify fail。`scentm-home.html`內嘅`.hibi-mark`只係Scent.M首頁整合參考，由Scent.M host theme擁有；HIBI bundle明確排除`.hibi-mark`同`hibi-lab-logo.png`相對路徑，host theme實作時必須用既有theme asset helper／`get_theme_file_uri()`解出實際logo URL，唔准由`dist/css/`相對尋址。可重現build固定用Node≥20.9.0、npm lockfile、Tailwind 3.4.19及Sharp 0.35.3：`npm ci`→`npm run build`→`npm run verify`。圖片verify核對source/output SHA-256、manifest、runtime class、尺寸、alpha、大小及Sharp deterministic重建，缺少／多餘／過期output一律fail；CSS build同時收集approved prototypes authored CSS＋Tailwind utilities＋store-chrome CSS，全部scope到HIBI root/page classes，並以byte-for-byte重建驗freshness，唔接受「build成功但漏自訂CSS」。`dist/`同`node_modules/`唔入Git；高清母檔保留。WP Dev將build輸出納入theme release artifact；CI／交付前必跑`npm audit`及verify。
- **日後 Media Library 上載：** Admin 只需照常上載 JPG／PNG，毋須自行轉檔、改副檔名或操作圖片 plugin；Theme 一律用 `wp_get_attachment_image()` 產生 `srcset`／`sizes`。Production custom domain 接上 [WP Engine Advanced Network／Global Edge Security 嘅 Automatic Image Optimization](https://wpengine.com/support/webp-image-optimization/) 後，由 edge 自動最佳化 cached images；只有 WP Engine 實際產生咗更細嘅 WebP 時，支援瀏覽器先會收到 WebP，否則收到經最佳化嘅原格式亦屬正確，唔可以用「每張圖必須變 WebP」做錯誤判定。**唔安裝 Imagify／EWWW。** Staging（包括未接 custom domain 嘅 `*.wpenginepowered.com`）launch gate 先驗新上載圖片有 responsive sizes、原圖 fallback 正常；production custom domain 再以 response headers／實際檔案大小驗證 edge optimization。若正式環境未能使用 Advanced Network／GES，Dev 必須先報告並取得批准，唔准靜靜加 plugin，亦唔准假稱 repo build-step 會處理日後 Media Library 上載。

---

## 21. 關鍵決策（客戶拍板；與 §§22–25 一併構成 normative contract）

**A. 商店結構 / 技術**
1. **URL 結構**：landing = `/hibi-lab/`；PLP = `/hibi-lab/shop/`；product permalink base = `/hibi-lab/shop/%product%`；分類 archive = `/hibi-lab/shop/{cat}/`（render 同一 PLP template + 對應 dept pill 預選；`?cat=` deep-link canonical 指向對應 archive）。cart / checkout / my-account 沿用 WC 預設 slug（journey 頁 noindex，§23.2#11；WPE cache exclusions 亦以預設 Woo 路徑最穩）。品牌內容 URL 全部歸一喺 `/hibi-lab/` cluster 下。
2. **Filter 引擎 = 全 custom in-theme**（無 plugin、無年費）：詳見 §4。
3. **分類入口動態化**：Landing 入口卡／Shop pills／橫幅由 top-level `product_cat` loop 出，內容 ACF term fields，現有 4 個做 seed。詳見 §3。
4. **404 = 按 URL 分流**：一個 `404.php`，`/hibi-lab/` 前綴（或 WC journey slug）→ HIBI 版；否則 Scent.M 版。見 §2 表。
5. **店內 menu 底部 link 改字改指向**：HIBI LAB 店頁嘅 fullscreen menu 底部「Explore Hibi Lab」→ **「HIBI LAB 全部商品」→ `/hibi-lab/shop/`**；brochure 頁維持「Explore Hibi Lab」→ Landing。
6. **Scent.M／HIBI LAB 共用後台契約（不可由接手 Dev 自行重設）**：
   - **資訊架構**：以整合當刻 Scent.M theme 嘅 `inc/admin-ux.php` 為單一來源；本 handoff 已覆核嘅 Local baseline 係乾淨 commit `5f38500cf6d6e12e429b8d78c76804784425e86f`（2026-07-25）。真正開始整合時，PR／handoff 必須再記低實際採用嘅 Scent.M commit；如已唔同，須重新核對 admin UX、capability、Subscribers、電郵及 webhook integration，唔可以假設 baseline 冇變。保留現有 `scentm_manager` 同「Scent.M 區 → 分隔線 → HIBI LAB／WooCommerce 區」次序。HIBI 功能放入分隔線以下既有 WooCommerce／Products／Analytics／Marketing 原生位置或其 submenu；Orders 繼續係 WooCommerce submenu。除非客戶另行批准，唔新增商店 top-level menu、獨立 Dashboard 或另一個管理角色。
   - **權限**：不可用隱藏 menu 當權限。menu visibility、page callback、POST／AJAX／REST action 各自做 capability＋resource authorization；沿用現有角色，新增功能只用 Woo 原生或範圍最窄嘅專用 capability，唔准為方便而開 `edit_users`、`list_users`、plugin／theme／core 管理權。已批准嘅 HIBI 日常操作 capability（包括 `hibilab_manage_vip_overrides`）只可由 Scent.M 整合層嘅 versioned role-capability migration 明確加俾現有 `scentm_manager`，migration 要可重入並逐項測試；plugin 一般 activation 不得重建角色、複製角色或順手擴權。任何俾 `scentm_manager` 見到嘅營運頁都必須真係可操作及可儲存；真正技術診斷仍用 Scent.M `scentm_is_site_admin()`，唔可以用 `manage_options` 代替。角色／capability matrix 必須納入 implementation PR 及 UAT。
   - **原生操作模式**：一般頁沿用`.wrap`、`nav-tab-wrapper`／`nav-tab`、`form-table`、`submit_button()`；列表沿用`widefat striped`、穩定排序、搜尋／filter／分頁及原生按鈕。產品可編輯內容沿用現有ACF tabs／repeater pattern；固定值用select。Theme只負責外觀及內容欄位。**Woo／官方Stripe Gateway／ShipAny各自繼續擁有原生order、price snapshot、stock、payment/refund及shipment真相；`hibilab-commerce`只擁有VIP／best-wins／免運政策、preorder demand／readiness投影、提醒、outbox同受控adapter/gate。**ACF／Theme不可成為交易真相或直接寫provider。唔造React SPA、自訂modal或第二套admin CSS；如有必要嘅admin CSS必須限於HIBI screen id。
   - **固定Admin位置及owner（唔新增top-level menu／Dashboard）**：產品、variation、`pdp_blocks`、預訂到貨期及會員搶先最低等級relation只放Woo **Products**既有產品編輯頁；分類banner／intro只放既有`product_cat`編輯頁。全店非機密HIBI設定集中喺既有WooCommerce top-level下面一個原生submenu **WooCommerce → HIBI LAB 設定**，用`.wrap`＋既有`nav-tab`／committed ACF option field groups：一般tab（免運門檻、預訂預設到貨期、只供Homepage使用嘅真實回饋repeater、HIBI WhatsApp號碼／預填文字）render／save要求`hibilab_manage_store_settings`；VIP tab（tier schema、會員價總開關）render／save要求`hibilab_manage_vip_settings`。Page callback只容許至少有其中一項capability嘅人進入，未授權tab連內容同POST action都不可見／不可用；唔用較闊cap代替，亦唔喺theme option、另一頁或產品meta複製。VIP手動指定沿用已批准 **WooCommerce → VIP 會員** submenu。HIBI電郵template／preview／log、寄送／通知／support地址沿用既有 **Scent.M 電郵**及Mail Center owner，唔另建HIBI Mail頁。Stripe、ShipAny credential及connection只留官方各自設定頁而且只畀真正網站管理員；`scentm_manager`不可見credential，但可操作以上所有獲授權、畫面有顯示嘅日常設定。呢個位置契約入menu/page/save/直達URL UAT。
   - **確認、狀態及錯誤**：單一非財務刪除可沿用一句短 browser `confirm()`；付款／退款／收貨／ShipAny 或要輸入資料嘅操作沿用 Workshop 現有 inline confirmation panel（有 `aria-expanded`／`aria-controls`、focus management、Escape、主／次按鈕），唔用 browser `prompt()`。Notice 用 WP 原生 `notice-success|warning|error`＋`scentm-notice`；營運文案只講發生咩事、資料有冇保留及下一步，raw error／技術資料只畀真正網站管理員。所有 mutation 一律 POST-only、action-specific nonce、capability、resource check、idempotency及 PRG redirect；bulk 要逐項結果＋清晰 summary，重按不可重做副作用。
   - **語言與密度**：HIBI 自訂 wp-admin 文字（欄位說明／提示／按鈕／確認／通知／店主內部警示電郵）用簡潔**港式書面語**；主選單沿用 Scent.M「English 中文」命名。第三方 WooCommerce／ShipAny 原生文字跟已安裝語言包，唔以 brittle 字串替換冒充全站翻譯。管理頁只顯示營運所需摘要，完整付款／技術詳情連去 WooCommerce／Stripe；唔堆開發者術語。
   - **i18n 交付契約（唔留畀 Dev 自選）**：ownership跟§1.1：`hibilab-commerce`擁有嘅Admin、policy、狀態、email同JS字串只用plugin folder／slug／text domain `hibilab-commerce`，translation path固定`/languages`；Scent.M theme擁有嘅HIBI template／chrome presentation字串繼續用現有theme text domain `scentm`及theme自己嘅`/languages` catalog，兩邊唔交叉、唔複製同一字串owner。Bundled runtime path必須明確註冊：plugin喺`init` priority 0呼叫`load_plugin_textdomain( 'hibilab-commerce', false, dirname( plugin_basename( HIBILAB_COMMERCE_FILE ) ) . '/languages' )`；theme喺`after_setup_theme`呼叫`load_theme_textdomain( 'scentm', get_template_directory() . '/languages' )`，並用WP 7.0.2 integration test證明冇too-early translation notice及PHP／JS catalog都命中。所有自訂PHP顯示字串以英文做gettext source，按context／plural使用`__()`、`_x()`、`_n()`、`esc_html__()`等WordPress API，而且text domain必須逐次寫相應literal；變數用numbered placeholder＋translator comment，輸出仍按context escape，禁止自行字串替換或把完整HTML放入一個translation。JS只用`@wordpress/i18n`，enqueue後plugin handle以`wp_set_script_translations( $handle, 'hibilab-commerce', HIBILAB_COMMERCE_PATH . 'languages' )`綁plugin catalog，theme handle則以literal`scentm`綁theme catalog，禁止另存一套硬編JS中文。Plugin repo必須commit`languages/hibilab-commerce.pot`、完整`hibilab-commerce-zh_HK.po`／`.mo`及由`wp i18n make-json languages --no-purge`產生嘅Jed JSON；Scent.M theme必須commit`languages/scentm.pot`、`languages/zh_HK.po`、runtime實際載入嘅`languages/zh_HK.mo`及Jed JSON（theme bundled MO只用locale filename，禁止誤用`scentm-zh_HK.mo`）。Plugin root固定執行`wp i18n make-pot . languages/hibilab-commerce.pot --domain=hibilab-commerce --exclude=vendor,node_modules,tests`，theme root固定執行`wp i18n make-pot . languages/scentm.pot --domain=scentm --exclude=vendor,node_modules,tests`；兩邊各自再執行`wp i18n make-mo languages`同`wp i18n make-json languages --no-purge`，全部係release build固定步驟。Phase 1環境驗收固定WordPress site／user locale=`zh_HK`：所有HIBI客面、Admin、email HTML／plain-text及JS字串必須由正確owner catalog命中已批核港式中文，POT同source、PO/MO/JSON同POT有任何漏字／fuzzy／stale entry即fail；切去非`zh_HK`只可正常顯示英文source fallback，唔影響第三方語言包。翻譯載入失敗只可開可見release／health incident，唔用散落硬編中文補位。
   - **客觀驗收**：以 `scentm_manager` 逐頁 UAT：應見頁面全部可開、可改、可儲存；Users／plugin／theme／core／技術診斷仍不可見亦不可用直達 URL 越權。另驗 menu 次序、hidden routes、窄畫面、table pagination／empty state、CSV（全結果、UTF-8 BOM、formula-injection neutralization）、partial bulk failure 同 notice lifecycle；未通過唔可交收。

   - **固定 capability matrix（normative；menu、page callback、mutation、resource authorization 四層全部跟同一表）**：

     | 功能 | 頁面／列表 capability | Mutation capability | 必須再驗嘅 resource |
     |---|---|---|---|
     | 產品、分類、ACF 商品內容 | Woo `edit_products`；新增／發佈另跟 `publish_products`、`manage_product_terms`、`upload_files` | 同左，唔另造 HIBI capability | `current_user_can('edit_product', $product_id)` |
     | 訂單列表／詳情／一般營運狀態 | Woo `edit_shop_orders` | Woo `edit_shop_orders` | `current_user_can('edit_shop_order', $order_id)`＋HIBI order scope |
     | 金額退款／商品取消 | Woo `edit_shop_orders` | Woo 原生 order refund authorization（唔另造只會隱藏UI嘅capability） | `current_user_can('edit_shop_order', $order_id)`＋Woo可退餘額／商品數量＋HIBI order scope |
     | 收貨 `+N`／一般庫存更正 | `edit_products` | `hibilab_receive_inventory` | `edit_product`＋resolved stock owner＋open-demand invariant；外部退款crash後真正ambiguous嘅restock復原另只限`scentm_is_site_admin()` |
     | ShipAny 建單／void／已交物流 | `edit_shop_orders` | `hibilab_manage_shipments` | `edit_shop_order`＋shipment identity／readiness |
     | VIP 等級設定 | `hibilab_manage_vip_settings` | `hibilab_manage_vip_settings` | tier-config generation／stale-write check |
     | HIBI 全店非機密設定（含Homepage真實回饋） | `hibilab_manage_store_settings` | `hibilab_manage_store_settings` | Settings generation／stale-write check；回饋row逐項sanitize及validate |
     | VIP 會員手動指定 | `hibilab_manage_vip_overrides` | `hibilab_manage_vip_overrides` | target-bound authorization；不可等於operator |
     | 會員生日更正（只限窄範圍例外） | `hibilab_manage_vip_overrides` | `hibilab_manage_vip_overrides` | 同一「VIP 會員」頁；target-bound且不可等於operator；server重讀舊值＋expected version，必填更正原因，audit保存old/new/actor/time，CAS拒絕stale form |
     | HIBI 電郵範本、preview、測試寄送、寄送記錄 | 既有 `scentm_manage_payments` | 既有 `scentm_manage_payments` | template allowlist；測試信只寄operator明確輸入地址 |
     | Subscribers 名單／CSV／移除 | 既有 `scentm_manage_subscribers` | 既有 `scentm_manage_subscribers` | 沿用Scent.M canonical subscriber service |
     | Woo／HIBI 報表及 CSV | `view_woocommerce_reports` | 無；唯讀 | 日期、HIBI scope、CSV PII authorization |
     | Action Scheduler／outbox 人手重試 | `hibilab_manage_operations` | `hibilab_manage_operations` | 對應order／product capability；不可改provider truth |
     | Schema migration、credential、plugin/theme/core、raw log | 真正網站管理員 `scentm_is_site_admin()` | 同左 | `scentm_manager`永遠不可見／不可用直達URL進入 |

     **角色寫入單一owner**：正式整合前，Scent.M `scentm_register_client_role()` 必須由現時 `remove_role()`／`add_role()` 重建改成canonical additive reconciler；role只喺首次完全不存在時建立，之後永不刪除／重建，亦永不再複製administrator整套capabilities。Committed manifest逐項列出Scent.M＋已批准HIBI owned capabilities；reconciler只補回缺少嘅manifest grant，移除capability只可由另一次明確、versioned deprecation migration處理，任何非manifest第三方capability一律保留。`administrator`同既有`scentm_manager`由上文唯一`hibilab_site_guard`（`active_kind=role_reconcile`）序列化更新，全部read-back正確先推進單一version marker。`hibilab-commerce` activation／deactivation唔另設第二個role healer，亦唔移除role、user assignment或已批准capabilities。每個page callback、POST、AJAX、REST、CSV及bulk action都要有allow／deny integration test。 UAT要覆蓋Scent.M role version bump不丟HIBI caps、HIBI先／後activate結果相同、deactivate／reactivate assignment不變、第三方cap保留、連續init零額外write，以及中途write／marker fail可重試。

**B. 會員 / VIP**
7. **VIP 會員等級 = 做真、完全後台可設定（簡化版・已對抗式 review：logic＋money-safety＋copy）**：任意等級數（demo 一般／銀／金＝seed）。每級一個**全單會員折 %**（自動會員價 price filter、非 coupon、never-store；全店 HKD、canonical decimal calculation、客面固定兩位小數）＋主色一隻（系統自動搭「深底→主色」漸變卡）＋**生日月折扣 %**（自動加成、無 code／無錢包）＋無限「其他會籍禮遇」自由文字（**會員搶先＝喺產品度 gate：勾「限會員」＋揀最低等級、非 per-tier 欄位，見 §5.D／§20.8**）。`hibilab-commerce` 擁有及驗證 tier schema；ACF repeater 只係 Admin 編輯介面，自由增／刪，儲存後由 plugin 按門檻自動排序、無拖拉次序；全域只設總開關（OFF＝全站原價）＋WooCommerce「VIP 會員」submenu 手動 override（`scentm_manager` 用窄專用 capability 搜尋會員後 pin 指定級，勝過自動；可留空到期日＝直至 Admin 清除，或香港日期當日完結後自動恢復 rolling 等級；不開 Users 權限）。**等級永不儲、每次即場用香港時區向前 12 個曆月商品淨消費算**：只計付款成功時間落喺窗口內嘅折扣後商品實付額，扣成功商品／未歸類退款，排除運費、稅及 fee；成功付款達標即升，成功退款或舊單跌出窗口即降；每日 Action Scheduler 核對一次；**冇會員週年結算、冇 cycle floor、冇保級期**。**guest 唔累積**（只計註冊後嘅單）。基本級（HK$0.00／0%）不可刪不可停用。demo 值（HK$0.00／HK$2,000.00／HK$5,000.00、10%／15%）可改。詳見 §5／§9，build guard §22。**唔做積分／優惠券錢包／軟著陸／generic 折扣引擎**。
8. **會員價 × 優惠碼 = 二選一取最抵（全單口徑）**：公式、兩條quote、rounding、winner／tie predicate、重算時點及HK$0.00結果**只以§6為準**，本節不重述第二份規則。客面**禁「保留／暫停／留」**字眼（§6／§22 文案）。
9. **優惠碼 = 一次一張**：全部碼 `individual_use`；**冇免運碼／冇並存例外**（免運＝全店門檻 §7，唔係coupon類型、亦唔接受另一個免運碼；實際資格只由§7 canonical threshold predicate決定）；已套用狀態要有「移除／更改」affordance。詳見 §6。
10. **Guest 售後閉環（2026-07-27 客戶裁決 A）**：Guest checkout 保持開啟，classic checkout 顯示 WooCommerce 原生、預設不剔嘅「建立帳戶」checkbox；不剔仍可正常以 Guest 結帳，剔選就由 Woo 建立帳戶並寄原生設定密碼連結，唔自製密碼或 token。Guest 訂單以 Woo 官方 order key 提供安全 view-only 訂單連結；確認 email 有一個「查看訂單」CTA。付款後頁唔再提供「建立帳戶以追蹤訂單」，亦唔承諾之後註冊會自動認領舊 Guest 單。Guest 舊單不補計 VIP。

**C. 商品 / 訂單新功能**
11. **預訂 = 第三個庫存狀態（2026-07-10 客面＋2026-07-22 履約最終鎖定）**：缺貨產品／variation 可作「預訂」落單，客即刻俾錢（WC Stripe，charge-now 全額，無扣款 cycle）。到貨期＝**per-product 欄位＋全局預設（出廠「約 1 週到貨」）**；前台三態＋`.stk-pre` 外框 terracotta badge＋到貨期行＋PDP「預訂」掣＋結帳**必填**同意 checkbox（§20.1 新句，入 place-order 驗證）。Cart、側滑購物車、Checkout、Order Received、Account 訂單詳情及 email 全部沿用現有商品行，唔新增 panel／頁面：有預訂量先加一行細字；全預訂＝「預訂 {preorder_qty} · {到貨期}」，同一 line 部分現貨＝「現貨 {in_stock_qty} · 預訂 {preorder_qty} · {到貨期}」。付款前 Cart／drawer／Checkout 用 server 最新值重算嘅 `provisional_in_stock_qty`／`provisional_preorder_qty`；Woo建立order item時凍結 `snapshot_in_stock_qty`／`snapshot_preorder_qty`／到貨期／同意版本，付款成功後 Order Received／Account／email只讀呢份order-item快照。內部履約 `allocated_qty`只用嚟判斷配貨，收貨或重算後**永遠唔可以倒改客人已付款時見到嘅現貨／預訂文字**。Shipping 政策亦標示預訂規則。**混合購物車＝允許，等齊貨一併寄**；Admin 唯一日常操作＝產品／variation「收貨 `+N`」，系統更新 Woo 唯一庫存並按固定 FIFO 自動分配，唔逐張 Mark ready；全單所有有效數量已分配先可出貨（§19.1）。**無接單上限**（商家熄 backorder 手動停）。樣本 _p27/_p28；以上只補資料行，外觀沿用 _p28 ④，未批准任何全頁重畫。詳見 §19/§20.8。
12. **補購提醒 opt-in（2026-07-11 定位改為 PDP 購買區 — Amazon 式，同 Subscribe & Save／Chewy autoship 同一擺位邏輯：決策喺產品 context、checkout 保持最少摩擦）**：PDP 加購掣下 soft panel opt-in「到期提醒我補購」＋**排程完全自由（Amazon 式，2026-07-10 客戶裁定）**：模式＋數值兩個控制 — **每隔 N 日（1–90）／每隔 N 週（1–12，釘住星期幾）／每隔 N 月（1–6，真曆月行進、日子不變 — 非 30 日近似）／每月固定日子（1–31，短月自動用該月最後一日）**；預帶＝產品建議值（每隔 N 週）；**只限登入會員＋產品設有建議間隔＋可購買（有貨/預訂）先 render（guest／唔適用產品＝成個 panel 唔出）；揀咗＝cart item meta → 只喺 `PaymentEvidenceService` 首次 `confirmed_paid|confirmed_zero` transition 建 schedule；checkout 唔再問**；**購買即重錨（下次提醒＝最近一次購買日＋排程間隔 — 防 anchor drift；只適用 interval 模式，「每月固定日子」＝釘死日曆、本質無 drift、唔重錨）**；**循環直至客暫停／取消（end-user 自主，無系統自動熄火 — 2026-07-10 裁定）**。所有模式都以§25 canonical reminder row計`next_due_at_utc`，Action Scheduler只排帶generation嘅**one-off wake-up**；成功落盤email後再由同一service計下一個真曆日，日／週亦唔用永久recurring action做第二份排程真相。月模式＝今次＋N個曆月、日子不變（短月用月尾），**唔准用30×N日近似**；每月固定日子同樣逐次排下一個曆日。一鍵再買連結；**絕無自動扣錢／訂閱**；**逐件獨立**（各自schedule各自email，零合併邏輯）。Account新增nav項「補購提醒」面板（列表第3位／共6項）（**改週期／暫停／恢復／取消**四動作；「已暫停」態＝設定保留，恢復＝由恢復日重新開錶；demo有齊活躍＋已暫停兩態）。最終送達 `completed`、browser return、Admin改status均不可重錨；checkpointed truth reconciler按immutable opt-in order-item snapshot補回transition落盤後、reminder upsert前crash，仍沿用同一per-product UNIQUE row及首次確認anchor。樣本：PDP購買區panel（Product demo＋_p27）＋Account面板＋_p24⑥。詳見§9/§22。
13. **ShipAny Phase 1（目標行為；受compatibility gate約束）**：送上門＋自取點；運費行 ShipAny 體系（checkout 顯示商家預設 per-method 費率，官方冇 live-quote）；免運門檻機制保留、金額 admin 後台自訂（demo HK$500.00 只係示範，設空/0=停用）；訂單狀態 4 步（已確認→處理中→已寄出→已完成），另有 §19.1 履約投影。Processing 自動出單關閉；HIBI wrapper只有 server-side gate確認`PaymentEvidenceService=confirmed_paid|confirmed_zero`、fulfilment未取消、全單已分配、送貨資料有效、冇active `needs_attention` reason及冇active shipment先可建運單。客面「已確認」由同一付款證據驅動，「處理中」由全單ready projection驅動；Woo `processing`本身唔直接推客面第二步。`label_created`只記錄面單／追蹤資料，**唔轉`wc-shipped`、唔寄出通知**；實際交物流後bulk「已交物流」先轉已寄出＋寄email；最終完成只跟§7 **exactly-one、whole-order-covered、provider-final-delivered** `ShipmentEvidenceService`。呢個gate只保護HIBI自己擁有嘅入口；ShipAny native／bulk／Portal入口能否同樣受控及void能力必須先完成§7 staging spike，未證實不可宣稱已封死。詳見§7／§19.1。
14. **送貨地區 = 4 個**（香港島／九龍／新界／離島，`woocommerce_states` 補 離島）；運費行 ShipAny 商家預設 per-method 費率（§7）。Account demo 嘅 3 區下拉以 4 區為準。

**D. 分析 / 隱私**
15. **GA4 分析 ＋ cookie 同意**：裝一個免費官方 plugin「Google Analytics for WooCommerce」行 GA4；前台加**全站 cookie 同意橫額，硬性 gate（未同意就唔追蹤）**。Consent預設denied；同意前GA／Ads script同事件完全唔載入／唔發送，同意後透過WordPress Consent API（implementation dependency manifest必須列明）更新狀態、更新Google Consent Mode並只載入一份tracker，拒絕就維持denied；Consent API缺失／未初始化時analytics fail closed（零GA request），但Cart／Checkout／付款照常。GA4事件（view_item/add_to_cart/begin_checkout/purchase）只喺同意後fire。Staging以Tag Assistant＋browser network log驗「未選／拒絕＝零GA request；接受＝每個事件一次；改選擇即生效」，亦驗冇theme/plugin重複tracking。分析主力＝WooCommerce內建Analytics＋已批准唯讀「購買洞察」後台報表（時段熱力圖／星期分佈／地區rollup／回購週期／新舊客總覽）；優先經Woo Analytics／order data-store公開query API，沿用Woo對included statuses、refunds、net sales及date range嘅定義。真係冇public API嘅單一read-only adapter先可喺manifest pin Woo版本後查lookup tables，並以HPOS on/off及Woo升級contract test保護，唔起第二套報表真相；時段用local時間。GA4補結帳漏斗流失等行為數據。**購物車停留時間（cart dwell）任何免費方法都攞唔到，明確唔做。Cookie橫額（2026-07-10鎖定）：底部細橫條、全站（HIBI＋Scent.M同一consent）、「接受」／「拒絕」平權、記住選擇（first-party cookie約12個月）、唔揀＝照瀏覽零追蹤；文案＝港式書面中文（客戶裁定例外）：「本網站使用 Cookies 分析網站使用情況，以改善購物體驗。」＋「私隱政策」link。樣本_p30；批准production class固定為`.scentm-consent-banner`／`.scentm-consent-accept`／`.scentm-consent-decline`，`scripts/prototype-css.js`只抽取呢組namespaced selector作全站共用CSS，helper嘅`.stage`／`.wrap`絕不入bundle，`verify-build.js`必須鎖定三個selector。** Custom橫額係唯一consent controller，WordPress Consent API只做狀態通訊；first-party cookie只存`{policy_version,choice,decided_at}`，Path=/、Secure、SameSite=Lax、無PII。用途／政策版本提升先重新提示；同意前繼續零Google request。
16. **Checkout 優惠訊息 opt-in**：保留，**預設不剔**（PDPO 正路）。剔咗先喺訂單保存 exact consent evidence（boolean、label、時間、來源），並於 order commit 後排一個 idempotent Action Scheduler job；worker只可呼叫現有Scent.M public service `scentm_record_subscriber()`，寫入canonical `$wpdb->prefix . 'scentm_subscribers'` custom table，從而同Scent.M聯絡表共用「Subscribers 訂閱」後台名單＋CSV export。**唔准寫已退休 `scentm_subscriber` CPT、另造user meta／第二張subscriber table，或直接SQL繞過service。** 未剔只代表今張單冇新consent，唔可以刪除或改動客人既有訂閱。Subscriber寫入失敗唔可以令付款／落單失敗；job以order ID＋consent version做semantic key安全重試，超限轉可見`needs_attention`，唔准靜靜遺失opt-in。**Label只可以係marketing**（「接收優惠及新品消息」）— 交易email一定發，唔可以出現喺opt-out選項度。 為防「客先退訂、延遲opt-in job後到」重新訂閱，canonical subscriber service必須保存email-scoped、versioned suppression／revocation generation（唔係第二份名單）；job帶明確consent event時間／version並喺同一email serialization下重讀，只有新過最近revocation嘅再次明示同意先可恢復，舊／並發job一律no-op。UAT覆蓋queued opt-in→unsubscribe→delayed worker、同時提交及backup restore後suppression重播。
- **Canonical consent／revocation service**：HIBI唔另造subscriber store。Scent.M既有subscriber module擴充同一public service boundary及一張最小revocation tombstone：每個normalized email一row、UNIQUE identity、generation、latest explicit-consent time、latest revocation time、last semantic event key及timestamps；只為履行退訂，唔當active subscriber或export marketing list。為確保日後匯入、結帳opt-in及restore都可可靠命中同一退訂者，tombstone可保留**normalized email本身作唯一必要suppression識別資料**；唔保存姓名、電話、地址、訊息、訂單內容或marketing profile，唔出現喺Subscribers名單／CSV／一般搜尋，只畀canonical service及真正網站管理員按最小權限讀取，並喺Privacy policy列明用途及保存準則。`scentm_record_subscriber()`、Forminator ingestion、HIBI checkout opt-in、可見退訂、one-click退訂及後台移除全部經同一service＋email-scoped lock；同時間衝突由revocation勝，只有event time嚴格新過最後revocation嘅日後明示consent先可重新加入。HIBI job只帶order ID、consent version／event time及semantic key；worker重讀immutable order consent evidence再呼叫service。另有checkpointed reconciler掃描「consent evidence已保存但未有applied／terminal semantic result」嘅order，補回order commit後、enqueue前crash；舊generation、重送及restore replay一律no-op。`scentm_subscriber_erase()`成為service內部原子步驟，刪canonical／legacy／ingestion PII後仍只保留上述履行退訂所需最少tombstone；當suppression用途不再存在或有較新明示同意取代時，按同一service嘅versioned lifecycle更新／清理，唔由散落job自行刪除。

**E. 電郵 / 支付 / 支援 / 政策**
17. **Woo 交易 email = WP dev 按已批准樣本實作**，唔准重新設計；跟 Scent.M 現有收據範本 pattern：品牌 wrapper + **後台可編輯範本**（option 儲存、placeholder、live preview、測試寄送）；訂單編號用 `HL-` 顯示格式。六款變體，詳見 §22。
18. **Stripe environment／webhook ownership＝上線硬閘**：共用同一Stripe帳戶只代表統一對數，Workshop同Woo仍係兩個owner namespace。Workshop物件固定metadata owner marker＋`workshop_id`，Workshop endpoint只處理自己namespace，其餘已驗簽事件2xx略過；HIBI如所pin gateway提供公開、受支援metadata filter，可選擇性加入`hibilab-commerce-v1`＋Woo order reference作觀察／對數；gateway唔支援就省略，絕不可用private hook，亦不可將metadata當付款正確性前提。Woo訂單嘅PaymentIntent／Refund、payload parsing、Stripe API version及webhook endpoint由**官方 WooCommerce Stripe Gateway唯一擁有**；Woo用獨立endpoint＋signing secret，`hibilab-commerce`只消費經staging確認嘅公開gateway hooks及Woo order／refund結果，唔另開第二個Stripe writer、唔硬編payload shape。Test／live key、account ID、endpoint及secret完全分開，冇cross-mode fallback。HIBI Phase 1係即時全額收款，Woo Stripe「Issue an authorization on checkout, and capture later／先授權後收款」必須保持**關閉**；呢個設定漂移時，HIBI checkout、order-pay同付款確認一律server-side fail closed，避免`requires_capture`訂單繞過已批准嘅confirmation／refund契約。完整version、dedupe及secrets契約見§25。
19. **支援渠道 = WhatsApp ＋ 後台自訂 email**：政策頁支援渠道 = WA 浮動掣 ＋ 支援 email（ACF option `hibilab_support_email`，mailto，hide-if-empty）；**唔用**「前往聯絡我們」指向（B2B contact form 唔做 B2C 支援路徑）。兩渠道均入 launch gate（§22）。
20. **瑕疵退換期 = 7 日**（全部統一）：Shipping 頁「不適用」段補明「商品本身瑕疵或運送損壞：收貨後 7 天內聯絡我哋」；Scent.M `page-terms.php` §5 fallback 7 天（如客已喺 ACF 儲過舊值，launch 前後台同步改）。**Terms 無 FPS／銀行轉帳句**（無業務線收 FPS）。
21. **取消訂單政策**（客戶指定文案）：Shipping 頁退換 section —「訂單確認後如需取消或修改，請於出貨前儘快透過 WhatsApp 聯絡我哋。我哋會盡力配合，惟不保證能夠成功取消或修改；如訂單已進入處理或包裝程序，或需收取相關額外費用。訂單一經寄出，恕無法取消，可於收貨後按退換政策處理。」確認 email 結尾加「如需取消或修改訂單，請於出貨前儘快聯絡我哋。」

**F. 其他**
22. **Footer「Shipping & Returns」連結 = 全站**（包括 Scent.M 頁 — 共用 chrome 變更）；footer「HIBI LAB →」指去 `/hibi-lab/`。
23. **真實回饋只屬Homepage**：§21.6固定 **WooCommerce → HIBI LAB 設定 → 一般** tab只保存一個 committed ACF repeater `hibilab_testimonials`，Admin可自由新增、刪除及拖拉排序。每row固定：`name` required純文字最多80 grapheme、`content` required純文字最多500 grapheme、`label` optional純文字最多80 grapheme；`label`只係Homepage卡片現有嘅產品／系列展示文字，唔係Woo product／category relation。星星固定5粒只係現有視覺樣式，唔提供可編rating。Homepage按repeater次序沿用現有testimonial card顯示全部有效row；repeater冇有效row就連同標題、carousel及dots整段隱藏。**PDP、Shop及任何分類archive一律唔讀、唔query、唔render真實回饋，亦唔建立任何產品／分類關聯欄位。** 唔新增filter、客面控制、產品meta或另一個回饋store；`pdp_blocks`繼續係PDP下半部唯一內容來源。
24. **產品目錄由空白入起**（唔 seed demo 產品）；入口 link 維持 coming-soon，直至客戶入好首批產品＋gateway 驗證通過。
25. **Scent.M 整合推遲**：先做好 HIBI LAB，再整合入 Scent.M；入口層（coming-soon 標示／第 4 nav 入口／首頁描述句）全部到整合階段先處理（§0／§22 flip checklist）。
26. **Wishlist／最近瀏覽 = 明確 deferred**（現目錄規模唔做；SKU 過 ~50 再評估）。

---

## 22. 技術補遺（實作規範，補齊各章未定細節）
- **Mail Center先落完整可重播receipt先ACK**：Resend public endpoint驗簽後、回2xx之前，site-wide canonical receipt必須保存`svix_id UNIQUE`、event type、RFC／provider message IDs、provider observed time、normalized delivery／reputation signal、owner namespace、`provider_profile_generation`、route state（`pending|routing|committed`）、attempt／next attempt、payload hash及timestamps；route無法收斂就保留最後checkpoint並開獨立incident，唔加`needs_attention` state。Raw body只留到normalize完成。Durable dispatcher重試route直至HIBI event inbox commit，同一svix-id全鏈idempotent；HIBI停用或route crash都唔丟bounce／complaint／suppression。Reactivation要先由captured Mail Center receipt high-water排清目標namespace，再准新send。每個HIBI outbox row亦必須有non-null `provider_profile_generation`，所有webhook／retrieve只可配對同一generation。
- **Missing-core sentinel只做maintenance deny gate**：`hibilab-commerce`停用／檔案缺失時佢自己唔可能再執行guard，所以Scent.M theme嘅integration bootstrap保留一個極細sentinel；只靠committed HIBI scope marker＋core readiness service存在與否，喺`is_purchasable`、add-to-cart、checkout、order-pay，以及HIBI refund／status／stock-owner／ShipAny／customer-email已知mutation boundary全部拒絕寫入，保留read-only order／product檢視。Sentinel唔計價、唔判付款／退款／庫存真相、唔做副作用；core正常ready且§25.1 `stripe_connection_generation` interlock明確為`ready`時完全pass-through，非HIBI Woo同Scent.M全部不受影響。**Stripe帳戶切換安全閘**：只要WordPress已成功載入theme bootstrap，sentinel就直接讀同一個持久、nonsecret site-wide interlock；phase唔係`ready`或interlock缺失／損壞／讀唔到時，即使HIBI core停用、缺檔或被Recovery Mode暫停，仍fail closed全站官方Woo Stripe checkout／order-pay／付款／退款及provider mutation，read-only Woo同獨立endpoint／credential嘅Scent.M workshop照常。Deactivate／uninstall不得刪呢個interlock。**任意PHP fatal可能早於theme bootstrap令整個request直接終止，sentinel無法宣稱會執行或提供read-only畫面**；呢種情況由外部health monitor、WP Engine fatal監察／Recovery Mode、rollback runbook及已落盤operation reconciliation處理，絕不寫成sentinel保證。UAT分開測①plugin deactivate／files missing／Recovery Mode但WP仍可載入，逐項驗deny matrix；②bootstrap前後注入fatal，驗request無後續provider mutation、外部monitor告警、復原後同一operation收斂且零重覆side effect。
- **Mail transport結果先正規化**：HIBI workflow永遠只接Mail Center typed result `accepted|retryable_failure|permanent_failure|unknown`，唔直接解讀SMTP／HTTP code。SMTP `4xx`＝retryable、`5xx`＝permanent；HTTPS adapter只有`429`＋`5xx`＝retryable，其餘`4xx`＝permanent；network timeout／斷線＝unknown。所有retry界線、alert及UAT按normalized result，禁止把HTTP taxonomy套落SMTP。
- **Resend credential profile continuity**：同一verified Resend account／domain內rotation先以新`send + reconciliation` pair一齊驗證，再原子啟用新`provider_profile_generation`；每個outbox row凍結nonsecret generation，舊read credential/profile保留至該generation所有`accepted|delivery_delayed|delivery_unknown` rows及其open incidents收斂或按retention結案。Accepted／unknown仍存在時唔准直接用另一帳戶key取代，否則會向錯帳戶查證或重寄。跨Resend account係maintenance migration：暫停新send、reconcile／封存舊profile未完row、驗證domain同新pair再切換；唔係日常key rotation。Secret仍只由Mail Center canonical owner保存，HIBI只存generation reference。
- **Email delivery同recipient reputation兩條軸**：Outbox send/delivery state仍按下文狀態機收斂；`delivered`後到達`email.complained`、late bounce或suppression唔可重開／重寄原信，而係另以append-only verified observation更新canonical address-level reputation／suppression projection並告警，阻止日後不合資格send。`delivered`只代表嗰封曾送達，唔代表收件人永遠healthy；客面／Admin兩者唔混為一個status。
- **Native auth mail由site-wide Mail Center擁有durable job**：Woo／WP新帳戶、忘記／重設密碼唔塞入HIBI order outbox；Mail Center建立最小auth-mail job，Action Scheduler args只放job ID，row綁user ID、purpose、reset-key generation／expiry、immutable recipient/template snapshot及semantic key。真正send前重讀最新未過期generation；舊／已用／已取代job no-op，terminal或expiry即清除token payload。Plugin停機、retry及reset兩次嘅UAT要證明舊link唔會遲到寄出，Scent.M既有auth觸發及品牌外觀不變。
- **Provider idempotency必須環境隔離**：Local semantic key保持業務唯一，但送Resend嘅idempotency key及RFC Message-ID要由`owner namespace + env_instance_id + semantic_key`導出（hash後符合provider長度），避免Local／staging／production或Scent.M／HIBI共用Resend帳戶時撞key。`env_instance_id`係環境binding資料，唔可只由可複製order ID或DB option決定：provisioning／release command按canonical hostname＋environment重新綁定，DB clone發現binding不同即fail closed並生成／保存目標環境新ID；同一環境restore保留原ID以維持去重。UAT以相同order ID喺staging／production證明不碰撞，同一production restore則仍dedupe。
- **SMTP Message-ID係compatibility launch gate**：Resend事件／retrieve支援回傳Message-ID，但唔假設任何SMTP stack一定原樣保留caller預設header。所pinWordPress／PHPMailer／Mail Center／Resend SMTP路徑要喺staging寄一封，證明預先保存嘅RFC Message-ID喺provider webhook同retrieve/list可exact match；做唔到就HIBI交易email保持launch blocked，或另開經批准嘅site-wide transport migration去有文件化correlation嘅渠道，唔用subject／recipient／時間猜配。
- **§19.1並發／取消UAT補充**：本節任何「成功qty取消先`cancelled`」只指該preorder demand嘅`required_qty`已減到0而且尚未final-delivered；一次或多次部分取消後`required_qty>0`必須仍為`open`。測試最少覆蓋同一item多次部分退款、同一行現貨＋預訂混合、waiting及allocated各自被取消、receipt後遲到舊demand、負數correction、payment-confirmed但demand-upsert前crash、delivery對撞refund／void、label-create對撞quantity refund／負數correction、Admin／REST／CSV absolute stock edit對撞checkout，以及地址／自取點更改對撞label create、label後void、更改及handoff後拒絕；每個barrier兩個interleaving同中途crash都要證明同一generation收斂、零重覆stock／shipment／email副作用。
- **Phase 1只售實物**：HIBI商品／variation必須`virtual=false`、`downloadable=false`；Admin save、REST及import都由same product-policy validator拒絕漂移，System Status列出違規商品並令佢不可購買。原因係本期已批准履約、退款及ShipAny模型全部係實物；Woo core喺refund建立時仍會撤銷download permissions並fire原生refund actions，HIBI只承諾自己嘅VIP／demand／客面／品牌email／ShipAny副作用等待provider成功，唔虛稱可阻止Woo所有內部hook。日後如賣數碼貨係另一個明示scope及UAT，唔靠現有實物流程猜。
- **Stripe capture-mode launch contract**：HIBI Phase 1只支援即時captured card payment；Woo Stripe「先授權後收款／manual capture」必須為OFF。Provisioning、System Status及每次checkout／order-pay／付款確認都read-only核對；漂移時只令HIBI payment mutation fail closed並顯示修正路徑，唔自動改Admin設定、唔影響Scent.M或非HIBI Woo訂單。Staging UAT先驗setting drift會喺call gateway前被拒絕，再用一筆captured card付款＋全額／部分退款證明operation UUID metadata及PaymentIntent／Charge／Refund chain完整round-trip；未通過不可上線。
- **HPOS＋Woo template相容**：啟用High-Performance Order Storage；`hibilab-commerce`所有order讀寫只經Woo CRUD／data stores，禁止直接查`posts`／`postmeta`，HPOS on/off integration tests通過後先用Woo `FeaturesUtil::declare_compatibility('custom_order_tables', …, true)`宣告。每個`woocommerce/` override必須由manifest所pin Woo版本嘅上游template起步，保留required hooks、nonces、input names及lifecycle actions（尤其`woocommerce_thankyou`）；每次Woo升級先diff upstream，Woo Status不可有outdated templates，staging驗Stripe／ShipAny／GA／Woo notices／auth hooks仍有觸發。Orders仍係`woocommerce`頂層選單submenu。
- **ACF schema-as-code（files-only deploy硬契約）**：所有field group key、field key、layout name及option key必須入Git，唔准只存在Local DB。Theme-owned內容／視覺欄（產品內容、`product_cat`、PDP Flexible Content、Testimonials、WhatsApp等）用Scent.M theme committed `acf-json/`並配置Local JSON load/save path；`hibilab-commerce`擁有嘅VIP／免運／營運設定由plugin PHP `acf_add_local_field_group()`註冊，再由plugin save callback做typed schema validation。ACF只係輸入介面，交易真相仍由application service擁有。Field key上線後不可重生；rename用兼容migration，唔可刪舊key再開新key。Files-only push後毋須喺後台按「同步」先可用；activation／Site Health驗證required group/key及ACF PRO最低版本。ACF缺失或schema不符時HIBI交易設定fail closed兼清楚告警，但不可影響Scent.M workshop既有欄位。
- **可重現dependency manifest**：implementation PR提交`docs/hibi-compatibility.md`（或等價machine-readable manifest），逐項記錄最低支援、staging實測、production安裝、owner、auto-update政策及最後UAT日期：WordPress、PHP、MySQL/MariaDB、WooCommerce、WooCommerce Stripe Gateway、ShipAny、ACF PRO、Action Scheduler、Google Analytics for WooCommerce、WordPress Consent API、WordPress timezone=`Asia/Hong_Kong`、Scent.M theme commit、`hibilab-commerce` version/schema version。Payment／refund／shipment adapter每項另要列出**逐名 public hook／API／Woo order datum、owner、ACK ownership、contract test及fail-closed條件**；目前 pin Woo Stripe付款證據明列公開`payment_complete()` lifecycle及其持久化`date_paid`＋transaction ID，唔准只寫「gateway verified」或依賴private meta。Scent.M source＝`https://github.com/xiNeRoar/scentm-wp.git`；本handoff已覆核baseline＝`5f38500cf6d6e12e429b8d78c76804784425e86f`，正式整合必須更新成實際commit。ShipAny以implementation當刻WordPress.org stable version做compatibility spike（本文件2026-07-26參考為1.1.105）；Action Scheduler只用Woo載入嘅單一runtime，唔另裝第二份。Stripe／ShipAny／Woo／ACF critical integration唔做未經staging嘅自動升級；每次先讀changelog、跑adapter contract＋payment/refund/shipment regression，再推production。
- **Plugin lifecycle／migration／rollback**：`hibilab-commerce`用單一code version＋schema version。Activation先驗Woo／HPOS／ACF／Action Scheduler、table engine、required public hooks及DB privilege；preflight fail不得半啟用。Migration只可versioned、idempotent、additive、可resume，並由上文唯一`hibilab_site_guard`（`active_kind=upgrade`）序列化及保存canonical checkpoint；schema／backfill完成後先更新version，失敗保留舊version並停止HIBI mutation，唔喺front-end request臨時補欄。每次schema deploy前建WP Engine manual backup checkpoint。舊code必須聲明可讀schema range；DB較新而code唔識就fail closed。可兼容additive release先准code-only rollback，否則還原同一checkpoint嘅code＋DB。Migration唔自動drop column/table；破壞性cleanup留下一個release週期兼另做已批准migration。
- **Deactivate／uninstall**：Deactivate不得刪table、order meta、reminders、canonical job／event row、role、audit或§25.1 `stripe_connection_generation` interlock；停止所有HIBI-owned workers／mutations，並由本節「Missing-core sentinel」作**唯一**maintenance deny matrix（購買、退款、status、stock、ShipAny、客信），唔再另列一套較窄四層規則。只取消§25.2 HIBI allowlist內可重建嘅Action Scheduler wake-up，絕不碰Woo／第三方action；正常Stripe connection phase為`ready`時，read-only Woo資料、Scent.M及非HIBI Woo照常；切換phase未`ready`或interlock不可驗證時，sentinel仍按上段封全站Woo Stripe mutation。重新啟用必須先完成§25.2高水位catch-up先ready。未知庫存不可扮售罄或照樣落單。Production package不註冊自動資料清除uninstall；永久移除只可由真正網站管理員先export＋backup再明確wipe，但有歷史付款責任或Stripe切換未完成時亦不得移除interlock。
- **Scent.M入口 flip checklist（§0／§21.25；只喺整合release執行）**：入口flip係一次release-level cutover，唔係各頁自行改link。前置條件固定為首批產品／分類已完成、gateway＋checkout＋shipping＋email UAT全綠、`/hibi-lab/`及PLP可公開、客戶已批准最終入口文案；未齊就保持現有coming-soon狀態。Cutover同一commit／deploy內一次過：駁通三個既有Scent.M inert stub去`/hibi-lab/`、按已批准Scent.M chrome加入手機／桌面共用`.hibi-entry`、移除相同三處coming-soon標示，並同步最終已批准首頁描述；Dev不可自行創作文案或逐處分批上線。驗收desktop／mobile四個入口、返回Scent.M、404／canonical／sitemap、匿名cache及無dead link；rollback只還原入口層同coming-soon狀態，唔倒改產品、訂單或HIBI schema。
- **WP Engine排程／監察／備份**：staging同production啟用WP Engine Alternate Cron，Action Scheduler唔依賴訪客流量，亦確認冇第二個外部cron重複觸發。Recurring dispatcher用`action_scheduler_ensure_recurring_actions`自我修復並更新heartbeat；所有時間／重試／batch數字只讀§25.2 `HibilabOperationalPolicy`。異常建立一個去重incident：WP Admin notice＋營運警示email，恢復後標resolved，唔反覆轟炸；Site Health顯示oldest due、failed count、last successful heartbeat及last provider error。備份契約固定為**每小時自動database backup（RPO≤1小時）**＋WP Engine每日完整backup，plugin／Woo／Stripe／ShipAny更新及schema migration前另建manual checkpoint；復原runbook目標**RTO≤4小時**，每季喺staging由真實backup做restore drill並保存開始／完成時間、核對結果及未達標改進。Local／staging／production credential及webhook secret各自獨立，database copy後重新綁目標環境secret，禁止fallback。
- **Stripe plugin**：官方 **WooCommerce Stripe Gateway**（`woocommerce-gateway-stripe`），實際版本必須鎖入compatibility manifest並先過§22／§25 staging付款及退款UAT，唔硬寫一個會過期嘅「最新版本」。Payment Element用appearance API對齊`.co-field` tokens —— **iframe內部無法逐pixel對齊demo輸入框，屬明示豁免**。**Apple Pay／Google Pay（express wallets）＝Phase 1唔開（2026-07-11客戶裁決）**：Woo gateway設定明確停用Express Checkout、Link／其他非卡付款方法、**saved payment methods／儲存信用卡**、**先授權後收款／manual capture**及Adaptive Pricing／任何會自行改顯示貨幣或銀碼嘅選項，再核對Stripe account端同樣只開批准嘅即時卡收款流程；唔可只靠Dashboard單邊設定。Phase 1唔顯示「儲存付款方式」控制，亦唔開Woo「付款方式」帳戶endpoint；provisioning、health check及UAT要驗上述設定保持OFF，漂移即只阻止HIBI付款mutation並顯示可操作告警，唔影響Scent.M或非HIBI Woo訂單。Checkout只出官方卡欄（demo視覺＝最終視覺）。Cart／Checkout頁一律轉**classic shortcode**（block版會繞過§2 template override）。 為免同HIBI品牌交易信重覆，Stripe Dashboard／gateway任何直接向客人發送付款收據或退款信嘅選項必須停用；staging以成功付款、全額／部分退款各驗客人只收到批准嗰一封，Stripe後台通知營運則不受影響。 **Woo Hold stock（2026-07-27 客戶裁決 A）＝60 分鐘**：只用 WooCommerce Inventory 原生 setting 管理未付款 `pending` 訂單嘅到期／釋放；`on-hold`、已付款、HK$0.00 confirmed 及 HIBI 履約 allocation 全部不受影響。唔加自訂 timer。Provisioning／health check 必須驗證值為 60，staging 用 59:xx、60+ 分鐘及付款／釋放 race 實測。
- **Checkout欄位對照**（`woocommerce_checkout_fields`）：電郵先行；客面單一「名字」欄位係`shipping_first_name`，server驗證後將同一值copy入`billing_first_name`作聯絡／付款snapshot，last name隱藏＋非必填；其餘地址及billing／shipping ownership完全依§20.6裁決2A，呢度唔另定第二套mapping；無postcode。**訂單備註＝保留 WC 原生 `order_comments`**（可選 textarea「訂單備註（可選）」，自動寫入訂單＋admin 訂單頁可見 — 2026-07-10 拍板：香港送貨常用「放管理處／送前致電」類指示欄）；**必填標示＝WC 原生 convention（2026-07-10 拍板）**：必填欄 label 尾紅 `*`（`abbr.required`、色 `#b4452a`、title「必填」），可選欄 label 尾「（可選）」（訂單備註／生日）；terms 同預訂同意 checkbox 一樣標 `*`；「變更密碼」三欄不標＋hint「不變更密碼可留空。」（WC 原生 leave-blank 行為）；**theme 保留 WC 原生 required 標示，不准剷**。
- **Coupon／VIP實作**：Admin save、REST及import三條路server-side強制所有coupon `individual_use=yes`，並拒絕／清除native`free_shipping=true`；runtime遇到不相容舊碼即fail closed並由System Status告警。Coupon、會員價、sale安全網、birthday boost、winner、rounding、100%及zero-total規則全部**只按§6唯一canonical quote公式／predicate**，本節唔再複寫。`woocommerce_after_calculate_totals`只做總和／winner assertion及必要notice，唔做主要價錢mutation；落單前以同一service重跑、保存winner及price snapshots，最後只由Woo order total交官方gateway收款（G2–G5）。
- **VIP 等級引擎**：`hibilab-commerce` 擁有 tier configuration schema、驗證、migration 同唯一 application service；§21.6固定 **WooCommerce → HIBI LAB 設定 → VIP** tab內ACF repeater「會員等級」只係 Admin 編輯／儲存介面，Theme 只消費 read-only projection。經 plugin 驗證後嘅 enabled 等級表先係 canonical configuration；兩個折扣欄嚴格按§5 `0.00–100.00`、最多兩位小數contract驗證。`resolve_tier()` **每次讀都用香港時區計算當刻向前 12 個曆月商品淨消費，對現行 enabled 等級表重新 bucket；自動計算結果永不認舊 stored tier id，唯一例外係 G10 明確而未到期嘅手動 override**（G1）；商品淨消費嚴格按 §5.C：以付款成功時間入窗、只計折扣後商品實付額、扣成功商品／未歸類退款、排除運費／稅／fee及其明確退款。會員價計算只引用§6；生日月加成／手動 override／總開關全部經同一 `computeTier()`／`resolve_member_discount()`；前台等級卡／會員價／主色 loop projection（加級零改 code，同分類動態化 §3 一樣）。**12 個曆月係固定業務規則，唔設 `hibilab_vip_calc_window` 或其他可改窗口；付款成功／退款事件即時重算，每日 Action Scheduler 再核對全體會員一次。** 詳見 §5／§21。

- **VIP × Coupon money-safety guards**：server-authoritative total、computed price never-store、order-build重算、官方gateway只收Woo最終total、WC_Order context gate、個人化response不進shared cache、admin-write invalidate、net-of-successful-refund、單一rounding point、搶先server-side gate — 以下不重覆：
  - **G1 自動 rolling tier 永不認 stored id；G10 當刻有效兼仍enabled嘅手動 override係唯一明確例外** — resolver先每次用近 12 月商品淨消費對現行 enabled 表 bucket，取「門檻 ≤ 消費」最高者，冇 match跌 HK$0.00 base；其後只可按G10驗證過嘅active override覆蓋結果，失效／已刪／disabled id即忽略並回自動結果（防刪等級靜靜收錯價／`->discount` null fatal）。單元測試：mid-request刪剛resolve嘅自動級或override級 → 斷言重讀現行表、回合法自動級／base，非null／fatal。
  - **G2 單一canonical quote service**：有效會員單價、sale安全網、birthday snapshot、minor-unit precision及顯示值全部實作§6唯一公式；使用Woo公開decimal／precision helpers，唔創造不存在嘅`wc_decimal()`，亦唔喺其他class重寫公式。
  - **G3 best-wins單一order-level predicate**：entry、每次recalc及order-build全部直接呼叫§6同一predicate；禁止用兩個不同base嘅「savings」數或另一個比較規則。
  - **G4 canonical quote先選後套**：qty／刪line／apply／remove令Woo recalc時，service按§6產生及選擇唯一winner；fixed-$ coupon由Woo官方discount APIs處理。`after_calculate_totals`只assert「winner、line snapshots、折扣行、Woo total」一致；如唔一致就fail checkout並記可見error，唔喺after hook再遞迴改價。
  - **G5 新checkout嘅server order-build無條件重跑best-wins**（重算當刻rolling tier＋coupon validity，再同G8 immutable生日snapshot合成有效會員價；coupon唔再勝就丟），**唔靠client recalc事件**。跨午夜／rolling-window邊界可更新tier／coupon結果；一旦Woo已建立有效order並鎖定items/totals，`order-pay`只支付該張已保存總額，唔跨日重價。Woo鎖定新order final total後先交官方Stripe Gateway；HIBI唔自行create／update PaymentIntent。
  - **G6 移除 coupon 嘅比較函數 idempotent／單向／無副作用**：移除前 check `WC_Cart->has_discount`；系統發起嘅移除 suppress WC notice；request-scoped guard flag 防 recursion（避免 notice storm＋數字唔一致）。
  - **G7 rolling spend只認immutable Woo order origin＋同一付款證據**：建立order時保存`customer_user_at_order_creation`、`created_as_guest`及`order_created_at`；只計`created_as_guest=false`、會員ID相符、order建立時間不早過註冊時間，而且`PaymentEvidenceService`回傳`confirmed_paid|confirmed_zero`，其`first_confirmed_at`落喺當刻向前12個香港曆月窗口內嘅訂單。窗口同FIFO都只用service嘅首次確認時間，reference只作audit／對數；日後billing email、customer assignment、status來回或任何Guest帳戶變動都不可改eligibility。HIBI唔直接再讀`get_date_paid()`另判一次，亦唔另造provider capture ledger。
  - **G8 生日 boost 落單一刻 pin**：checkout submit 用香港時區 resolve boolean＋所用%，寫 immutable order meta `_hibilab_birthday_boost`；price filter 之後只讀 pinned flag，order-build／settle **唔准**再行 `is_birthday_month()`。1 號祝賀 email 係另一條 Action Scheduler＋reconciliation 流程，唔 gate 價。
  - **G9 rolling 商品淨消費即時升降**：付款成功即時按折扣後商品行實付額計入；只喺退款 `succeeded` 後扣商品／未歸類退款，明確 shipping／tax／fee refund唔扣；每單貢獻 clamp ≥0。退款要回寫並重算原訂單貢獻，原單已離開窗口就唔產生額外負數；取消／付款失敗／HK$0.00單貢獻0。每次 resolve 都按當刻向前 12 個曆月重算，舊單跌出窗口就即時反映；每日 Action Scheduler 核對 projection。**冇週年結算、冇 cycle floor、冇保級期。**
  - **G10 override＝一個plugin-owned、typed、versioned user meta state**：`{tier_id|null,set_at|null,set_by|null,expires_at_utc|null,version}`。所有寫入只經WooCommerce「VIP會員」專用application service：action-specific nonce、`hibilab_manage_vip_overrides`、target-bound authorization、target必須係一般會員；不可為顯示submenu而額外授予`manage_woocommerce`，WordPress Users／Profile／REST一般user update完全忽略此欄。保存時以表單帶入嘅expected version同目前state做optimistic concurrency check，stale form拒絕並要求重新載入；成功後`version+1`、寫audit、清相關cache。下拉`auto`原子清空active tier／expiry但保留version；指定tier必須仍enabled，日期只接受今日或未來香港日期並normalize成翌日`00:00 Asia/Hong_Kong`所對應嘅UTC instant，空值代表無期限。`resolve_tier()`每次讀都以server time驗tier仍存在／enabled及未過期；失效即時回自動rolling tier，Action Scheduler只做清理及cache失效，唔係正確性來源。Expiry action帶`user_id+expected_version`，版本不符／提早／重送一律no-op。結帳每次由Woo重算價錢及資格；若override喺送gateway前失效，更新Order Review並要求客重新確認；官方gateway開始3DS後沿同一Woo order total完成，HIBI唔中途重價。唔開跨`wp_users/wp_usermeta` transaction、唔row-lock core table、唔造第二張override table。
  - **G11 搶先 gate fail closed**：引用最低等級被刪／停用＝**停售**（絕不 fail-open 俾人買會員專售）；gate 用門檻值（消費額）比較、非 repeater row 位置；VIP 設定儲存後亦必須按門檻自動排序，後台無拖拉 affordance。**HK$0.00 base 等級 non-deletable／non-disableable**（validation 鎖 enable toggle）。**主色 validation**：enabled 級必須有合法 hex，save 拒空；render 時色缺失／非法 fallback base `#e4d6c5`，永不 emit 含空 stop 嘅 `linear-gradient`。
  - **G12 付款前總額必須server-authoritative**：新checkout每次提交都按當刻商品、有效會員價、生日snapshot、coupon、運費及fee重算；官方Woo Stripe Gateway只可收到Woo final order total，client posted amount永不可信。若重算同客人上次畫面不同，更新Order Review並要求重新確認，未確認前唔送gateway。既有`order-pay`唔重算或改寫舊order items/totals；只支付同一張仍有效、未付款兼可履行嘅order。hold／reservation過期、商品已不可履行或saved order invalid時，取消／expire舊單並要求以目前Cart重新結帳，唔喺舊單上改價。3DS開始後由官方gateway完成同一order/payment，HIBI唔事後重價或另建PaymentIntent。免運 subtotal 遵守 §7 固定 eligible-lines 組成及 winner-path pre-coupon 定義；達標後 coupon 不撤銷免運，亦唔可改用 shipping、tax、fee 或 order total。

- **VIP／Coupon 客面文案（專業品牌廣東話，取代 demo 口語；全站 `HK$`＋千位逗號＋固定兩位小數；禁「保留／暫停／留」；客面只用「會員價／會員優惠」，「折扣／優惠碼」只留 coupon）**：
  1. **PDP／商品卡會員價**：PDP「**銀級會員價 HK$306.00**」＋劃線 `HK$340.00`（所有客面金額經 `wc_price()`／同一 canonical display formatter；**唔出**任何解釋細行 — 2026-07-10 客戶裁定；同時重申 §10：HIBI 內容區文字一律 ≥16px，tag／pill／chip 都包）；卡＝價下一行 16px terracotta 細字 tag「**{級}會員價**」（文字行，非 pill）、原價劃線。tag 一律「{級}會員價」，絕不寫「折扣」。
  2. **未登入 PDP 單一提示**：「**登入享會員價，此產品低至 HK$306.00**」（HK$306.00＝Q10 例；_p14 樣本頁＝寵物噴霧，出 HK$151.20）。HK$X 必須由該產品最低可購 variation 嘅會員價 server-side derive，禁止另出全店折扣 teaser，亦禁「限時／優惠」（會員價係永久）。
  3. **coupon 因會員價已較平被拒**（取代 demo 錯用嘅「優惠碼無效,請再試」）：「**會員價更抵，此優惠碼無需使用。**」（2026-07-10 客戶裁定短版）
  4. **coupon 贏會員價、成功套用**：「**已套用優惠碼。**」（2026-07-10 客戶裁定短版）折扣行 label「優惠碼 {code}」。
  5. **Cart／Checkout 會員自動折扣行（不可移除）**：label＝**「會員優惠」淨四個字**（2026-07-10 客戶裁定：全站統一，唔出級名、唔出「自動」章），金額 `−HK$X`；**唔畫 ✕**（行不可移除）。
  6. **等級卡（修 demo）**：非頂級只出一行「距離 **銀級會員** 還差 HK$1,320.00」＋右邊「HK$680.00 / HK$2,000.00」＋進度條，禁止再加同義「再消費…即升…」；頂級保留「尊尚會籍 · 已達最高等級」，右欄「維持中」。
  7. **生日月啟用提示**：cart／banner「**生日會員價已自動生效，無需優惠碼。**」（2026-07-08 客戶裁定：去晒祝賀語，淨陳述）；PDP tag（生日月取代常規 tag）「生日會員價 HK$299.20」。
  8. **搶先鎖（兩態＋server 拒絕 notice 同句）**：訪客「**此為會員搶先發售產品,登入 HIBI LAB 會員帳戶即可選購。**」＋登入／註冊 CTA；未夠級會員「**此產品為 銀級或以上會員 搶先發售,你目前為 一般會員。累積消費至 銀級即可選購。**」＋連去等級卡（唔露 raw WC error）。
  9. **生日祝賀 email 開首**：「Amanda,生日快樂。由今日起整個生日月,你嘅會員價會自動升級為生日禮遇價 — 無需優惠碼,購物時已為你計好。」（明寫「無需優惠碼」免客搵 code）。
  10. **無效優惠碼**（碼不存在／過期／未達使用條件）：「**優惠碼無效，請再試。**」（WC 原生錯誤經全域 notices skin，demo 已用呢句）。
  11. **付款處理中 overlay**（§20.1）：「**正在安全處理付款…**」＋「**請勿關閉或重新整理頁面**」。
  12. **預訂同意 checkbox**：「**我明白並接受預訂商品約 1 週到貨，訂單將於預訂商品到貨後一併寄出。**」（「約 1 週」＝到貨期欄 derive；多件唔同期變體見 §20.1）。
  13. **補購 opt-in（PDP 購買區表單）**：「**到期提醒我補購**」（無「— {產品}」suffix — 已喺 PDP 產品 context；{產品} 只留 email ⑥／Account 面板行）＋排程「每隔 N 日／週／月」或「每月 N 號」＋hint「**只會寄提醒電郵，唔會自動扣款；可隨時喺會員中心「補購提醒」更改或取消。**」；面板空狀態「你未有補購提醒。喺產品頁勾選「到期提醒我補購」即可設定。」；面板動作 label＝「暫停提醒」／「已暫停」／「恢復提醒」／「取消提醒」；email ⑥ 尾句「想暫停、更改或取消提醒，隨時可以喺會員中心嘅「補購提醒」處理。」
  14. **Cookie 橫額（書面語例外 — 2026-07-10 客戶裁定）**：「**本網站使用 Cookies 分析網站使用情況，以改善購物體驗。**」＋「接受」／「拒絕」＋「私隱政策」。
- **PLP 分頁**：未篩選＝crawlable `/page/N/`（跟 workshops archive-loadmore pattern）；有 filter 狀態＝AJAX＋URL 參數（`?pa_scent=…`），filter 版 canonical 指返基礎 archive（唔入 index）。「顯示 X / Y 件」由 query `found_posts` 出。
- **排序對照**：精選推薦＝`menu_order asc`＋date desc fallback（客喺 Products 排序畫面拖）；價格低至高／高至低＝price asc/desc（meta lookup）；名稱 A–Z＝title asc（custom orderby）。
- **series ↔ catLabel**：series＝`product_cat` **子分類**（寵物護理 → 情緒／身體／日常）；卡 label 組合規則＝「父分類 · 子分類」，無子分類只出父分類；PDP breadcrumb 連去子分類 archive。
- **audLabel單一來源**：卡面「適用」label由產品已剔選嘅`pa_audience` term display names經一個批准formatter組合；`pa_audience`同時係filter及顯示真相。唔另開「適用對象顯示」ACF文字欄，避免兩份資料要人手同步。
- **免運門檻／文案／實際 rate 單一來源**：`hibilab_free_ship_threshold`（admin 唯一輸入，空/0=停用）＝canonical business setting。**當前購物面** — Cart（nudge「再加購 HK$X 即享免運費」）／Checkout／Shipping 政策頁／**側滑車 footer**（**PDP 唔出** — 2026-07-08 客戶裁定）— 讀目前 setting 同同一 predicate；**歷史 Order Received** 只顯示 Woo order 已保存運費及落單時 immutable eligibility snapshot，唔再讀目前 setting。**Cart 頁未達門檻視覺：運費行值＝「於結帳計算」（muted brown），nudge 另起一行（視覺 pattern＝側滑車 _p26 ②）**；金額一律 `HK$`＋千位逗號＋固定兩位小數。門檻停用只隱藏當前購物面門檻文案，絕不可倒改舊單。**Email 唔屬目前 setting 消費位**：email 運費 label 淨出「運費」＋`{shipping}` order 實數，唔准喺 prose 焊死門檻金額。運費本身 = **商家預設 per-method 費率**；達標時由 `hibilab-commerce` 經 Woo 受支援 rate filter 將當前可用方法統一套成免費。WC／ShipAny 內建門檻保持停用，health check 驗證冇第二來源，Admin 毋須三處同步。
- **搜尋契約**：overlay數據＝自訂、session-aware REST endpoint。每個結果係固定typed projection：`name:string`、`catLabel:string`、`audLabel:string`、`price:string`（不含`HK$`嘅HKD decimal wire value，例如`"340.00"`）、`mprice:string|null`（同格式）、`img:string`（由Woo attachment API產生、只准allowlisted HTTPS attachment／CDN URL）、`url:string`（經same-origin驗證嘅產品canonical permalink）、`stock:'in'|'preorder'|'out'`、`locked:boolean`、`early:string|null`、`sizes:string[]`；`price`／`mprice`只係server已決定嘅顯示值，browser絕不自行計價。REST JSON只回schema-validated／sanitized canonical raw values（URL用`esc_url_raw()`加上述origin／allowlist驗證），**永不預先HTML-escape**。Production renderer建立DOM nodes，文字只用`textContent`，已驗證URL只經`href`／`src` property賦值；如實作必須產生HTML string，就只可喺最終sink按context escape一次，絕不把REST data原樣插入`innerHTML`。Renderer必須消費`img`／`url`，**不得**再為`img`加副檔名、亦不得硬編共用產品href；原型無`url`／只用圖片basename時先fallback去demo頁及`.png`，呢條fallback不得帶入production adapter。`locked=true`時`early`必須係非空有效等級label；否則`early=null`。`mprice`、`locked`及`early`必須同PLP呼叫同一個canonical catalog pricing／eligibility projection；Guest固定`mprice=null`，唔可洩漏會員專屬價，`sizes.length>1`只決定已批准嘅「起」字樣。最終REST response因含登入身份／生日／VIP eligibility，統一回`Cache-Control: private, no-store`並禁止WP Engine/shared cache；公開catalog facts可喺server內部獨立object-cache後再逐request合併，但唔准cache整份個人化response或另寫第二套價錢／gate邏輯。**overlay結果行要消費`stock`欄出售罄／預訂tag（§19）**；最多回首50件，超出時顯示「顯示首 50 件」及連去帶同一query嘅PLP瀏覽完整結果。直入`?s=`→301去`/hibi-lab/shop/?s=…`由PLP render結果（無獨立search.php設計）。**匹配欄位契約：overlay同PLP `?s=`兩條路必須查同一組欄位—產品title＋`product_cat` label＋`pa_scent` terms＋`pa_benefit` terms**—一個定義兩邊實作。Overlay零結果→只出「冇符合…」＋「睇全部商品」link去PLP（**無熱門/建議詞功能**）。
- **PLP 空狀態三變體 ＋ Cart 清空**：#plp-empty 按狀態切換 — (a) 無 filter 無搜尋（空分類）→「呢個系列即將上架」＋「瀏覽全部商品」掣（切返「全部」pill）；(b) 有 filter →「未有符合的商品／試下放寬篩選條件」＋「清除全部篩選」；(c) 有搜尋字 →「搜尋「X」冇結果 — 清除搜尋或試下其他關鍵字」＋「清除搜尋」。clearAll() 喺空分類情境要一併 reset 返「全部」分類。**PLP AJAX 失敗/timeout**＝同搜尋 overlay 錯誤同一口徑（「暫時未能完成，請再試一次。」＋重試掣，唔准當「未有符合」空結果呈現 — §23.2#9）。**Cart 清空**：items 列換 cart-empty block 時，成個訂單摘要 aside 一齊藏（production 行 WC 原生 `cart-empty.php` 分支自然無 aside）。
- **側滑車 qty／移除接線**（§12 fragments 唔夠）：drawer 嘅 qty ＋／−／移除 = **由`hibilab-commerce`擁有嘅自訂 wc-ajax endpoints**，server授權／nonce／cart session重讀後呼叫 `WC()->cart->set_quantity()`／`remove_cart_item()`並回傳 refreshed fragments；Theme只提交action及render response。不接受 backorder 時，stepper 上限＝Woo 判定嘅可購數量；接受 backorder 時**唔可以用目前現貨量封頂**，超出現貨嘅部分按 §21.11 同一商品行顯示「現貨 X · 預訂 Y · 到貨期」，每次 qty response 都由 server 重算，Checkout 再做 §20.1 最終重驗及同意。**Drawer 空狀態**：最後一件移除 → 「購物車暫時係空嘅」＋「繼續購物」（關 drawer），藏 查看購物車／去結帳／小計。
- **全域 WC notices skin**：`woocommerce/notices/{error,notice,success}.php` overrides，用現有 token（error 三色組 `#fbeae4`/`#e3a58c`/`#8a3618`、成功 `#1f7a4d`）＋zh-HK 字串；覆蓋 session 過期／庫存不足移除／coupon 錯誤／order-again 通知等全部原生 notice（render 喺 Cart items 上方＋Checkout form 上方）。**Stripe 付款錯誤例外** — 繼續入 summary 嘅 `#pay-error` 槽。
- **order-pay／order-failed／cancelled**：用`#pay-error`語彙skinWoo notices＋thankyou.php失敗分支（沿用Order Received版式，紅banner取代綠tick）；無需新設計稿。`order-pay`只重付同一張仍有效未付款Woo order，沿用已保存items、shipping、coupon、生日資格、預訂承諾及final total，唔重新定價。提交時只驗order key／身份、狀態、hold／reservation仍有效、商品仍可按已承諾數量履行；任何一項失效就取消／expire舊單並要求客人以目前Cart重新結帳，唔喺舊單上跨日改商品行或銀碼。PaymentIntent重用／取消、3DS、return及webhook全由官方Woo Stripe Gateway處理，HIBI唔直接retrieve／create第二個Stripe object。
- **付款回程／Order Received只投影§5 action matrix**：template先取得`PaymentEvidenceService`狀態＋typed reason，再逐格使用§5已批准精確文案及Pay CTA條件；唔由Woo raw status、browser return、`needs_payment()`或template另寫「pending/on-hold」規則。只有兩個`confirmed_*`狀態可顯示成功tick、成功內容及enqueue確認信；ambiguity／unknown無Pay CTA、無確認信、無履約；`unpaid`亦只有§5全部canonical payable前置成立先可出原生Woo Pay CTA。畫面只讀同一Woo order及order-item快照，唔fallback去live cart、現時產品資料或allocator；遲到／重送event只由官方gateway＋同一projector idempotently收斂。
- **Account 訂單狀態 label map**：已確認／處理中／已寄出／已完成（timeline 步）＝正常4步；只有**全額退款成功且無餘下履約責任**、已取消或真正付款失敗先用terminal單行label（已退款／已取消＝muted brown、付款失敗＝`#b4452a`）並移除timeline。部分退款保留餘下商品正常履約timeline，另顯示累計成功退款「已退款 −HK$X」；每筆Woo refund ID可獨立audit，processing／unknown唔可當已退款。同email變體④對齊。未確認付款各狀態、文案及Pay action完全依§5 matrix；一律唔出4步success timeline，亦唔由order status自行放行重新付款。
- **長列表／數據量 stress-case 口徑（unbounded-list 處理單一來源）**：PLP＝載入更多（12/批）＋crawlable `/page/N/`；搜尋 overlay＝截斷 50＋「顯示首 50 件」disclosure；filter facet popover＝max-height 64vh 內部 scroll（term 幾多都得，demo CSS `.fpop` 已有）；側滑車 items＝區內 scroll；Checkout 摘要 items＝scroll＋fade；**訂單記錄 >10 張＝WC 原生 my-account 分頁（`/orders/page/N/`，上一頁／下一頁掣品牌 skin）**；**概覽「最近訂單」mini-list cap 最近 3 張**（更多靠「訂單記錄」nav）；**補購提醒＝per-product 唯一，分頁只讀§25.2 policy（Phase 1=`20/頁`）**，超過policy page size先顯示Woo原生分頁；Cart 頁 line items＝自然直落無 cap。
- **產品評論停用**：WooCommerce Settings→Products 關閉 reviews（demo 無評論 UI、§10 無假數據）；PDP template 不含 reviews section。
- **Landing 精選產品區**＝WC featured products loop；無 featured 產品 → 成區 hide-if-empty（同 §21.23 見證區一致）。
- **Email deliverability（上線硬項）**：`hibilab-commerce`只可接入現有Scent.M Mail Center嘅單一Admin／send boundary，**本handoff明確唔會切換或停用Scent.M現有transport**；HIBI只消費Mail Center公開send contract。Resend係目標供應商，但API／SMTP adapter由site-wide Mail Center設定擁有（Resend兩者都支援idempotency key），HIBI唔揀第二套transport。如日後要將全站adapter改成Resend HTTPS API，必須另開獨立、獲批准、可回滾嘅site-wide migration，喺staging用同一DNS、template及failure regression證明parity後先原子cutover，唔藏喺HIBI實作。WordPress／Woo lifecycle仍係template trigger owner。HIBI使用logical table `hibilab_email_outbox`（實體經`$wpdb->prefix`）作canonical delivery truth：`semantic_key UNIQUE`、email_type、subject_type、nullable order_id／refund_id／user_id／reminder_id、**non-null `provider_profile_generation`**、state、attempt_count、`next_attempt_at_utc`、claim_token／claim_until_utc、預先產生且retry固定重用嘅`rfc_message_id UNIQUE`、nullable `provider_message_id UNIQUE`、template_version、凍結recipient／from／reply-to及凍結render payload（或rendered subject／plain／HTML）、last_error及timestamps；index `(state,next_attempt_at_utc,id)`同`(provider_profile_generation,state,id)`。同一semantic key所有retry只可寄同一snapshot，Admin之後改template／地址唔倒改已排信。每個email_type必須server-side驗證合法subject關聯組合：訂單／寄出／完成／退款／店主信綁order（退款另綁refund），生日綁user＋香港年份，補購綁user＋reminder；唔准用recipient／subject／時間猜關聯。Action Scheduler只喚醒sender。Scent.M workshop outbox有workshop-specific FK，HIBI唔直接共用嗰張表，但沿用同一Mail Center UI pattern，令Admin仍喺同一「Scent.M 電郵」介面睇範本及寄送記錄，唔起第二個mail後台。現有site-wide SMTP transport新增公開typed send receipt：送前已持久化同一immutable snapshot、RFC Message-ID及semantic key，並以同一key加入Resend官方`Resend-Idempotency-Key` header；retry重用三者。回傳只可係`accepted|retryable_failure|permanent_failure|unknown`，裸`wp_mail()` boolean唔等於delivered。Resend verified webhook按exact RFC Message-ID＋同一profile generation配對，再保存provider email ID；event早過SMTP response亦可因outbox先存Message-ID而正確配對。Scent.M既有範本、地址、Mail Center UI及SMTP設定行為不變。
- **Resend event inbox**：另用logical table `hibilab_email_event`保存`svix_id UNIQUE`、**non-null `provider_profile_generation`**、rfc_message_id、nullable provider_message_id、PII-free semantic correlation、event_type、received_at_utc、processed_at_utc、normalized_payload_hash／state及必要關聯鍵；state只准`pending|matched|processed`，index `(provider_profile_generation,rfc_message_id,state,received_at_utc)`及`(provider_profile_generation,provider_message_id,state,received_at_utc)`，唔保存email正文、完整地址或raw payload。Site-wide Mail Center係唯一Resend webhook驗簽／ACK owner：先將verified event durable落自己嘅最小receipt再2xx，之後按provider profile generation＋RFC Message-ID namespace路由HIBI normalized event入本表；HIBI唔另開public Resend endpoint。HIBI async handler先按同一generation＋exact RFC Message-ID配對outbox，再保存／核對provider message ID，所以event早過SMTP response、亂序或重送都唔會丟失／重覆處理；generation不符、RFC Message-ID未知或缺outbox配對嘅verified event保持`pending`並由reconciler再配，超過§25.2 SLA只開可見incident，絕不改event state或跨credential generation猜配。已處理event按§24保留90日。
- **寄送、claim與重試**：canonical outbox states只准`queued|retry_due|sending|accepted|delivery_delayed|delivered|permanent_failure|delivery_unknown`。Sender以compare-and-set由`queued|retry_due`claim成`sending`並設短lease；transport明確接收後轉`accepted`，呢個同`delivery_delayed`都係等待最終delivery evidence嘅**非terminal**狀態，可再轉`delivered|permanent_failure|delivery_unknown`。只有`delivered|permanent_failure`係terminal；`delivery_unknown`阻止盲目重寄但仍可由verified webhook／read truth收斂到terminal，另由incident registry持有原因／營運gate，唔叫「重開」。Sending lease過期只可喺同一Resend 24小時idempotency key有效期內，用同一semantic key／RFC Message-ID／snapshot安全重領；超過界線而結果不明即`delivery_unknown`，唔重寄。Webhook首次配對後保存provider message ID；Resend webhook用raw body＋Svix headers驗簽後，以verified `svix-id` dedupe，處理`email.sent`、`email.delivered`、`email.delivery_delayed`、`email.bounced`、`email.complained`、`email.failed`及`email.suppressed`。**API accepted／`email.sent`只代表已接收，Admin不可顯示「已送達」；只有`email.delivered`先係送達。** Permanent bounce／complaint／suppressed同normalized `permanent_failure`唔自動重寄；只有normalized `retryable_failure`先喺idempotency界線內排retry，`unknown`先讀provider truth、不可判就`delivery_unknown`。失敗喺order／System Status可見並通知營運。Webhook payload及log唔保存多餘正文／地址，PII同secret一律redact。DNS設**SPF＋DKIM＋DMARC**；launch gate包括Gmail／Outlook inbox及visual render、bounce／complaint／suppressed、24小時邊界、event-before-response、同一semantic key零雙信。低頻delivery reconciler對`accepted|delivery_delayed|delivery_unknown`而超過明確observation SLA嘅row，用Resend官方bounded retrieve/list讀`last_event`；仍不可判就保留`delivery_unknown`兼可見告警。時間只觸發核對，絕不直接猜已送達／失敗。另有truth reconciler掃描帶`_hibilab_order_schema`嘅Woo order／refund transitions，按預期semantic keys補建任何「transition已成立但enqueue前crash」而缺失嘅outbox row；UNIQUE令重跑no-op。Admin更正錯誤收件地址後重寄必須建立新`manual_resend:{original_key}:{generation}` semantic key及新immutable recipient/render snapshot，唔修改、重開或冒充原row。
- **Woo email replacement allowlist（只限HIBI order）**：HIBI outbox取代Woo `new_order`、`customer_processing_order`、`customer_completed_order`、`customer_refunded_order`四款內容owner，並另擁有「已寄出」同補購提醒；同一semantic transition一經durable enqueue就由HIBI sender唯一寄送，唔再並行寄Woo版本。特別係`customer_completed_order`：HIBI order嘅Woo原生trigger固定停用，只有§7 `ShipmentEvidenceService` completion由false→true先以`order_completed:{order_id}` enqueue一次；Admin手動status、重送provider event或reconciler重跑都不可另寄。**`customer_on_hold_order`對HIBI order固定停用**：raw Woo `on-hold`並唔係第六個付款真相，亦唔在已批准客信矩陣；pending／unknown不寄確認信，首次`confirmed_*`只寄下段同一封品牌確認信。`customer_note`、新帳戶、忘記／重設密碼等仍由Woo／WP原生lifecycle寄送，只套共用品牌wrapper。`customer_failed_order`／`customer_cancelled_order`保留真正未確認付款嘅原生lifecycle，但加只限HIBI order嘅enabled／recipient eligibility guard：`PaymentEvidenceService=confirmed_paid`而未有合法成功退款／reversal時兩者一律禁止；`confirmed_zero`只有canonical quantity cancellation evidence先可寄cancelled，failed永遠禁止。Admin、REST或第三方只係改status唔可令已確認客人收到「付款失敗／訂單取消」矛盾信。所有enabled／recipient guard只限帶`_hibilab_order_schema`嘅HIBI order，唔可以用全域filter一刀切晒Woo email。通知收件人讀Mail Center嘅`hibilab_notify_email`，Reply-To讀`hibilab_support_email`，From讀已驗證網站網域寄件設定；空值或未驗證係launch blocker，絕不fallback去`admin_email`。技術設定只畀真正網站管理員，`scentm_manager`只見營運狀態及範本。無論內容owner係HIBI、Woo或WP，實際send都經同一Mail Center transport boundary及可觀察log；native Woo／WP trigger唔被HIBI複製，但transient transport failure一樣要有immutable job同bounded retry。密碼重設先即時嘗試，重試不得超出token有效期；成功或永久失敗後唔再寄。
- **訂單確認／店主新單唯一trigger**：兩封都只由同一張order嘅`PaymentEvidenceService`首次由非確認狀態轉成`confirmed_paid|confirmed_zero`時建立，但各自用獨立semantic key（`customer_order_confirmed:{order_id}`、`owner_new_order:{order_id}`）及immutable recipient／render snapshot。Browser return、Woo raw `new_order` hook、Admin改status、order note、webhook重送或reconciler重跑都唔可直接寄信；truth reconciler只補建「付款transition已成立但outbox缺row」嘅同一key。店主信action文案仍按履約projection分ready／awaiting stock，觸發時機同文案分支不可混為一談。
- **付款 overlay ownership**：HIBI只可監聽Woo公開checkout submitting／error狀態，用`#pay-overlay`防重複提交；Stripe challenge／3DS UI及生命週期完全交官方gateway。唔hook `stripe.confirmPayment`或gateway private JS。所pin版本如冇公開事件可安全協調，就保留Woo原生processing UI，唔為追求demo overlay而接駁private callback。
- **Woo 交易 email**：WP dev必須按repo已批准嘅`HIBI LAB Email.html`同`_p24-email-variants.html`視覺實作，唔准自行重新設計；跟Scent.M收據pattern做**後台可編輯**（option儲存＋placeholder＋live preview＋測試寄送）；訂單編號顯示`HL-`格式；new-order通知收件人＝店主通知地址（非admin_email）。每封同時輸出approved HTML、由同一render model產生嘅`text/plain` fallback，以及hidden preheader；preheader唔另開一份容易漂移嘅文案，固定由該變體第一段已批准可見摘要清理markup並截斷產生。HTML、plain text同preheader所述狀態必須一致。**六款變體（同一wrapper）**：①訂單確認（4步狀態條第1步active；`{discount_rows}`出「會員優惠」／已用碼行、預訂項讀付款時snapshot）②**寄出通知**（第3步active＋追蹤號卡）③訂單完成（第4步）④退款通知（只可由refund succeeded transition觸發；退款金額行，唔出狀態條）⑤店主新訂單通知（內部，加客人聯絡資料）⑥**補購提醒**（Action Scheduler到期觸發；非扣款；樣本_p24）。**另**：VIP生日祝賀email只寄俾已opt-in會員，必須有visible退訂連結及標準one-click unsubscribe headers；Welcome／密碼重設沿用Woo lifecycle，只換同一品牌wrapper。**Placeholder contract**：`{name} {customer_email} {customer_phone} {order} {date} {items} {subtotal} {discount_rows} {shipping} {shipping_method} {fee_rows} {tax_rows} {refund_amount} {refunded_total} {total} {address} {tracking_no} {tracking_url} {carrier} {order_url} {status_note} {product} {schedule} {reorder_url} {manage_reminders_url} {unsubscribe_url}`。placeholder按HTML text／attribute／URL／plain-text context做typed escaping，禁止範本輸入任意PHP／shortcode／未清洗HTML；preview同真正send必須用同一sandboxed renderer及同一fixture。儲存範本時驗證unknown／required placeholder；render後若required值缺失，唔寄錯內容，轉可見失敗。`{order_url}`只可用 Woo 官方 guest view/order-received URL＋order key，HTTPS、不可用可猜 order ID；任何付款／修改 action 仍需 Woo 身份／order-key／nonce 驗證，email link 只提供 view。Guest 訂單確認 email 必須喺訂單摘要／狀態後、support footer 前顯示一個品牌主按鈕「查看訂單」連去 `{order_url}`；plain text fallback 同樣列出 URL。此 URL 不可經第三方 click-tracking redirect，亦不可送入 GA4、mail log 或 error log。
- **Email可編輯區域同每款required contract**：wrapper、商品清單、totals、進度元件、CTA destination及結構全部由renderer code擁有；Admin每款只可編subject、eyebrow、title、opening、closing／`status_note`等已批准copy slot，允許markup限安全inline文字格式，唔可編整份HTML、PHP、shortcode、商品／金額loop或URL attribute。Subject最多150 grapheme；hidden preheader由opening移除markup、收空白並截至120 grapheme（含ellipsis）產生，唔另存第二份copy。儲存同真正render均按下表驗unknown／required placeholder，缺值就唔send並開incident：

  | Email | 預設subject | Required placeholders | Optional placeholders |
  |---|---|---|---|
  | ① 訂單確認 | `[HIBI LAB] 訂單確認 — {order}` | `{name} {order} {date} {items} {subtotal} {shipping} {shipping_method} {total} {address} {order_url}` | `{discount_rows} {fee_rows} {tax_rows} {status_note}` |
  | ② 已寄出 | `[HIBI LAB] 訂單已寄出 — {order}` | `{name} {order} {items} {carrier} {tracking_no} {tracking_url} {order_url}` | `{address} {status_note}` |
  | ③ 已完成 | `[HIBI LAB] 訂單已完成 — {order}` | `{name} {order} {items} {total} {order_url}` | `{address} {status_note}` |
  | ④ 退款通知 | `[HIBI LAB] 退款通知 — {order}` | `{name} {order} {refund_amount} {refunded_total} {order_url}` | `{items} {total} {status_note}` |
  | ⑤ 店主新單 | `[HIBI LAB] 新訂單 — {order}` | `{order} {date} {items} {customer_email} {customer_phone} {address} {total} {order_url}` | `{subtotal} {discount_rows} {shipping} {shipping_method} {fee_rows} {tax_rows} {status_note}` |
  | ⑥ 補購提醒 | `[HIBI LAB] 補購提醒 — {product}` | `{name} {product} {schedule} {reorder_url} {manage_reminders_url}` | `{date} {status_note}` |
  | 生日祝賀 | `[HIBI LAB] 生日會員禮遇已生效` | `{name} {unsubscribe_url}` | `{status_note}` |

  Welcome／密碼重設繼續由Woo／WP原生subject及token contract擁有，只套品牌wrapper，唔塞入HIBI template matrix。HTML、plain text、preview、test send同production send必須消費同一typed render model及fixture；test send只替換recipient，內容仍按所選fixture生成。
- **Woo 交易 email — 已批准 4 步進度元件（2026-07-24）**：視覺及 copy 以 `HIBI LAB Email.html`＋`_p24-email-variants.html` 五個 `email-safe order progress` 樣本為準。元件固定顯示「已確認／處理中／已寄出／已完成」四步，使用 email-safe presentation table＋inline CSS：圓點欄 `19%`、連線欄 `8%`（四點三線合共 100%），實心 `●`＝已到達、空心 `○`＝未到達；已到達點／線 `#A64B2A`，未到點／線 `#D8C8BA`，當前 label `#A64B2A`＋bold，已完成但非當前 label `#5A4A3E`，未到 label `#A08E7E`。說明句另置於下方、以 `1px #ECE0D2` 分隔，唔再同進度列排成一行；外卡 `#FAF5EE`／`#ECE0D2`。連線用 nested table `bgcolor` 1px row，唔用 flex/grid/SVG/JS；Outlook 忽略圓角時只可方角降級，內容及對齊不可改。①會員確認、①訪客確認、①付運費／優惠碼確認＝第 1 步；②寄出＝第 3 步（追蹤卡仍獨立放上方）；③完成＝第 4 步；④退款、⑤店主通知、⑥補購提醒、生日祝賀、Welcome／密碼重設全部唔出進度元件。**此元件係寄信一刻嘅靜態狀態快照，唔會靠外部 API 改寫已寄出電郵**；ShipAny adapter按 §7 提供②寄出通知嘅真實 tracking 資料／handoff trigger，並另以已驗證 final-delivered trigger推進 Woo `completed`、客面第4步及③完成電郵，兩種 trigger 唔可混為一談。
- **收件／寄件設定單一來源**：上文「店主通知地址」一律指Mail Center嘅`hibilab_notify_email`；Reply-To一律指`hibilab_support_email`；From一律指已完成供應商網域驗證嘅site-wide寄件設定，唔讀`admin_email`亦唔在template另存第二份。
- **Email 商品行快照**：`{items}`必須讀Woo order items＋付款時保存嘅HIBI order-item meta，並同§21.11完整顯示數量：「預訂 Y · {到貨期}」或「現貨 X · 預訂 Y · {到貨期}」；不可讀live allocator／目前產品資料，亦不可因之後收貨而將原本付款時嘅預訂文字改成現貨。
- **店主新單通知唔可催錯出貨**：⑤ 內部電郵嘅 action 句由履約投影決定；`awaiting_stock`／任何預訂未分配完＝「此訂單包含預訂商品，請待系統顯示『可出貨』後安排出貨」；全單已 ready 先可用「收到新訂單，請安排出貨」。現有 `_p24-email-variants.html` 固定句**只係 wrapper／ready 全現貨分支嘅視覺樣本，唔係可直接複製嘅 unconditional copy**；WP 實作必須用上述兩個 branch，違反即 UAT fail。
- **登入後 redirect**：checkout 用 WC 原生 inline returning-customer form（§17，唔跳頁）；其他店頁登入 link 帶 `?redirect_to=`，theme hook `woocommerce_login_redirect`＋registration redirect 尊重（fallback＝my-account）。 同一規則亦套 registration redirect。
- **「查詢此訂單」**：Account 訂單詳情＋thankyou 各加一條細 link — `wa.me/{hibilab_whatsapp_number}` 預填「你好，我想查詢訂單 #HL-XXXX」；WA option 未設定 → 藏（mailto `hibilab_support_email` fallback）。
- **no-js 韌性**（theme＋store 頁通用）：head 加 2 行 `no-js`→`js` class swap；CSS 加 `html.no-js .ani-this{opacity:1;transform:none}`（hero 類 `opacity-0` 元素同理）— JS 失敗／關閉時內容照現形，JS 用戶零視覺差異。
- **771px scoped 實作**：771px media block 只喺 **store body class**（如 `body.hibi-store`）之下嘅 store-scoped stylesheet 生效；共通 chrome 維持 768。`body{overflow-x:clip}` 同樣只喺 store body class 落 —— theme 手機版嘅 `overflow-x:hidden` 會殺死 PLP sticky filter bar，**不可全局合併**。
- **上線UAT總閘（staging，與production同版本）**：以manifest鎖定嘅官方WooCommerce Stripe Gateway test mode跑卡成功、3DS成功／取消、拒絕、browser付款後斷線、webhook重送／亂序、double-click、兩個tab、有效／已失效`order-pay`、HK$0.00單及Woo raw pending／on-hold；驗Woo只建立／完成正確訂單、stock只由Woo處理一次、HIBI唔另建PaymentIntent、未付款畫面唔假成功、每個交易email semantic transition只寄一次。`PaymentEvidenceService` contract matrix逐格交叉測 HIBI scope、total/currency/method、`date_paid`、transaction ID及`payment_complete()`公開lifecycle：checkout seal未read-back就不可call gateway；候選成功後分別模擬immutable confirmation row INSERT前、INSERT commit後而guard未清、以及row read-back前crash，全部只可由同一projector／operation identity收斂，physical `PRIMARY KEY(order_id)`下只得一份exact-hash相同嘅immutable row，完整row齊先confirmed，任何缺／矛盾都pending／unknown而無客面副作用；全程不得建立或測試Woo payment-confirmation meta mirror。HK$0.00另測標準zero-total成功、double-submit／重送／crash recovery只保留首次immutable confirmation、缺欄或被改即unknown，以及Admin手動改processing／completed唔會產生confirmation。由seal起，Admin、REST、CSV/import、bulk及第三方入口逐項嘗試改product／variation identity、qty、currency、line／order totals、pricing、split／ETA／consent均必須server-side拒絕；地址／聯絡資料仍可按既有受控流程更正。Admin手動將無付款證據order改processing／completed仍唔建demand、唔計VIP、唔開ShipAny、唔出成功客面或完成信；已有正確付款證據order手動改on-hold則付款真相唔倒退、唔出第二次支付action。逐格驗`PaymentEvidenceService=pending|unknown`即使raw Woo `pending`／`needs_payment()`為真亦零Pay CTA；只有service=`unpaid`、冇success／in-flight證據、同一order仍payable、hold／claim有效及全部server gates通過先可出原生Woo Pay並resume同一張order；任何一項失效都無Pay CTA。Woo Hold Stock 59:xx、60+、付款／釋放race同時實測。必須實證browser-return中斷或webhook暫時遺失後，所pin gateway有受支援同步／重試路徑令Woo order最終收斂；如做唔到就保持launch blocker並另設只讀reconciliation方案，唔假設gateway內建定期修復。再驗test／live key、account、endpoint、secret完全隔離，Workshop webhook唔誤處理Woo事件；付款後切換目前Stripe帳戶亦不可改寫舊單凍結嘅account／livemode／gateway／currency／transaction chain，舊單退款只可向原交易tuple執行，錯帳戶／錯模式fail closed。同一UAT要驗直接改paid order做cancelled/refunded唔會假退款／假restock、一般庫存編輯／CSV／REST唔可繞過受履約責任保護嘅stock-owner guard、官方Stripe saved payment methods／My Account Payment Methods於Phase 1保持關閉，以及Stripe客戶receipt/refund email已停用而無重覆信。
- **退款／訂單UAT**：先由Admin、AJAX、REST及bulk入口直接嘗試正金額`refund_payment=false`，除精確provider-succeeded recovery外全部必須server-side拒絕；再喺Woo Admin經官方gateway分別做全額、部分商品qty、部分amount-only、運費／fee退款，覆蓋provider成功、processing、明確失敗、timeout／unknown及重送。驗gateway call前operation已durable落盤並加入provider metadata；pending／unknown期間無restock、無parent `refunded`、客面唔當成功、remaining amount／qty鎖住且第二筆被拒絕。分別模擬Woo refund row仍存在及core因WP_Error已刪除：verified succeeded只建立／保留一筆Woo money refund並一次restock／投影；verified failed／cancelled先tombstone再刪未成功row、清cache及恢復remaining amount／qty／evidence-derived status，之後新operation先可成功；不可判就一直鎖住兼告警，時間到及Action Scheduler重試都不可再次call Stripe。另逐個測claim前後、Woo row save後、provider accepted但response遺失、provider ID bind前、success restock後及projection前crash，重跑必須同一operation收斂且金錢／stock／email零雙動作。只有Woo／provider成功結果先改VIP貢獻、preorder demand、客面狀態及退款email。未寄item qty取消先釋放demand／stock，amount-only唔改出貨qty，已寄退貨唔自動入庫。Stripe Dashboard外部退款另測精確Stripe refund ID↔Woo refund ID配對、錯帳戶／錯livemode／錯PaymentIntent或Charge chain／錯order／錯currency／同額多筆／亂序、配對contract缺失fail closed、amount-only照常出貨、未寄item quantity取消、已寄item拒絕、退款不足拒絕、同一refund double-click／webhook重送no-op、同一order多筆外部退款逐筆收斂，以及只關閉已完成嗰個reason。另鎖死：HK$0.00 local adjustment唔入provider normalizer／VIP money delta／退款email；兩筆HK$50.00共同覆蓋HK$100.00 item、額外金額歸amount-only、已有Woo quantity refund不可再調整；兩個不同external refund request並發揀同一qty只可一個active batch、正常Woo item refund同recovery並發亦只可一個qty writer成功；gateway amount-only import不污染已凍結batch；active label先void；全額amount-only後Woo `refunded`仍可按fulfilment truth安全處理。金額另測qty3／line total HK$100.00分三次取消、fixed／percent coupon、會員價、多line同batch及parent-managed共用stock owner，確認deterministic residual分配總和精確、restock map先按owner aggregate。逐個failpoint測operation claim後、Woo adjustment save後、restock後及projection前crash，重跑只resume同一adjustment且stock只恢復一次；`needs_attention`另測證據自行收斂、網站管理員確認已restock、確認未restock並執行一次、stale version拒絕、`scentm_manager`不可操作、active slot成功後釋放及其他reason不清。任何按金額／時間／order note猜配對、再次call Stripe、第二個adjustment或第二次restock均為UAT fail。
- **Status旁路UAT**：對confirmed-paid及confirmed-zero HIBI order，由Admin、AJAX、REST、bulk及模擬第三方逐個嘗試`processing|on-hold|completed → pending|failed|cancelled|refunded`；known boundary全部save前拒絕，第三方drift亦必須收斂回evidence-derived status並開可見incident。每格驗Woo core冇產生本地正金額refund、stock／`_reduced_stock`、coupon usage、product sales count、demand、客面狀態及email全部保持真相；sanctioned RefundProjector／ShipmentEvidenceService context則仍可完成合法轉移。另跑非HIBI Woo order，證明wrapped core callbacks及原生status行為完全不變。
- **預訂／ShipAny UAT**：先對checkout claim跑：stock=1兩個同owner並發checkout只可一張final split承諾現貨1；既有native hold後切開backorder，新claim仍扣除嗰筆H；claim commit對撞Woo payment reduction、claim consume、Admin／REST negative correction及同owner receipt，逐格證明`S-H-C`只可保守低估、不可高估；active claim先到會阻止負數correction，negative operation先到會阻止新claim，正數receipt照常。Checkout、Order Pay、Admin、REST、bulk及manifest第三方入口逐個測缺scope、缺seal、缺／released claim都必須喺建立／重用PaymentIntent或stock-reducing status前拒絕；合法payment開始後Woo原生reduce→stock-reduced flag→native release完整pass-through，HIBI投影故障亦不可阻止真實扣貨；非HIBI完整pass-through。再測provisional≠final時同一pending order／claim返回Checkout、預訂checkbox重新未剔、第二次提交只resume同一order，abandon／double-click／item-meta失敗／claim-commit後crash／60分鐘到期／failed再order-pay／3DS晚到各自零重覆order、零claim洩漏、零過早release；unknown只告警兼關outbound。其後覆蓋parent及variation各自管理stock、同一owner最後現貨並發、10張A貨只收`+3`、部分分配、A+B混合單、item qty取消後FIFO重分、重按收貨不重複加stock、付款證據＋immutable snapshot完整但missing demand會由reconciler按原priority idempotent補建；demand enum另逐格驗未分配／部分／全分配但未送達仍`open`、final-delivered先`fulfilled`、成功qty取消先`cancelled`，`needs_attention`只關gate而唔改業務state。corrupt／矛盾 demand先顯示`needs_attention`兼關出貨。ShipAny必須實證未ready拒絕、ready只建立一張面單、追蹤write-back、`label_created`唔當寄出、實際交物流先寄②、重送／亂序observation idempotent、observation早過shipment relation仍可稍後配對、webhook／hook遺失後public API reconcile收斂。完成predicate另測：零current shipment、經native／bulk／Portal另建第二件而provider enumeration見到超過一件、只覆蓋部分餘下quantity、void而未replacement、replacement新generation未完整覆蓋、唯一current完整覆蓋兼final-delivered；只有最後一種可完成，其餘全部`needs_attention`兼保持未完成。另測較舊version遲到no-op、同version唔同hash開警告、兩個worker同時project只一個CAS成功，以及provider無monotonic version時全量public truth重建。Admin手動改completed唔出客面第4步／完成信、唔自動倒改core status；期間observation/reconcile照行，受控recovery可修正，而真正送達仍只以`order_completed:{order_id}`寄一次。完成後相反terminal event只開`needs_attention`而不自動倒退客面。void／timeout、未知status、自取點真機選點、plugin-owned callback無ACK ownership及HIBI-owned endpoint先落盤後2xx全鏈都要測；供應商無受支援gate／枚舉／public truth API嘅入口不可用隱藏掣假裝已封。同時驗consent registry可重現舊句／ETA，files-only新環境attributes/pages/zones齊全，產品普通stock edit／import無法繞過open-demand correction入口；另驗普通現貨未寄、已全分配預訂、variation／parent stock owner、active return／exchange／legal hold、delivered後香港第6／7日都不可永久刪產品；第8日亦只喺全部case及責任已完結先可刪，trash／unpublish始終可用。
- **VIP／優惠UAT**：固定rolling 12個香港曆月、付款即升、成功退款／舊單離窗即降、每日核對；2月29日／月尾no-overflow、cutoff同一秒及早一瞬間；Guest／註冊前單唔計；生日由Account／REST／直接POST／兩個並發request只可成功寫一次，Admin更正沿用窄capability並驗old/new/reason/audit＋stale CAS；商品淨額排除運費／稅／fee並逐單clamp≥0；同一charge先部分退款後final-lost chargeback、事件反向／重送／退款與reversal重疊，canonical outflow只扣一次且cap原商品貢獻；outcome／identity／net-outflow不明就保持原級兼reason-scoped incident。全店HKD兩位小數；生日snapshot、搶先gate、總開關、sale-price安全網、override新增／stale form／到期／清除／引用級刪除全測，Account三個統計由同一contribution service得出。用有效sale、百分比／fixed coupon、生日及會員價交叉覆蓋§6唯一formula／winner matrix，唔喺UAT另定predicate；免運另測兩種winner path、coupon令實付跌穿門檻及門檻邊界，全部依§6／§7同一canonical snapshot判定。
- **提醒／電郵／隱私UAT**：補購四種排程、購買重錨、暫停／恢復／取消、舊generation no-op、停機補排、產品不可購買唔寄dead link；workflow逐格驗normalized `accepted|retryable_failure|permanent_failure|unknown`、24小時idempotency邊界及逾期unknown轉人工核對；adapter unit tests另鎖SMTP 4xx=retryable／5xx=permanent、HTTPS 429＋5xx=retryable／其他4xx=permanent、network timeout=unknown。再驗RFC Message-ID event-before-response、exact match、錯／未知Message-ID不誤配、verified `svix-id`重送去重、`sent`不當`delivered`、bounce／complaint／failed／suppressed可見、HTML／plain text／preheader一致、Gmail／Outlook render及同一semantic key零雙信；marketing opt-in失敗可重試且唔阻付款，退訂兩路、privacy export／erase、retention及log redaction通過；privacy資料超過batch size時逐頁無漏／重覆、重跑erase idempotent、target隔離且舊job唔復活PII。生日祝賀另測：1號後先設定生日、1號後先opt-in、enqueue前crash由reconciler補回、enqueue後退訂令worker no-op、2月29日、月尾／跨年、重送／並發及同一香港曆年最多一封。Guest另測不剔帳戶成功付款、剔帳戶但付款失敗（帳戶不回滾）、已存在email登入提示、正確／錯誤order key、HTML「查看訂單」CTA同plain-text URL一致且不入GA／log。另直接由Admin／REST／第三方將confirmed-paid HIBI order改failed／cancelled，必須零`customer_failed_order`／`customer_cancelled_order`；真正未確認付款失敗／取消仍各寄一次，confirmed-zero只按canonical cancellation evidence寄cancelled。另驗transition成立後模擬enqueue前crash可由truth reconciler補信、手動更正地址重寄建立新semantic key而唔重開舊row、全部webhook丟失由provider reconcile收斂／不可判就告警、退訂後延遲opt-in永不復活，consent policy version更新先re-prompt。
- **整站／營運UAT**：Scent.M與HIBI menu／role capability、404分流、search/filter/cache session隔離、Cart／Checkout／Account responsive、CSV、GA4 consent、SEO/noindex/schema、WP Engine cache matrix／Alternate Cron／backup restore（含backup後付款、退款、寄信、ShipAny event重播而零雙動作）、plugin deactivate／reactivate（只移除／重建HIBI wake-ups，零重覆side effect）、files-only換plugin files而不reactivate仍由UpgradeCoordinator收斂、Woo／Stripe／ShipAny／ACF upgrade regression。實際version、結果、截圖／log證據及未通過項寫入release checklist；任何P1／P2、客戶待決或真實provider全鏈未通過都不可標Production Ready。 Woo HPOS on/off、所有template override同pin版本upstream diff、Woo Status零outdated template，以及`woocommerce_thankyou`／notices／auth／Stripe／ShipAny／GA hooks完整觸發一併屬release gate。


## 23. 狀態樣本檔＋合約補充（2026-07-08 demo 全面修訂）

> 2026-07-08 demo 完成「乾淨交付」修訂：**前端計算層全部拆走**（會員價／best-wins／tier gate／免運門檻數學 — 全部變成 dataset 設計值欄位 `mprice`／`locked`／`stock`，兩份 JSON 已歸一＋§20.5 照舊 demo-only）；每個預設 demo（persona＝已登入銀級會員）見唔到嘅狀態，做咗獨立**狀態樣本檔**。theme build 讀本節＝完整狀態 checklist。

### 23.1 狀態樣本檔清單（`_pXX`＝唔上線）
| 檔 | 狀態 |
|---|---|
| **`_p01-states-book.html`**＋`states-img/`（49 張截圖／50 格） | **全狀態型錄（review helper）**：50 格說明＋互動預覽；其中 49 格有 review snapshot（檔名跳 48），新增 `_p29` 付款待處理 helper 暫無 screenshot、以 linked HTML 為準。一頁碌晒所有狀態（含互動先出現嘅畫面：優惠碼被拒/套用、拒付、側滑車、搜尋、登入四 view、付款狀態等）。**交付優先次序固定為：本 Handoff → linked HTML → screenshot；如三者有差異，截圖不可推翻前兩者。Repo 冇包含 screenshot generator，唔准聲稱截圖已自動驗證或可一鍵重生。** |
| `_p13-shop-emptycat` | PLP 空分類（§22 空狀態 (a)；dataset＝canonical 減兩件助眠） |
| `_p14-pdp-guest` | PDP 訪客：原價、無 tag，只出一行「登入享會員價，此產品低至 HK$X」（§22.2） |
| `_p15-pdp-soldout` | PDP 整件售罄：gallery 售罄 badge＋主圖 `opacity:.6`＋灰掣「已售罄」`#cbb8a6`（§20.3；無補貨通知 input） |
| `_p16-pdp-early-guest` | 搶先鎖・訪客：badge＋原價＋§22.8 訪客句＋登入/註冊 CTA |
| `_p17-pdp-early-under` | 搶先鎖・未夠級會員：badge＋原價＋§22.8 未夠級句＋鎖掣「需 {級}」＋等級卡 link |
| `_p18-shop-guest` | PLP 訪客：卡原價無 tag、搶先卡「登入即可選購」（§5.D） |
| `_p19-cart-guest` / `_p19b-checkout-guest` | Cart／Checkout 訪客：無會員行、總計＝小計、有效碼直接套用 |
| `_p20-cart-birthday` | 生日月：§22.7 banner＋有效%＝max(tier10%,生日12%)＝12%（340→299.20 對齊 §22.7 例）＋行 label 不變 |
| `_p21-checkout-below-threshold` | 未達免運門檻：per-method 實價（無劃線）、運費行實數、nudge、**G12「價格已更新」re-confirm notice 樣本** |
| `_p22-orderreceived-guest` | 感謝頁訪客：無會員行、HK$575.00；不提供付款後開戶假入口，安全「查看訂單」連結由確認 email 嘅 Woo order-key CTA 提供 |
| `_p23-orderreceived-failed` | `PaymentEvidenceService=unpaid` review helper：只喺§5全部canonical Pay gates通過先用失敗單order-pay版式（紅 banner `#pay-error`語彙＋「重新付款」→同一張order-pay）；raw Woo `failed|pending|on-hold`本身不可顯示CTA。 |
| `_p29-orderreceived-payment-states` | 付款狀態 review helper：正式實作只按§5 semantic matrix；`PaymentEvidenceService=pending|unknown`一律中性頁、無成功tick／4步timeline／Pay CTA；只有service=`unpaid`、冇success／in-flight證據、同一order仍payable、hold／claim有效及全部server gates通過先可顯示原生Woo Pay並resume同一張order。Raw Woo `pending|on-hold|failed`不可自行揀畫面或action。 |
| `_p24-email-variants` | Email ②③④⑤**⑥補購**＋訪客(無折扣行)＋付運費/優惠碼贏 變體（六款 wrapper 齊；⑤ 固定「安排出貨」只係 ready 分支視覺，實作必須按 §21 履約狀態分支，§21.12/§23.2#15） |
| `_p25-account-states` | 一般會員(HK$0.00/0%)卡／頂級卡(無進度條)／超標 clamp／生日 banner／生日未設定時一次輸入樣本／空訂單／訪客訪問 my-account |
| `_p26-slidecart-search-states` | 側滑車 訪客／未達門檻 nudge／空車＋搜尋 loading／錯誤／截斷 |
| `_p27-pdp-preorder` | PDP 整件預訂：外框 terracotta badge＋到貨期行＋「預訂」掣（§19／§21.11） |
| `_p28-preorder-states` | 預訂其他位：PLP 卡／搜尋行 tag／結帳同意 checkbox／email＋訂單標示 |
| `_p30-cookie-banner` | Cookie 同意橫額（底部細橫條，書面語文案，§21.15） |

樣本檔內嘅 `window.HB_GUEST`＝**demo 專用顯示旗**（揀樣本狀態，非邏輯）— theme build 唔存在呢個概念，一律 server-side render。

### 23.2 合約補充（本輪修訂拍板，theme build 必守）
1. **送達 SLA（2026-07-08 客戶最終裁定）：客面唔 hardcode 任何送達日數。** SLA 只存在於**送貨政策頁**（admin 可編輯 page content）一處；Checkout 方式 radio 只出「方式名＋運費」（兩者＝WC shipping zone admin 資料，theme 唔加副題）；Order Received **無「預計送達」row**；PDP 唔出 SLA、亦唔出免運門檻句（points 行成行剷，免運消費位六減五 §22）。日後如要客面出 SLA＝先加 per-method admin 欄位＋客戶批文案，先准 render。Email `{shipping_method}` **只含方式名** — 唔准焊死 courier 名／SLA。
2. **Email 變體區域擴充**（§21.17 補充）：per-variant 區域＝eyebrow／標題／開場白／狀態條＋**狀態條 lead 句＋閉幕「如需取消或修改…」句（只出①）＋運費值＋送貨方式格**。`{discount_rows}` 三形：空（guest／0%／無碼→成行略過）／「會員優惠」（label 全站統一，07-10 裁定）／優惠碼 {code} — 二選一永不並存。自取點單 `{address}`＝自取點地址（plugin write-back §7）。`{date}`＝中文日期。
3. **客面日期格式**：全站「YYYY年M月D日」（訂單記錄／email／thankyou 一致）。**thankyou 詳情卡有「日期」row**＝`$order->get_date_created()`（喺訂單編號下；demo OR 三檔已示範 — 2026-07-10 裁定加）。
4. **counts口徑（2026-07-23客戶裁決）**：「共N件」「N件商品」＝**行數量總和**（`get_cart_contents_count()`／`get_item_count()`）；nav購物車數字＝WC cart fragments綁定。Account三條projection分開：**累積訂單**＝member-origin Woo order首次由`PaymentEvidenceService`確認`confirmed_paid|confirmed_zero`後，以`member_order_counted:{order_id}`永久計一次；付款重試／webhook重送只可計一次，之後退款、item cancellation、退貨、爭議或履約狀態改變都唔倒扣歷史count。取消／失敗／從未確認付款單唔計；`created_as_guest=true`嘅單日後即使客人以同一email註冊亦唔附加、認領或加入會員累積訂單。**累積消費**＝§5.C商品淨消費口徑嘅lifetime版本（成功退款即使喺第13個月仍減原單lifetime貢獻）；**近12月消費**＝同一口徑嘅rolling 12個香港曆月版本（tier基準；原單service首次確認時間離窗即貢獻0，之後退款不令rolling變負）。兩者排除運費／稅／fee及其明確退款，商品／未歸類成功退款後逐單clamp≥0。
5. **訂單歷史＝Woo order／order-item快照**：ship-to、運費、價錢、商品名、public variation、顯示SKU由Woo order/item擁有；HIBI只另存§19.1最少化stock owner、付款時split、承諾ETA、同意版本及pricing snapshot。付款成功後Order Received／Account／email只讀Woo order＋呢份immutable meta，唔跟現時預設地址、產品資料、門檻或價錢倒改。Readiness／ShipAny另讀current open demand（扣除已成功item-quantity cancellation），但不可倒改客人歷史文字。冇會員行嘅舊單＝落單當刻未夠級；「已退款 −HK$X」只讀provider succeeded後正規化嘅Woo成功退款額（可多次部分退）；追蹤meta未有就成卡藏。My Account嘅Woo原生「支付」action同樣只按§5 matrix：`PaymentEvidenceService=unpaid`、冇success／in-flight證據、同一order仍payable、hold／claim仍有效及全部server gates通過，先連去order-pay重付同一張saved-items／totals訂單；raw `failed|pending|on-hold`本身唔可放行。任何前置失效就無Pay action，並取消／expire舊單後要求重新checkout，唔重算改寫舊單。
6. **等級卡 derive 規則**：名／還差／target／fill％全部由（近12月商品淨消費, 下一級門檻）一對數 derive；還差 clamp ≥0、fill clamp ≤100%；非頂級卡只保留一行「距離…還差…」，禁止另加同義暖行；**Account 頂級卡無進度條**；英文名＝可選欄位（冇就唔出）；「頂級」＝之上冇 enabled 級（data-driven）。**總開關 OFF**＝全站原價之外，Account 概覽**唔出等級卡**（成個 VIP 面向隱藏）。生日 caption「生日月享會員生日禮遇」只喺該級生日%＞0 時出。
7. **Auth 合約**：忘記密碼回應＝中性句「如果呢個電郵有登記帳戶，我哋已寄出重設密碼連結」（唔透露帳戶存在）；登入現有密碼欄**無** client 端長度驗證；註冊／重設／變更密碼全部用 WooCommerce 原生 strength meter、提示及 validation，Theme 只做 skin，唔加固定「最少 N 位」label 或 `minlength`；註冊 consent 兩個名詞＝真連結（WC registration privacy-policy text option）。生日只儲月＋日、server 驗證合法組合（拒 2月30／31日），會員只可喺未設定時寫入一次，其後唯讀；更正只經有 audit 嘅 Admin 專用 action。已登入訪問 login URL → WC 原生 dashboard；auth 錯誤＝WC 原生 notices 經全域 skin。
8. **PDP variation 合約**：貨號＝selected variation SKU（跟容量或 `pa_scent_formula` 切換）；PDP 完整配方選項同 Shop `pa_scent` 原子 filter terms 係兩個獨立資料軸，唔准用其中一邊解析另一邊；「部分配方暫時缺貨」句只喺 ≥1 variation 缺貨時 render；使用示範只讀單一 YouTube 連結，**無片長欄或片長顯示**（§20.7）。
9. **搜尋 overlay production 三態**：loading（REST in-flight）／錯誤（fetch 失敗＝「暫時未能完成搜尋，請再試一次」＋重試，**唔准**當「冇符合結果」呈現）／截斷（>50 出「顯示首 50 件」disclosure）。**無「熱門」建議詞功能**（2026-07-10 客戶裁定剷走）。
10. **價格 filter/排序基準聲明**（§22『排序對照／PLP 分頁』重申）：filter／slider／「N 件符合」／price 排序基準＝**原價**（wc_product_meta_lookup、會員 price filter 之前）；卡面顯示會員價唔改 filter 基準 — 已為契約決定，唔好「順手」改。
11. **SEO head**：demo HTML 嘅 `<head>`（canonical→scentm.hk、Scent.M B2B og/description、假電話 LocalBusiness JSON-LD）**全部唔准照抄** — production 由 WP per-page 生成：product＝自身 canonical＋產品 og；my-account／cart／checkout＝noindex；LocalBusiness/Organization schema 讀真實 NAP 單一來源（沿 Scent.M inc/seo.php pattern）。
12. **會員價詞彙拆分**（§22 文案 1/5 重申）：價格 tag（卡/PDP）＝「{級}會員價」；summary/訂單折扣行＝「會員優惠」淨字（無級名、無章 §22.5）；未登入 PDP 提示＝「登入享會員價，此產品低至 HK$X」（HK$X＝該產品最低可購 variation 會員價），只可出呢一行，唔好再加全店折扣 teaser。
13. **Demo-only 元素（build 時剷走）**：Cart/Checkout 試碼快捷鍵 row（§6 禁 coupon-hint）、`DEMO_CODES` 固定結果、`HB_GUEST` 旗、Tier Cards 頁設計師註記、Checkout PICKUP mock 數據、全部 `_pXX` 檔。
14. **Checkout 補充**：**訪客登入會員價提示只出喺 PDP（§22.2）— Cart／Checkout／PLP 一律唔出登入推銷（2026-07-08 客戶裁定）**；checkout 保留 WC 原生「已有帳戶？登入」及 guest 專用、預設不剔嘅「建立帳戶」checkbox，並按 §17 固定 Accounts & Privacy 四項設定。空車訪問 checkout＝WC 原生 redirect 返 cart＋notice（經全域 skin）；揀自取點→地址欄由 plugin write-back 自動填（§7，客唔使自己填）。Phase 1商品發布驗證及Checkout server validation一律fail closed拒絕香水、酒精、易燃、受限制液體或其他危險品；**唔新增客面運輸提示或未定hazmat分支**，詳見§7。
15. **三套原先 deferred、現已正式納入 WP 實作範圍嘅功能規格已鎖定＋局部視覺 pattern 已提供**：預訂（_p27／_p28＋政策頁「預訂商品」段＋引擎 preorder branch；_p28 係狀態 pattern，唔係 Cart／Checkout／Account 全頁 mixed-order mockup）、補購提醒（PDP 購買區 panel＋Account「補購提醒」面板＋email ⑥ 喺 _p24）、GA4 cookie 同意橫額（_p30）。剩返 WP 側先做到嘅：GA4 plugin 接駁＋consent gate、Action Scheduler 排程、backorder 設定，及 2026-07-22 已鎖定嘅 `hibilab-commerce` 收貨／FIFO demand allocation／ShipAny gate（§19.1）— 按 §19／§21.11-12／§21.15 實作；未經客戶另行批准，唔新增或重畫全頁 UI。


---

## 24. 業界審計補遺（安全／效能／SEO）
- **HIBI order deletion guard**：Admin、bulk、REST、CLI及direct CRUD對`_hibilab_order_schema` order嘅trash／permanent delete全部server-side拒絕，只要未到`retention_until`、有legal hold，或仍有任何付款／退款／履約／shipment／email unknown／active responsibility。只有上述audited retention worker喺到期、無hold、無active責任後可用typed context做法定範圍內匿名化／清理；非HIBI order完整pass-through。Pre-delete boundary、HPOS on/off、繞過後observer incident、orphan custom-row foreign reference及allow／deny matrix全部入UAT。
- **可執行retention anchor同legal hold**：每張Woo order／HIBI audit嘅`retention_until`取所有相關可稽核事件（首次付款確認、每筆退款／爭議最終結果、最後shipment delivery／void、退換／客訴結案）各自時間＋7年之最大值；新事件只可向後推，唔向前縮。Legal hold係add-only/versioned order-level state，保存reason、actor、created/released timestamps及audit；Phase 1只畀真正網站管理員經WP-CLI／release runbook操作，唔新增未批准Admin畫面。任何喺Stripe／ShipAny／email／電話等外部渠道開始、而本系統未有canonical case row嘅退貨、換貨、爭議或客訴，營運runbook第一步必須以typed WP-CLI command喺相關order建立legal hold（必填case kind、外部reference、reason及actor），成功read-back後先繼續外部處理；結案時同一command以expected generation釋放並寫結案時間／reference，從而令產品刪除守衛及retention anchor有唯一可稽核來源。重覆建立／釋放idempotent；缺order、reference或generation就拒絕，唔准只靠自由文字order note代表active case。到期、無hold且無未完責任先以bounded cursor batch用Woo CRUD／privacy APIs匿名化法定毋須保留PII並清理HIBI可刪stubs；每批可resume、target隔離並有count/read-back。成功terminal job正文按既定30日、resolved failure／email event按90日；Mail Center verified receipt processed後90日清，unresolved保留至resolved再計90日。Retention worker／dry-run／restore UAT要證明唔破壞退款、對數或其他order。

> 本節係WP Dev必守嘅安全、效能、SEO及營運資料契約，必須同§§21–23及§25一併遵守。歷史審計snapshot唔代表WordPress實作已達production readiness；若有直接衝突，視為文件缺陷並提出修正，唔准自行估。

**A. 無障礙（客戶明確排除本次範圍）**
- 本交付唔以WCAG conformant為聲稱或release gate，亦唔要求WP Dev新增無障礙UI／互動。現有HTML／Woo原生語意不得因轉theme而刻意移除；日後如要正式無障礙改善，須另行界定範圍及批准UI改動。

**B. 安全（補 G1–G12 之外）**
- **Route-by-route authorization，唔准一條規則套晒所有 endpoint**：
  - 公開 filter／facet／price slider／搜尋只可做 bounded read：`permission_callback` 明確公開、參數 allowlist＋schema validation、最短 query／最大結果／rate budget、回應無 PII；public nonce 只防 CSRF／濫用，**唔當身份驗證**。
  - 側滑車 qty／remove 綁目前 Woo session＋cart item key，mutation 必須 nonce、server 重新驗商品及數量；唔接受 posted user/customer id。
  - 補購 opt-in／改排程／取消只限已登入會員，先驗 nonce，再以 `get_current_user_id()` 驗 reminder owner；任何 URL／body user id 只可作被核對資源，唔可決定身份。
  - Admin mutation 逐 action 用下述 capability matrix＋resource authorization；所有 POST／AJAX／REST 輸入先 schema validate，再 `sanitize_*`，輸出按 context escape。Direct URL、錯 nonce、換 resource id、低權角色全部列入 allow/deny integration test。
  - PII CSV／報表export係受保護read action：action-specific nonce＋§21.6 capability＋HIBI data scope；回應`Cache-Control: private, no-store`、UTF-8 BOM及formula-injection neutralization，唔接受URL傳入user scope。
  - Stripe付款webhook由官方WooCommerce Stripe Gateway endpoint同signing secret擁有；HIBI唔另開第二個raw Stripe writer endpoint。Resend webhook由site-wide Mail Center唯一擁有一個獨立public POST route：無WP nonce／session，必須用raw body同`svix-id`／`svix-timestamp`／`svix-signature`驗provider signature、限制body size；壞簽名回4xx。成功以`svix_id UNIQUE`寫入Mail Center最小verified receipt後先2xx，暫時落盤失敗回5xx；之後按exact RFC Message-ID／namespace路由HIBI normalized event並async配對outbox，唔靠event到達次序。
- 商家資料輸出按最終sink處理：PHP server-render嘅PDP Flexible Content／product_cat term fields／`pa_audience`衍生audLabel／spec一律文字用`esc_html()`、限定markup用`wp_kses_post()`、URL用`esc_url()`。搜尋REST JSON只做schema validation＋canonical raw value sanitization，**回傳前唔做HTML escape**；production client用DOM `textContent`及經驗證嘅`href`／`src` properties，demo `innerHTML`內插pattern唔准原樣搬去DB／REST-fed path。任何value全鏈只可喺最終render sink按context escape一次。

**C. 效能（補 §20.9）**
- **Asset-scoping（客最關注嘅「慢站」防線）**：非 `/hibi-lab/` 且非 WC journey 頁（cart/checkout/my-account）一律 dequeue `woocommerce-general/layout/smallscreen`、`wc-blocks-*` styles、`wc-cart-fragments`、order-attribution scripts；store-scoped stylesheet＋store-chrome.js 只喺店頁 enqueue（brochure 頁 Woo 重量＝0）。
- 圖片交付（補 §20.9）：product/category 圖經 `wp_get_attachment_image()`（自動 srcset/sizes/`loading="lazy"`）；首屏 hero/banner `loading="eager"`＋`fetchpriority="high"`；保留 4:5 aspect-ratio wrapper 防 CLS。
- 字體：production 只載實際用到嘅 family（demo 每頁 request 5 個、Mea Culpa 只 scentm-home 用）；Google Fonts preload＋`display=swap`；FontAwesome 改 subset／self-host（demo full CDN CSS 屬 throwaway）。
- 搜尋 REST endpoint：固定讀§25.2 policy嘅debounce `250ms`、query `2–50` grapheme、abort stale request、每次最多`50`結果。Shared object cache只准存matching product IDs同公開、user-neutral facts（taxonomy、公開庫存狀態）；**有效會員價、生日價、VIP override、會員搶先資格、customer/cart/order資料永遠request-local即時計算，唔進shared cache，亦唔cache完整personalized response。** 登入會員／有Woo session嘅相關REST／AJAX response一律`Cache-Control: private, no-store`；cart、checkout、my-account、order-pay、order-received沿用Woo／WP Engine cache exclusion。公開cache generation喺產品／variation新增、更新、刪除，以及stock、backorder、visibility、catalog price、media、相關taxonomy／term／relationship任何改動時失效；VIP／refund／override只影響request-local projection，毋須維護per-user cache矩陣。Launch test用Guest、一般會員、兩個不同VIP及兩個同時session查同一query，驗證價錢、資格、cart同PII零交叉。

**D. SEO/GEO（補 §14/§23.2#11）**
- Meta：每 template 預設 title 公式（`{product} — HIBI LAB` 等）＋description（產品短述／ACF 分類 intro）＋per-post/term override 欄（沿 Scent.M seo.php pattern）。
- Sitemap/robots：WP core sitemap（或 theme filter）scoped 到 products／product_cat／可索引 `/hibi-lab/` 頁；排除 noindex 端點＋flip 前 noindex URL；robots disallow `/?add-to-cart=`、`wc-ajax`（唔准 Disallow 需要 crawl 嘅 noindex 頁）。
- 搜尋頁 `?s=`：`noindex,follow`（結果內容異於 base archive，單靠 canonical-to-base 唔可靠）。
- OG/Twitter per-template：product（og:type=product＋featured image）／分類 archive（ACF banner）／landing·policy（品牌預設圖、ACF option 可換）／site_name·locale／`twitter:card=summary_large_image`（WhatsApp 分享靠呢個）。
- 圖片 alt 鏈：媒體庫 alt→產品/文章 title fallback；裝飾 chrome 圖 `alt=""`；ACF 圖欄配對 alt sub-field；launch checklist 提醒上載相帶 alt。
- Structured data（GEO）：Woo core係Product／Offer／Breadcrumb JSON-LD唯一owner；HIBI只經Woo官方structured-data filters擴充同一個Offer，絕不輸出第二個Product object。`shippingDetails`必須由Woo shipping zones／實際可用method rates，加上驅動同一可見Shipping政策嘅地區／費率／送達資料共同產生；免運門檻只係其中一個欄位，唔足以單獨生成完整schema。任何required資料不足就省略`shippingDetails`，唔猜。`hasMerchantReturnPolicy`喺未有可驗證嘅結構化政策來源前省略，唔可以由「7日」一句文案猜出退貨方式、費用或例外。只有可見`pdp_blocks`內存在最少一組通過§15驗證嘅`faq` Q/A時，先由**同一份已sanitize內容**輸出恰好一個`FAQPage`；零有效Q/A就省略，theme／plugin不得另出第二份。Product／Offer JSON-LD、product feed及sitemap價格／availability永遠只用公開catalog（regular／有效公開sale）同公開庫存狀態；唔因登入會員、VIP／生日價、coupon、cart或session改值，亦唔將個人化價寫入shared cache。UAT以guest、兩個不同VIP及生日月session讀同一URL，schema必須byte-equivalent而可見客面價仍各自正確。

**E. 電商完整度／表單（補契約）**
- **Checkout資料對照（2026-07-29客戶裁決2A；只改Checkout mapping，保留既有Account兩張地址卡）**：Checkout可見欄固定係**單一「名字」→ `shipping_first_name`、單一「電郵」→ `billing_email`、單一「電話」→ `billing_phone`**。Server先驗證名字，再將同一值copy入`billing_first_name`作canonical contact／payment snapshot；無第二個名字欄。`billing_phone`係聯絡／courier電話唯一持久datum，唔另存`shipping_phone`。客面可見「送貨地址」其餘欄固定寫Woo `shipping_*`：`shipping_country=HK` hidden、四區寫`shipping_state`、詳細地址寫`shipping_address_1`；`shipping_city`、`shipping_address_2`及`shipping_company`喺Checkout移除並unset required。Returning customer預填先讀已保存shipping fields，個別shipping欄空先fallback同名billing address欄作今次表單預填；submit後仍只保存shipping destination，除上述名字copy外唔把送貨資料反寫billing。送上門submit驗呢組shipping資料；自取點由所pin ShipAny public write-back填同一組shipping snapshot，gateway前save＋read-back。Order Received、訂單Email、ShipAny及FulfilmentAddressService全部只讀該單shipping snapshot。**Account維持現稿兩張卡**：Billing Address用Woo原生`billing_address_1/state/...`並可獨立編輯；Shipping Address用同一組canonical `shipping_*`並可獨立編輯；兩者唔互相覆寫。電話仍只讀寫`billing_phone`。
- HK 電話：8 位驗證（strip +852/空格/dash 後）經 `woocommerce_checkout_process`＋zh-HK 錯誤（全域 notices skin）；feed ShipAny SMS。電話 canonical＝`billing_phone`（Account 設定讀寫同一 datum、三處同步）。
- autocomplete token 全站一致（email/tel/cc-*/bday/street-address/address-level1）；Account 地址·設定照抄 Checkout token set。
- 密碼規則單一來源（已批准）：使用 **WooCommerce 原生 password strength meter／validation**；theme 只做品牌 skin，同步顯示原生規則提示，唔另設 option-backed minimum length、custom validator 或第二套 `minlength` 規則。變更密碼三欄維持 Woo 原生 leave-blank 行為，有輸入先由同一原生規則驗證。
- **補購提醒 persistence＝一個 canonical table，Action Scheduler只係wake-up mechanism**：logical table `hibilab_reminder`保存`id`、`user_id`、`product_id`、nullable`variation_id`、schedule mode/value、anchor／`next_due_at_utc`、status、generation、last order ID及timestamps；實體表名經`$wpdb->prefix`；DB`UNIQUE(user_id, product_id)`保證每會員每產品只得一項，variation只保存目前一鍵再買目標。Account直接讀此表，唔用user meta做第二份真相。新增／改期／暫停／恢復／取消／購買重錨全部經同一service＋optimistic version check；commit後best-effort取消舊action並建立帶`reminder_id+expected_generation`嘅one-off action，舊／重複generation即no-op。Worker重讀canonical row及owner/status/due time後，以`reminder:{id}:{occurrence}` semantic key建立email send；成功落盤一次先推進下一個真曆日。短月按月尾；停機錯過多期只合併成一封目前提醒，唔連環補寄。固定dispatcher每日核對到期但冇有效action嘅row並補排；產品下架／刪除／不可購買時唔寄dead link，會員取消／privacy erasure時取消actions並刪row。前台維持onchange auto-save＋inline「已更新 · 下次提醒 {date}」；PDP checkbox剔咗先enable排程select。 主要dispatcher index固定為`(status,next_due_at_utc,id)`。所有「加N個曆月」先以`Asia/Hong_Kong` local date做calendar subtraction／addition及month-end clamp（包括2月29日），再轉UTC保存；唔用固定30日或UTC日期直接運算。 所有到期日以香港時間當日10:00作固定send target（唔加Admin option）；停機恢復後只補一封仍有效嘅目前提醒，唔連環補過期信。
- Related products（PDP demo有）：分類context完全依§3 deterministic canonical leaf；先同leaf、無結果先direct parent，候選產品按Woo `menu_order`升序再product ID升序，cap 4並排除本品／不可見／不可購買項，重用`content-product.php`卡（會員價／庫存badge自動一致），無則整區隱藏。禁止用`get_the_terms()`第一項或隨機順序，資料歧義由§3 Admin警告處理，唔加主分類UI。

**F. 法律／信任（HK；政策頁內容一律 admin 可編輯 page content，客自行撰寫）**
- **生日祝賀 email（客戶裁決 2026-07-11：綁去 marketing opt-in；2026-07-27 補寄選 B）**：只寄俾已剔「接收優惠及新品消息」嘅會員（§21.16 subscriber）；§5.B／§22 email spec／文案 #9 據此改；email 內附退訂連結。 Birthday dispatcher喺 enqueue 同真正 send 前都要重讀 canonical consent 及最新 suppression generation；已退訂／已刪除／job版本舊過 revocation 即 no-op，唔可以靠排程當日嘅舊快照寄出。固定於生日月1日香港時間10:00排送（唔加 Admin option）；每次 enqueue／retry／reconciliation 共用 durable semantic key `birthday_greeting:{user_id}:{YYYY-HK}`，DB UNIQUE 保證同一會員同一香港曆年最多一封，成功或永久失敗後唔重開同一 generation。排程延遲只可補一封當月仍有效嘅祝賀。會員喺生日月1日之後成功設定生日或成功取得有效marketing opt-in時，寫入嗰個canonical service同一transaction commit後即做catch-up eligibility check並排**本月一次**；另由每日香港時間10:00 reconciliation補任何enqueue-before-crash遺漏。enqueue及真正send前仍重讀生日、consent、suppression及同年度semantic key，已寄／已排、已退訂、月份已過或帳戶已刪除全部no-op，絕不補寄過去月份或同年第二封。
- Terms 網店段（launch-gate，admin 撰）：訂單接受、標價錯誤、預訂付款/延誤/退款、VIP 條款修訂權、coupon 規則、產品責任；同 Privacy 一樣客喺 theme dev 期間寫。
- 預訂延誤（charge-now＋無限接單）：政策頁預訂段（admin 可編輯）加「如到貨顯著延遲，會通知並可全額退款取消」；內部 ops：延遲→通知＋提供退款／取消。取消未寄出商品必須喺 Woo refund 畫面指定 item quantity；有實收款訂單只喺官方 Woo Stripe Gateway 確認退款成功後，先由同一個 idempotent projection worker關閉相應 demand並按 Woo 已確認嘅 restock 結果更新履約。HK$0.00 訂單唔接觸 payment provider，直接用 Woo 原生取消／退款記錄及 item quantity 更新同一 projection。唔提供獨立「已收錢但不退款只取消履約」正常 action，亦唔可以只退銀碼而留低仍待出貨嘅 obligation。實物損壞／遺失另做有原因 correction，唔借取消流程暗中扣貨。 實際延誤通知沿用Woo原生、品牌化`customer_note` email，由Admin喺order加入customer-visible note觸發；唔另開第7款自訂email或新通知狀態，亦唔改已批准前台UI。
- 退貨運費（客戶裁決 2026-07-11：admin 遲啲自行喺貨運政策頁撰寫）：**我方不寫死 copy，只確保 Shipping/Returns 政策頁＝admin 可編輯 page content（現已是）**；返回郵費責任／原運費退款規則由客上架時填。
- Marketing退訂機制：每封marketing send都帶不含電郵明文、不可猜測兼驗簽嘅退訂token。**兩條入口分開**：客人撳可見link可先GET開確認頁，再POST完成退訂；email client嘅`List-Unsubscribe-Post: List-Unsubscribe=One-Click` endpoint則必須直接接受標準one-click POST，唔插確認頁。兩者都idempotent並只resolve canonical subscriber ID，再呼叫現有`scentm_subscriber_erase()`，由既有service清除canonical／legacy／historical-ingestion reference；**現有資料模型冇active flag，唔准invent「flip subscriber flag」或直接SQL刪row。**重複／已退訂請求以成功式中性回應處理，token無效只顯示中性錯誤；後台沿用現有「移除訂閱者」action。退訂全鏈入launch checklist。 Canonical service同時維護email-scoped、versioned revocation／suppression generation；任何較舊queued opt-in都不可令退訂復活，日後重新訂閱必須有較新明示consent evidence。退訂token頁及one-click endpoint唔載GA、YouTube或其他第三方script，並設`Referrer-Policy: no-referrer`、`Cache-Control: no-store`。
- 產品聲稱審查：功效欄旁加 admin note（港式書面語 §21.6）警告勿用醫療/療效字眼；客上架前確認「消炎水」用詞（例改「舒緩」）。
- 7 日退換 vs 法定權利：政策頁＋Terms 加保留句「以上安排不影響閣下於香港法例下可享有之權利。」
- **自訂資料生命週期（PDPO＋Woo privacy tools）**：`hibilab-commerce`註冊`wp_privacy_personal_data_exporters`及`wp_privacy_personal_data_erasers`，以會員ID／已驗證email匯出或處理VIP override／projection、補購提醒、preorder demand、Action Scheduler/outbox及email delivery metadata，輸出用人可理解欄名。Eraser即時取消提醒actions並刪reminder；刪VIP override／可重建projection；訂單、付款、退款、庫存／履約audit如仍屬法定業務記錄則保留最少必要reference並匿名化非必要PII，唔破壞Woo order／refund關聯。
- **Privacy callback ownership／分頁**：HIBI只註冊自己擁有資料嘅WordPress Privacy callbacks；Woo order／customer資料仍由Woo core exporter／eraser擁有，HIBI唔重覆輸出或直接刪Woo finance rows。每個callback用stable item name、§25.2固定`100`row batch、stable `(id > cursor)` pagination及官方正確`done`回傳，只按已驗證user ID／normalized email取target，重跑idempotent。Action Scheduler args只放internal row ID＋expected generation，禁止email、電話、地址或render payload。Exporter以人話分VIP／生日、提醒、預訂履約及通訊metadata；唔輸出secret、raw provider payload、order key或其他人資料。Eraser取消target actions，刪reminder／生日／marketing projection，匿名化非必要outbox／event PII；法定保留audit只留最少去識別reference並喺callback message解釋。刪除後任何reconciler／job都唔可由舊snapshot復活PII。
- **固定retention**：Woo訂單及必要財務／退款／履約audit按香港業務記錄需要保留7年，期滿且無legal hold後匿名化／清除；成功terminal job payload 30日後清正文，只留semantic key、provider reference、狀態、order ID及時間；已解決failure／email delivery log保留90日；processed／matched shipment observation保留90日後清除，unmatched／retry_due／`needs_attention`留至收斂，長期audit只留order projection上最少provider reference／狀態／時間；Resend raw webhook body完成normalize後唔長期保存。官方Woo Stripe Gateway嘅raw payload／log完全跟該plugin自身設定及retention，HIBI唔擁有、複製或聲稱清理。補購提醒取消即刪；marketing consent／退訂沿用Scent.M canonical subscriber lifecycle。Active會員為提供Account lifetime訂單／消費紀錄所需嘅最少order↔member關聯可喺帳戶存續期間保留並喺Privacy policy披露；帳戶erasure／關閉、服務終止或關聯不再必要時即移除並匿名化，法定財務audit只留去識別化必要資料。禁止另建一份不可匯出／不可刪嘅永久aggregate。
- **PII／secret redaction**：production application／PHP／provider log不得寫API key、signing secret、Authorization header、client secret、order key、退訂token、完整email／電話／地址或email正文；顯示只可mask，correlation用internal UUID／provider ID。Production關debug log；raw diagnostics只畀真正網站管理員。Privacy policy說明live erasure同加密backup按固定週期淘汰；restore後重跑erasure tombstone／suppression reconciliation，避免已刪資料復活。 `order_key`、unsubscribe token及其他敏感query必須喺analytics送出前移除，server access／application log只記redacted route＋correlation ID；含token頁設`Referrer-Policy: no-referrer`、`Cache-Control: no-store`且唔載第三方script，避免經referrer、GA或cache外洩。

**G. 明確唔做／已記錄（補齊「每個 do/not-do 都記低」慣例）**
- **棄置購物車/結帳挽回＝明確唔做**（客戶裁決 2026-07-11）。
- 低庫存數量顯示（「只剩 N 件」）＝唔做（§19 三態足夠）。
- Gift options（包裝/賀卡/隱藏價錢單）＝唔做（`order_comments` 承接）。

**H. 文檔手尾（本輪已修）**
- §23.1 states-book 已對齊為 49 張既有 review 截圖＋1 個未截圖付款狀態 helper＝50 格；_p24 行補明 ⑥；_p24 header「⑥補購 DEFERRED」→ locked；§22.13 去「— {產品}」；_p24 comment「六個消費位」→「五個」；§23.1／§23.2舊section／line reference已修正；§20.4 現有語意屬性範圍已記錄。
- HIBI 客面第一人稱統一「我哋」（documented default）：Shipping「我們樂意」→「我哋樂意」、Landing/index hero「我們相信」→「我哋相信」（共通 Scent.M footer/menu 維持原樣）。
- PDP 手機 sticky bar 品名 production 綁 `get_the_title()`（同 H1）、demo 短名改回全名。

---

## 25. 營運、供應商與交付契約（WP Dev必守）

### 25.1 Stripe單一owner及環境隔離
- HIBI付款／PaymentIntent／退款／webhook raw payload由**官方WooCommerce Stripe Gateway唯一擁有**；`hibilab-commerce`只讀Woo order、refund及公開gateway outcome，唔另開Stripe writer endpoint、唔直接建立／更新／退款第二個Stripe object。Workshop自訂endpoint繼續使用自己嘅metadata namespace及signing secret，並只喺該endpoint對已驗簽但唔屬自己namespace嘅event安全2xx略過；Woo官方Stripe endpoint、payload parsing、response及重試完全由官方gateway管理。HIBI metadata只可經manifest所pin版本嘅公開filter加入作內部scope，絕不可成為gateway付款正確性前提。
- Test／live官方Woo Stripe connection、Stripe account ID、official endpoint及signing secret完全分開，冇cross-mode fallback；HIBI唔提供或保存一套手填Stripe API-key欄。**Local係deterministic development contract**：自動test只用versioned fixtures／official gateway adapter mocks覆蓋成功、3DS、拒絕、pending、unknown、退款及重送，Stripe Dashboard不得保留指向`.local`／localhost嘅permanent webhook；開發者要手動真Stripe smoke時，只可臨時用Stripe CLI或有時限HTTPS tunnel，完結即刪endpoint／secret，唔將結果當release證據。**Staging先係provider integration gate**：用公開HTTPS、獨立Stripe sandbox/test connection及official gateway endpoint跑付款、3DS、browser中斷、webhook重送／亂序、全額／部分退款、Radar／dispute同reconciliation；**production**只用獨立live connection。DB clone後先令HIBI mutation fail closed，清除來源環境connection binding，再喺目標環境重新Connect／reauth、驗hostname、account、mode、endpoint health及相應smoke，未通過唔可開收款。Secret唔入Git、唔落前端、唔寫log。正銀碼Order喺首次付款確認時凍結官方gateway ID、Stripe `account_id`、`livemode`、`HKD` currency及原始PaymentIntent／Charge transaction chain；退款／復原逐項對返呢個歷史tuple，唔用目前後台帳戶代替舊交易身份。HIBI唔創造第二套payment identity。
- **Stripe帳戶切換（2026-07-29 客戶批准 B）**：同一環境／mode同一時間只可有一個officially connected account；HIBI唔保存多套OAuth／API secret、唔做organization key、Connect platform或multi-account router。工作坊未完、Woo訂單未完成或仍有歷史訂單本身唔阻止切換，但真正網站管理員必須經受控maintenance流程操作。開始後先將public ready設false、關閉新ingress：**全站所有使用官方Woo Stripe Gateway嘅Checkout、order-pay、付款、退款及其他provider mutation一律fail closed**，包括非HIBI Woo訂單，避免暫連舊帳戶期間收去錯帳戶；Woo產品／訂單read-only頁可照常，使用獨立workshop endpoint／credential嘅Scent.M報名收款不受影響。呢個全站Woo Stripe暫停範圍係單一official connection方案嘅硬限制，唔可縮窄成只封HIBI。
- **Maintenance-safe收斂而唔自鎖**：ready=false只禁止新付款入口及一般outbound writer；固定allowlist仍容許①官方gateway已驗證incoming outcome持久化，②不再發起provider mutation嘅本地PaymentConfirmation／Refund／Dispute projector及bounded read reconciliation，令maintenance前已開始嘅付款／退款可安全收斂，③真正網站管理員經action-specific nonce、窄capability及version CAS批准嘅**一個exact-scoped operator operation**。正式切換credential前，所有非目標Checkout／PaymentIntent及provider write lease必須收斂；`unknown`付款、身份不明退款或未有精確provider identity嘅money ambiguity仍阻止。已用精確provider ID持久化、冇write in-flight嘅pending退款／爭議可保持order-scoped incident並按下文日後再核對，唔需要令全站停幾日；今次reauth正要處理嘅target incident亦可被精確pin入coordinator，唔會用自己個`needs_attention`反過來阻止進入maintenance。時間只判SLA，唔猜provider結果。
- **最小、可續跑connection coordinator**：只保存一份nonsecret、versioned `stripe_connection_generation` state（正常`account_id`／`livemode`、phase、temporary target account、nullable target order／refund operation／allowed action、started-by／timestamps、checkpoint/version、nullable `blocked_from_phase`／`resume_phase`及pre/postcondition hashes），不得複製gateway token、signing secret或歷史帳戶credential。所有寫入由上文唯一`hibilab_site_guard`（`active_kind=stripe_connection`）序列化：guard短transaction內核對exact active operation＋expected coordinator version，transaction外先經WordPress公開Options API寫入non-autoload option，再重取同一guard read-back完整schema/value/hash先承認checkpoint；Options row唔充當mutex，亦唔另造option/advisory/transient lock。呢份state係Scent.M theme sentinel同HIBI core共讀嘅唯一持久site-wide marker；任何缺欄、decode／read-back失敗一律視作非`ready`，theme不得另存第二份marker。Deactivate、files missing、upgrade及一般cleanup不得刪除或重設佢；任意PHP fatal嘅真實邊界依§22 Missing-core sentinel，不宣稱fatal request仍可執行guard。一般Stripe-related operation／worker開始及每次side effect前都重讀ready flag、generation、目前官方gateway account／mode及訂單immutable tuple，不吻合就no mutation、保留canonical工作並開incident。只有上段allowlist嘅incoming／local truth convergence，或tuple逐項吻合`{target order, operation UUID, action, account, mode, generation}`嘅單次官方gateway操作可越過maintenance gate；任何一項缺失、另一張單、重送或兩位Admin並發都拒絕。中途request／PHP／browser crash只由同一guard active identity＋coordinator checkpoint續行；未完成、連錯帳戶或管理員離開畫面時維持maintenance，絕不自動恢復收款。
- **Coordinator phase graph（唯一合法狀態機）**：`phase`只准`ready|draining|connecting_target|verifying_target|reconciling_target|restoring_home|verifying_home|blocked`。正常edge只准：`ready→draining`；`draining→connecting_target`，或者只喺未disconnect／未有provider mutation而同一operator明確取消時`draining→ready`；`connecting_target→verifying_target`；`verifying_target→reconciling_target`；`reconciling_target→ready`（永久cutover完成）或`reconciling_target→restoring_home`（舊帳戶單次售後完成／已保存exact pending truth）；`restoring_home→verifying_home`；`verifying_home→ready`。任何非ready phase `X∈{draining,connecting_target,verifying_target,reconciling_target,restoring_home,verifying_home}`遇到不可即時收斂嘅failed／ambiguous postcondition都只可`X→blocked`，原子保存`blocked_from_phase`、`resume_phase`、checkpoint、pre/postcondition hashes及current observed connection。`blocked`唯一出路係`blocked→resume_phase`，而`resume_phase`只准上述六個非ready phase、永不准`ready`：draining失敗→draining；connect未發出→connecting_target；connect已發出或結果不明→verifying_target；target reconciliation失敗→reconciling_target；restore未發出→restoring_home；restore已發出／結果不明或home verification失敗→verifying_home。Retry前重讀官方connection truth；retry再失敗就留`blocked`並只更新同一checkpoint/incident，只有相應verification/reconciliation完整postcondition成功先沿正常edge推進。每次轉移都先用唯一`hibilab_site_guard`核對active operation＋version並durable保存phase、expected current/target account、mode、operation UUID及precondition hash，釋放短lock後先做官方UI/provider side effect，再重取guard read-back postcondition；side effect後crash只查真相，唔重做mutation。`ready`必須target／blocked欄全空、目前官方connection精確等於normal tuple、cleanup/transfer proof（永久切換時）、bounded reconciliation及health/smoke全部成功；否則留原phase或入`blocked`。Graph schema、每條合法／非法edge、每phase crash/resume及兩Admin並發都要contract test，Dev不得新增時間到ready或大概完成分支。
- **新帳戶正常cutover及account-scoped資料清理**：preflight全綠後由官方Woo Stripe `Disconnect／Connect／reauth`介面接新帳戶；coordinator read-back核對精確`account_id`／mode、Payment／Payout／Webhook／Sync health及官方endpoint。Woo官方指出customer／source／card identifiers及Woo payment tokens屬單一Stripe帳戶，換帳戶唔清理可令付款失敗（[Woo官方database cleanup指引](https://woocommerce.com/document/stripe/customization/database-cleanup/)）。所以永久正常account A→B時，先向Stripe確認資料可否受支援轉移；無正式轉移就按compatibility manifest盤點所pin gateway全部account-scoped user meta及`gateway_id=stripe` payment tokens，用versioned、idempotent、checkpointed migration經Woo／WP公開CRUD優先清理，只有官方文件對所pin版本要求而無公開CRUD嘅最小部分先可用`$wpdb->prefix` bounded migration；先backup、逐批count/read-back、不可刪order transaction／歷史付款tuple，零舊identifier先可開B。每次B→C同樣再做；**暫時reauth舊帳戶做售後絕不執行cleanup**。之後完成bounded read reconciliation及sandbox smoke；只有新generation、cleanup／transfer proof及全部read-back通過先原子設正常account＋ready。失敗維持Woo Stripe maintenance並顯示incident，唔fallback舊key。
- **舊帳戶售後maintenance reauth**：舊帳戶訂單日後要退款、核對或處理爭議時，從該訂單immutable `account_id`／`livemode`精確選目標並pin exact operator operation；先關閉全站Woo Stripe ingress、用maintenance-safe allowlist收斂非目標in-flight工作，再只用官方Woo Stripe流程暫時reauth嗰一個舊帳戶。客面仍不可用Woo Stripe付款；Admin只可執行pin住、歷史tuple同目前暫連account／mode完全相符嘅單次操作，其他writer保持禁止。Provider call完成後，operation必須已持久化精確provider identity及當刻truth：succeeded即投影完成；如屬可長時間pending／open但冇write in-flight，就保留order-scoped incident、鎖住該單後續money mutation，唔使全站長期停機。離開舊帳戶前先對佢跑bounded read reconciliation並保存checkpoint，再接回記錄中嘅正常account；接回後核對正常account webhook／gateway health並跑佢嘅bounded reconciliation，最後先原子ready。遲到／重送event只按immutable account＋mode＋provider ID去重，唔信次序；舊帳戶未連線期間唔假設網站仍收得到佢嘅webhook。
- **舊帳戶事件發現係明示營運契約**：流程可重覆套用第1、第2、第N個歷史帳戶，目標永遠由訂單凍結tuple取得，網站唔另存credential/router。每個仍可能有退款／爭議／對數責任嘅舊Stripe帳戶必須保留MFA登入，開啟Stripe原生dispute、refund、security同webhook-failure通知去一個每日營業日有人查看嘅共用營運信箱，指定primary＋backup owner並於1個營業日內ack；每週另喺各舊Stripe Dashboard核對未決dispute／refund／webhook health，唔靠網站poll。收到alert、發現外部退款或任何old-account mutation後，owner即按incident runbook進入上段maintenance reauth＋canonical reconciliation；未完成前相關order維持可見incident及禁止後續money／ShipAny推進。帳戶失去登入、官方gateway無法reauth或pin版本無可驗證truth時保持`needs_attention`，絕不猜。正式關閉舊帳戶前要證明已無active operation、未決爭議、退款責任或其他資金義務；歷史付款tuple仍按retention保存。
- **切換UAT／回歸**：固定覆蓋A→B正常切換、存在pending Checkout／unknown付款／write-in-flight退款時拒絕、maintenance仍可收斂已成功但未投影付款、target incident唔自鎖、錯account／mode／order／operation／generation拒絕、暫連A期間非HIBI Woo Stripe付款同退款亦被拒絕、**暫接A途中core deactivate／files missing／Recovery Mode而WordPress仍載入theme時，非HIBI Woo Stripe仍被sentinel拒絕；另獨立注入bootstrap fatal，驗request終止、外部monitor告警、rollback後同一operation收斂，唔把fatal寫成sentinel能力**、interlock缺失／損壞／讀取失敗亦fail closed、B正常運作期間暫接A做部分退款再接回B、pending退款記精確ID後安全回B再稍後接A收斂、永久A→B清理舊customer/source/card IDs及payment tokens但保留order transaction、暫時reauth不cleanup、A→B→C後仍可按A訂單精確暫接A、**A已斷線後新dispute→Stripe原生alert→ack→reauth A→final truth→接回正常account**、每個phase crash/retry、兩位Admin並發、舊generation worker及遲到／重送event。每格驗證零重覆扣款／退款、零跨帳戶mutation、零靜默失信及Scent.M workshop不受影響。呢套只係罕用受控runbook，唔新增客面UI或多帳戶管理Dashboard；沿用官方Woo Stripe連線頁、既有System Status／maintenance notice及同一capability matrix。
- API／webhook version由所裝官方Woo Stripe release支援嘅版本決定並記入compatibility manifest；HIBI唔用硬編日期字串拒絕已驗簽event。Gateway／API升級先喺Stripe sandbox跑付款、3DS、browser return中斷、webhook重送／亂序、全額／部分退款及reconciliation regression。
- HIBI normalized side effect以Woo order/refund ID＋transition kind或durable row semantic key去重；唔信event順序，處理前重讀Woo及所pin gateway可驗證truth。Browser return唔係付款真相。不可預設官方gateway有通用「定期reconciliation」；只有manifest文件及staging failure test證實嘅sync/retry能力先可依賴，否則browser-return中斷／webhook遺失仍係launch blocker，或另加只讀、受支援嘅order reconciliation，絕不另建Stripe writer。

### 25.2 Action Scheduler／失敗可見性
- **所有HIBI自有營運數字由一個immutable owner提供**：`HibilabOperationalPolicy`係code-committed、versioned typed config，唔係Admin option；implementation其他class、JS同runbook只可引用佢或由build輸出嘅同generation projection，禁止再散落magic number。Woo「Hold stock」到期只讀§21鎖定嘅Woo原生setting；Resend idempotency有效期只讀所pin provider contract，兩者都唔抄入呢個policy變成第二份真相。Phase 1固定：DB／job ownership lease=`5分鐘`、lock acquisition max=`5秒`（provider network call永不放喺DB lock內）；HTTP connect timeout=`5秒`、total timeout=`15秒`；retryable work最多`6`次、base=`60秒`、factor=`2`、cap=`3600秒`並用full jitter（`unknown`只重查真相，唔重做provider mutation）；payment／refund／shipment／email reconciliation每`5分鐘`、每批最多`50`row、單次wall-clock budget=`20秒`並保存stable cursor；accepted email未有delivery observation嘅SLA=`30分鐘`；public webhook raw body max=`256 KiB`、signature timestamp tolerance=`300秒`；搜尋debounce=`250ms`、query長度`2–50` grapheme、每次最多`50`結果；privacy／migration／retention batch=`100`；補購提醒Admin列表=`20/頁`；critical dispatcher heartbeat每`5分鐘`、遲`15分鐘`告警，VIP每日核對遲`36小時`告警，生日／補購每日10:00 dispatcher遲`1小時`告警；外部health monitor每`5分鐘`檢查、連續`2`次失敗先alert、連續`2`次成功先recovery。Lease／SLA／attempt用盡只代表需要reconcile／incident，**永遠唔裁決付款、退款、出貨或email結果**。Policy schema、值同所有consumer有單元／contract test；改值只可跟code release及changelog。
- **Reactivation先追真相、後ready**：plugin停用期間Woo付款／退款／status同ShipAny provider truth仍可能變，唔可只由HIBI tables重建wake-ups。重新啟用後保持HIBI mutation fail closed；以persisted high-water至啟用一刻captured high-water，checkpointed／分頁掃描全部scoped Woo orders、refunds及shipment public truth，經同一PaymentEvidence／Refund／Shipment projectors收斂或逐筆開incident。Scan及read-back全部完成先原子設ready，再由canonical rows重建allowlisted wake-ups；中途crash由cursor續行，唔重call provider。
- Action Scheduler係唯一background執行queue；canonical email／receipt／reminder資料仍保存喺各自細型durable row，唔以scheduler action當第二份業務真相。每個handler idempotent，外部call、backoff、attempt及batch一律讀上文policy。時間只判斷工作遲到，絕不用timer猜付款／退款／出貨結果。**Exhausted scheduler／job execution failure**保留canonical row最後可證明state及correlation ID、masked provider reference、last error／next action，並開exact reason-scoped incident，喺Woo order／System Status／WP notice可見；provider明確宣告嘅email `permanent_failure`仍係該outbox row terminal delivery state，只另外開incident／suppression，唔改寫成另一個email state。
- VIP每日核對、preorder reconciliation、reminder dispatcher、email及subscriber opt-in各有獨立group、heartbeat及oldest-due監察；同一故障只建立一個incident，恢復後resolve。Action Scheduler停機只延遲工作，唔改Woo order、reminder、demand或付款真相。
- **Action Scheduler lifecycle**：等Action Scheduler初始化完成先註冊／核對actions；activation、upgrade及每日ensure共用同一idempotent scheduler registry。Deactivate只按HIBI allowlisted hooks／groups取消pending／recurring wake-ups，唔刪outbox、reminder、demand、receipt、event row、order meta、角色或capability，亦唔碰Woo／第三方actions。Reactivate／upgrade maintenance期間**只**排UpgradeCoordinator、Mail Center ingress drain及高水位catch-up；business handlers一律no-op而唔自行重排。全部catch-up/read-back通過、ready原子flip成功後，registry先一次過由canonical rows＋generation重建business wake-ups；marker write fail就仍maintenance並只重跑coordinator，避免「preflight後早排、handler見未ready no-op、之後永遠冇人再排」。舊／race action每個side effect前仍重讀active flag、schema readiness、lease及generation；多次toggle不得重覆provider mutation。

### 25.3 Files-only deploy及release gate
- **Plugin package identity（normative；第一個 implementation commit 已固定）**：folder／slug／main file固定為`hibilab-commerce/hibilab-commerce.php`，顯示名固定`HIBI LAB Commerce`，PHP namespace root固定`ScentM\HibiLab\Commerce`；唔可將交易政策塞返theme、另拆第二個HIBI runtime plugin或改slug造成翻譯／activation／更新身份漂移。Main file header第一版必須逐字包含以下欄位；`Version`係plugin release version唯一人手owner，runtime如需要只可用`get_file_data()`由main header讀取，禁止另維護一個不同version constant／option。Schema version係`UpgradeCoordinator` migration registry最後一個immutable migration ID，係另一條獨立序列，唔假設同plugin SemVer相等。初始受支援baseline以本交付核實嘅Scent.M Local WordPress `7.0.2`／PHP `8.2.29`及Woo穩定線`10.9.4`為準；所以最低header鎖WordPress `7.0`、PHP `8.2`、Woo `10.9`，tested manifest鎖精確`WordPress 7.0.2`／`WooCommerce 10.9.4`／`PHP 8.2.29`。較新版本唔自動視作支援，先更新manifest並跑本節完整matrix先可改header。`Requires Plugins`只列WordPress.org runtime owner `woocommerce`；官方Woo Stripe Gateway同ShipAny係launch integration readiness requirement而唔係plugin activation prerequisite，缺少／停用時HIBI commerce按既有dependency guard fail closed、Scent.M其餘功能照常，禁止為繞過dependency而自製gateway／shipment fallback。`Plugin URI`／`Author URI`／`License`／`License URI`係可選metadata，今次私人委託未有客戶批准公開source page或另訂授權文字，所以**刻意唔寫入header，Dev不得自行填GPL／proprietary字眼或虛構URL**；source identity由交付repo origin、release commit SHA及artifact checksum寫入compatibility/release manifest，法律／授權繼續由客戶同Dev既有委託合約管理。`Update URI: false`係刻意阻止WordPress.org同slug更新覆蓋，日後如批准私人update service先另作獨立決定。

  ```php
  /**
   * Plugin Name: HIBI LAB Commerce
   * Description: HIBI LAB policies and projections on top of WooCommerce.
   * Version: 0.1.0
   * Requires at least: 7.0
   * Requires PHP: 8.2
   * Author: Scent.M
   * Text Domain: hibilab-commerce
   * Domain Path: /languages
   * Update URI: false
   * Requires Plugins: woocommerce
   * WC requires at least: 10.9
   * WC tested up to: 10.9.4
   */
  ```

- **Release metadata同changelog（可執行、唔靠人記）**：repo root固定commit `CHANGELOG.md`，格式用Keep a Changelog式 `## [Unreleased]`＋`## [x.y.z] - YYYY-MM-DD`，版本用SemVer。每個release entry最少有實際適用嘅`Added|Changed|Fixed|Security`，另固定列`Schema/Migrations`（migration ID、是否backfill、rollback/data compatibility）、`Compatibility`（WordPress／PHP／Woo／Gateway／ShipAny／ACF精確tested版本）及任何operator action；冇內容嘅一般分類可省略，但`Schema/Migrations`同`Compatibility`不可省略。CI `tools/verify-release-metadata.php`必須讀main header作唯一plugin version，驗最新released heading完全相同、日期合法、compatibility manifest plugin version相同、migration registry最後ID同changelog schema欄一致，而且released version不可仍留`Unreleased`項；任何一項不符即release fail。`readme.txt`對呢個私人、files-only plugin唔係必要artifact；日後要上WordPress.org／Woo Marketplace先另行批准分發metadata，唔由Dev預先加scope。
- **可重現PHP quality gate（private plugin嘅固定QIT-equivalent，唔留畀Dev自選）**：implementation必須commit`composer.json`、`composer.lock`、`phpcs.xml.dist`、`phpstan.neon.dist`、`phpunit.xml.dist`、cross-platform `tools/lint-php.php`、`tools/verify-release-metadata.php`及CI workflow。Composer dev dependency由lockfile固定實際版本；constraints最少包括PHPCS 3.x、WPCS 3.x、PHPCompatibilityWP 2.x、PHPStan 2.x＋WordPress extension同PHPUnit 10.5.x（與PHP 8.2 baseline相容），升級dependency亦要獨立PR＋全閘重跑。`phpcs.xml.dist`固定套`WordPress-Core`、`WordPress-Extra`、`WordPress-Docs`及`PHPCompatibilityWP`（`testVersion=8.2-`），只排除vendor／generated language catalog；自訂table／Woo規範只可加嚴，唔可全域exclude security、escaping、nonce、prepared-SQL或i18n sniff。PHPStan固定level 8並載入WordPress／Woo stubs；不得用未解釋baseline掩蓋新錯。`composer.json` scripts固定：`lint=@php tools/lint-php.php`、`phpcs=phpcs --standard=phpcs.xml.dist --report=full`、`phpstan=phpstan analyse --configuration=phpstan.neon.dist --no-progress --memory-limit=1G`、`test:unit=phpunit --configuration=phpunit.xml.dist --testsuite unit`、`test:integration=phpunit --configuration=phpunit.xml.dist --testsuite integration`、`verify:release=@php tools/verify-release-metadata.php`，而`test`順序執行全部六項。CI實際command固定為`composer validate --strict`、`composer install --no-interaction --prefer-dist`、`composer test`，零PHP syntax error、PHPCS error／warning、PHPStan error、unexpected skipped／risky／incomplete test先pass；任何ignore必須逐行reason＋issue link＋到期release，唔准blanket baseline。
- **Woo quality matrix／QIT決定**：呢個係私人Scent.M plugin，冇Woo Marketplace extension identity／partner entitlement，所以**官方managed QIT唔係必要release dependency，Dev亦唔需要自行決定用唔用**；以repo committed integration／Playwright package作正式等價閘。Implementation固定commit`playwright.config.ts`、`tests/e2e/global-setup.ts`、`tests/e2e/fixtures/compatibility-manifest.json`同按§§22／25 journey分組嘅`tests/e2e/**/*.spec.ts`，Node dev dependency `@playwright/test`由`package-lock.json`鎖實；`package.json`固定scripts為`test:e2e=playwright test --config=playwright.config.ts`、`test:e2e:hpos=playwright test --config=playwright.config.ts --project=hpos`、`test:e2e:legacy=playwright test --config=playwright.config.ts --project=legacy`。CI兩個project各自建立一次性、production-like WP instance；global setup先read-back精確WP `7.0.2`＋PHP `8.2`＋Woo `10.9.4`及compatibility manifest版本，再以公開WP-CLI／Woo設定切換HPOS authoritative或legacy posts authoritative，任何版本／profile唔吻合即未開始測試已fail。兩個profile都跑fresh activation、deactivate/reactivate、files-only upgrade、REST/API、classic Cart／Checkout／Order Pay／My Account、§§22／25全鏈UAT同Scent.M workshop non-regression；官方Stripe Gateway、ShipAny、ACF同Mail Center用manifest精確版本activate，provider mutation只用隔離sandbox／fixture account。CI release job必須逐條執行`npm ci`、`npm run build`、`npm run verify`、`npm run test:e2e:hpos`、`npm run test:e2e:legacy`；零failed/flaky retry、零PHP notice／warning／deprecated、零unexpected network/provider mutation先pass，Playwright HTML/JUnit report同environment manifest保存為release artifact。若日後取得QIT entitlement，才把`qit.json`同`run:activation`、`run:phpcompatibility`、`run:security`、`run:woo-api`、`run:woo-e2e`作**額外**gate，唔取代上述project-specific tests，亦唔為咗QIT改商業流程。
- **Schema未ready連read path都隔離**：code/schema version mismatch、migration或backfill未完成時，target-schema repository不可畀front-end template、Admin list、REST或worker讀取，避免缺column／半backfill直接fatal或顯示假資料。Bootstrap只提供version-compatible maintenance projection（HIBI客面不可購買、Admin清楚顯示升級中／失敗原因、read-only既有Woo基本資料可見），Scent.M其餘功能照常；UpgradeCoordinator全部physical contract、backfill、read-back及高水位核對通過後先原子flip ready，正常repositories先可建立。
- Local→staging可files-only deploy嘅所有schema、ACF group、default option migration、capability migration、template、JS/CSS build source及dependency manifest都要committed；不可依賴Local DB先存在嘅field group、page option或secret。內容值／產品／訂單仍屬DB資料，按實際部署選項另行遷移。 WordPress「網站時區」固定用IANA `Asia/Hong_Kong`（唔用手動UTC+8），Woo store address／香港四區shipping zones同production URL亦列入environment validation；staging copy後自動check，漂移就阻止HIBI mutation並提示修正。 Versioned provisioning／release check亦要建立或核對Woo core Cart／Checkout／My Account pages及classic shortcodes、`/hibi-lab/` landing、Shipping政策page、product permalink base、香港四區shipping zones及必要top-level categories／term ACF seed；rewrite只喺versioned migration完成後flush一次，唔喺request重覆flush。內容可以留畀Admin填，但page／route／zone／attribute不存在就release fail，避免files-only staging出404或空設定。
- **Code-only deploy upgrade coordinator**：activation hook唔係唯一migration入口。Plugin bootstrap只做中央version／readiness check；code／schema mismatch即只將HIBI mutation fail closed，Scent.M其他功能照常。單一`UpgradeCoordinator`取得上文唯一`hibilab_site_guard`（`active_kind=upgrade`）後，按checkpoint執行bounded、versioned migration；長backfill分batch並由Action Scheduler續行，business request handler唔可自行`ALTER`／補column。Fresh install、files-only upgrade、WP-CLI／release command共用同一coordinator；全部schema、backfill、physical contract及read-back通過先原子更新ready/version，marker write fail可重試而唔假ready，舊code遇較新schema亦fail closed。
- **Classic Checkout鎖定**：plugin用Woo `FeaturesUtil::declare_compatibility('cart_checkout_blocks', ..., false)`明示目前唔支援Cart／Checkout Blocks，plugin header／compatibility manifest記`WC tested up to`；versioned provisioning核對Cart、Checkout及Order Received仍由Woo classic shortcodes／endpoints承接，唔容許Woo onboarding或更新靜默換成Blocks而繞過已批准hooks、best-wins、預訂同意或ShipAny picker。
- **Security advisory release gate**：每個release由實際staging inventory匯出WordPress core、theme、MU／normal plugin、Composer同npm package精確版本，對WordPress.org／WPScan或WP Engine所提供嘅WordPress advisory、WooCommerce／Automattic及各供應商官方security advisory、Composer audit同`npm audit`逐項核對；結果、查核時間、source及處置寫入compatibility／release manifest。任何影響已安裝版本而未有受支援mitigation嘅critical／high advisory，或任何已確認P1／P2 security finding，一律block release；唔可以因功能smoke pass就豁免。只有升級／停用受影響component、供應商正式patch或有期限兼已批准嘅vendor-documented mitigation先可關閉，並要喺staging重跑相應payment／checkout／email／ShipAny regression。呢個gate只核對現有依賴，唔為掃描而新增runtime security plugin。
- 每個release固定依次跑：`composer validate --strict`、`composer install --no-interaction --prefer-dist`、`composer test`、`npm ci`、`npm run build`、`npm run verify`、`npm audit`、`npm run test:e2e:hpos`、`npm run test:e2e:legacy`；當中已明確涵蓋PHP lint、PHPCS、PHPStan、PHPUnit、Woo integration tests、HTML／JS／local-link／duplicate-ID static checks及兩個Woo data-store profile嘅Playwright journey，禁止用一句「已跑測試」代替command evidence。之後再做manual responsive pass、email client render、WP Engine cache／cron／backup驗證。Browser support matrix：latest stable＋previous major Chrome／Edge／Firefox、current＋previous Safari／iOS Safari、current Android Chrome；付款及ShipAny picker至少iOS Safari＋Android Chrome真機。Email驗Gmail web/app、Outlook desktop/web、Apple Mail/iOS Mail；唔要求逐pixel一致，但功能、可讀性、無overflow及所有主要action必須完整。Woo store設定亦入machine-readable/checklist驗收：currency=`HKD`、decimal places=`2`、taxes=`disabled`（Phase 1只售香港、香港無VAT／sales tax；如日後跨境或法規改變另作已批准release），frontend／Cart／Checkout／Account／Email／CSV輸出完全一致。Production前以與production同版本嘅staging跑§22付款／退款／預訂／ShipAny／email／VIP全鏈UAT；Static HTML同handoff本身**唔等於**已驗證WordPress implementation。
- Launch／rollback清單要記實際Scent.M commit、plugin/schema version、Woo／Stripe／ShipAny／ACF版本、migration result、manual backup checkpoint、webhook health、Action Scheduler heartbeat、Resend delivery及最後UAT證據。任何client待決項、ShipAny compatibility blocker、真實gateway／email／自取點未測或已知P1／P2 finding未關閉，都不可標Production Ready。

### 25.4 WP Engine cache、restore及災難復原
- **Cache matrix**：只准真正anonymous、冇登入、冇Woo session，而且只含公開catalog facts嘅landing／catalog response入shared page／object cache。Cart、Checkout、My Account、order-pay、order-received／guest order view、wc-ajax／個人化REST、unsubscribe／token頁，以及任何已登入、Woo session、VIP／生日／會員搶先個人化response，必須用Woo／WP Engine受支援exclusion＋`DONOTCACHEPAGE`／`Cache-Control: private, no-store`；如catalog server-render個人化價而無法證實每個登入／session request自動bypass，就將相應`/hibi-lab/`路徑排除full-page cache。Cache key、URL log及analytics不得含PII、order key或token；唔用IP、timer或query-string workaround。
- **Backup／restore契約（客戶批准7B）**：production必須有每小時自動database backup，目標**RPO≤1小時**；WP Engine每日完整backup照常，deploy、plugin／Woo／Stripe／ShipAny更新及schema migration前另建manual checkpoint。Restore前停HIBI mutations及provider workers，只還原互相匹配嘅code＋DB checkpoint，重綁目標環境secret／webhook並驗hostname、test/live及account。由backup timestamp起，以官方Woo Stripe gateway可驗證read truth、ShipAny public truth、Resend event／read API truth及canonical rows逐筆做read-only reconciliation；Action Scheduler舊action只可靠semantic key／generation no-op，絕不因restore盲目重call付款、退款、收貨、寄信或出貨。所有backup後provider outcome收斂或開可見reason-scoped incident先解除maintenance；staging restore永遠阻止live provider mutation及真客email。復原目標**RTO≤4小時**；每季喺staging由最近真實backup做restore drill，保存backup time、開始／完成時間、資料read-back、provider reconciliation、RPO／RTO結果及跟進owner。任何一次未達標都係release／operations incident，唔可以只記「做過演練」。

### 25.5 安全、供應商及可量度營運閘
- Compatibility manifest同release checklist記Woo、Gateway、ShipAny、Resend、Action Scheduler、ACF、WordPress、PHP及WP Engine版本／owner／support link；production帳戶強制MFA、最小權限、離職撤權及定期key rotation。**每類credential只跟官方／現有owner一個canonical store**：Woo Stripe connection只留喺官方gateway支援設定、ShipAny token只留喺官方plugin設定、Resend只由site-wide Mail Center保存；HIBI只存nonsecret account/profile generation、masked fingerprint及最後驗證時間，唔複製secret。Admin可編輯嘅credential必須以環境binding＋server-side encryption-at-rest保存，DB clone後hostname／environment binding不符即自動失效並要求網站管理員喺目標環境重新輸入，禁止fallback；provider本身支援嘅official encrypted connection store照原生，不另搬去自製vault。Resend SMTP/send使用獨立、domain-scoped `sending_access` credential；delivery reconciliation因provider目前冇read-only key而使用另一個`full_access` credential，呢個provider限制必須喺設定頁及manifest誠實標示。Full-access secret只供網站管理員設定及server-side專用adapter持有，adapter只暴露allowlisted bounded GET email／必要分頁list方法，絕不暴露send、delete、domain或generic mutation client；兩把key分開rotation及audit。Tracking按私隱決定保持關閉；前端／Git／log永不出secret，健康頁只顯示masked fingerprint／最後成功時間。
- **Provider-independent external health contract（客戶批准8A）**：公開read-only `GET /wp-json/hibilab/v1/health`只讀已持久化readiness、schema／connection generation、critical heartbeat同oldest-due projection；**唔喺health request call Stripe／ShipAny／Resend、唔排job、唔修資料**。正常回HTTP `200`＋固定schema `{status:"ok", generation, checked_at}`；maintenance、schema mismatch、critical heartbeat逾§25.2 SLA或任何會阻止付款／履約嘅open critical incident回HTTP `503`＋`{status:"degraded", reason_code, generation, checked_at}`。Response不得含PII、secret、raw error、provider ID、訂單／用戶數或可推算交易量嘅數字，並`Cache-Control: no-store`；rate limiting由WP Engine edge負責而唔用browser cookie／IP做業務身份。另用**獨立於WP Engine、WordPress、Action Scheduler、Mail Center及Resend**嘅外部uptime monitor每`5分鐘`檢查主公開頁＋health endpoint；連續`2`次失敗先alert、連續`2`次成功先發recovery。Primary＋backup alert recipient係production deployment必填設定／runbook欄位，交付時由客戶填實際地址，文件及code**唔虛構email**；缺任何一位即release fail。站內Admin notice／營運email只係secondary signal，唔可取代外部monitor。Monitor驗收包括DNS、TLS、200→503、timeout、fatal、maintenance及recovery通知。
- Production headers經staging驗證後設定HSTS、`X-Content-Type-Options: nosniff`、合適`Referrer-Policy`／`Permissions-Policy`及CSP（先report-only再enforce），唔阻Stripe、ShipAny、YouTube consent gate或Scent.M既有資源。內部監察最少包括無扣款checkout smoke、Action Scheduler heartbeat／oldest due、webhook失敗率、email terminal failure、PHP fatal同WP Engine uptime；每項alert有owner、severity、acknowledgement及incident runbook，並同上文外部monitor互補而唔互相依賴。
- Performance以真實手機及production-like staging驗Core Web Vitals：p75 LCP≤2.5s、INP≤200ms、CLS≤0.1；同時記PHP／DB慢query、cache hit及checkout latency baseline。任何達唔到要有已批准改善或明確release blocker，唔用prototype本機速度代替。

---

*本文件＝現行單一真相源（交付時以指定commit hash凍結）。已批准spec總覽：店中店套落Scent.M theme（§0）、WooCommerce data-driven、PDP=Flexible Content（§15）、filter全custom in-theme（§4）、分類入口動態化（§3）、guest checkout允許＋登入會員享自動會員價（§17）、**VIP等級・完全後台可設定・生日月自動加成・會員搶先（§5）**、**會員價對coupon二選一取最抵・一次一張（§6）**、**預訂第三庫存態・即收錢・per-product到貨期・必填同意・混車等齊寄・Admin收貨`+N`＋FIFO自動分配＋全單ready先出貨（§19/§19.1）**、**補購提醒opt-in・自由排程・購買重錨・循環直至暫停/取消・會員限定・無自動扣錢（§9/§22/§25）**、**GA4＋硬性cookie同意（§21.15）**、**ShipAny Phase 1・建面單與實際寄出分離（§7）**、404 URL分流（§2）、Landing 16px豁免＋771px store-scoped（§10/§22）、後台文案港式書面語（§21.6）。§§21–25一併構成normative contract；任何直接衝突都必須先修正，唔准由接手Dev自行估。*
